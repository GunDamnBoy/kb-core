# 投顧知識庫｜沿革與維護判斷

給**維護者**讀，不進執行路徑。
記三件事：**事故經過、當初為什麼這樣決定、被否決的選項**。
每一筆用五欄：動到哪些檔／量測／怎麼驗的／怎麼倒回去／當時已知的風險。

---

## 維護判斷（不隨版本改變的那幾條）

這四條在舊系統是血淚換來的，但它們**不是執行端該讀的東西** ——
執行端每天照規格做，這些是「規格本身該怎麼改」的判斷。放這裡，不放 BRIEF。

### 改任何數字之前，先問它現在有幾份副本 —— 包含值相同的那些

單錨原則的操作版。權威值住在會被執行的東西裡（`anchors.json`、檢查程式），文件只指路。

**判準是「有沒有第二份副本」，不是「流程有沒有變」。**
2026-08-16 傍晚把一般卡字數由 650–800 放寬為 550–900，判斷「未動流程故不重推 prompt」，
結果執行端拿舊數字寫、被新標準量，**而且不會報錯**。隔天中位 504、59/82 低於下限。

再隔一天收斂只收一半（brief 改了、排程 prompt 第 5 步還抄著 350–450），
中位從 504 掉到 441 —— **收斂一半比不收斂更糟，因為它看起來像已經處理過了。**

同一輪的對照組：深度卡的數字**在全系統只有一個家**，8 張全部落在區間內、範圍僅 926–1078。

### 加長卡片便宜，多收卡片貴

2026-08-14 實測：採集佔全輪 60.5%、讀網頁 12.3%（都不受卡片長度影響），
撰寫輸出 ＋ 寫入快取只佔 17.3%。**要提高價值密度就加長，不要加量。**

「卡片文字 +50% 約等於全輪 +2%」是估算，**至今未被用量檔驗證** ——
若實際超過 +5%，回頭查是不是連帶推高了別的東西。

### 一次只動一個變因，而且要看過程指標

2026-08-11 一次改四件事，8/13 壞掉時無法歸因。

**單日結果不能證明規則有效**：每則 16.2 萬 vs 28.3 萬 token，同一份規格。
要看回合數這種過程指標。

**動生產文件前要有回滾點。**

### 來源去留看歷史產出，不看單日結果

判斷依據是跨期的產出紀錄，不是今天拿到幾則。

**風控有兩種，處置不同**：
「進門就擋」（Reuters，八天七天零產出）→ 移除來源；
「自傷型」（Bloomberg，平時全站最大來源）→ 改採集方式。

**用手動測試推論自動流程的可用性是錯的** ——
手動能通、自動被擋，正是反自動化風控的預期行為。

---

## 2026-08-19｜lock 被外部視角寫掉兩條，而它報成「新增」

**動到哪些檔**　`checks.lock`（還原 `advisory.card_vocab`、`advisory.index_entry_source`）。

**事故經過**　18:08 的 `67c6a4f` 在本機加了兩條檢查，並正確地把它們寫進 lock（28 條）。
22:28 的 `580f1df` 要收 `watch.external_binaries`，lock 卻是**從另一台機器的工作副本產生的**——
那份副本只有 12 條 advisory 檢查。結果 lock 少掉兩條、多一條，變成 27 條。

**它是怎麼騙過去的**　`--selftest` 把「註冊表有、lock 沒有」一律印成
**「新增的 check id」**。差集只有方向，沒有成因：
`card_vocab` 與 `index_entry_source` 明明是**十小時前就存在並鎖過**的檢查，
被讀成剛寫好的新東西。**看板上寫著「有東西加進來」，實際發生的是「有東西被拿掉」。**

再加一層：當時的指令是 `--selftest | tail -3`，兩條裡的 `card_vocab` **被 tail 截掉了**。
看到的是一條來路不明，真相是兩條被刪。

**怎麼驗的**　不是看 lock 的行數（27 看起來很正常），是 `git log -S` 問
「哪一個 commit 改變了這個字串的出現次數」——它直指 `580f1df`，
而那個 commit 的訊息裡完全沒提到 advisory。**檔案的修改史比檔案本身誠實。**

**怎麼倒回去**　在本機跑 `--write-lock`，diff 必須恰好是 +2、沒有別的。已驗。

**當時已知的風險**　這兩條檢查在這段期間**照常執行**，lock 不擋執行、只擋察覺。
但若期間有人從 `checks/advisory.py` 刪掉它們，lock 不會叫——
**安全網上被開了兩個洞，開洞的正是那個要來補網的 commit。**

**規則**　`checks.lock` 是**執行機器上註冊表的產物**，不是可以在別處編輯的清單。
任何不是由該機器 `--write-lock` 產生的 lock 內容，一律視為污染。

→ 通則：**差集不等於成因。** 「A 有 B 沒有」有兩個方向的解釋，
工具只挑了對它方便的那個講。凡是把差集講成敘事的地方，都要反問另一個方向。

## 2026-08-19｜合規紅線縮為三條：這是決定，不是遺漏

`67c6a4f` 把 §5 由五條縮為三條，拿掉的是「付費內容一律從已登入的訂閱讀、
不用鏡像站與快取」與「偵測到繞牆擴充套件的注入痕跡就換篇並具名記錄」。

**查證過它不是搬家**　全 repo 掃 `archive.ph`／`archive.today`／`12ft`／`freedium`／繞牆，
`.md`／`.json`／`.py` 全過一遍，唯一命中是一句無關的 reCAPTCHA 換源理由 ——
它確實不在任何其他檔案裡，是真的拿掉了。

**裁定（Kenny，2026-08-19）**　**以今天在 advisory 設定的三條為準，不還原。**
曾提議還原，被否決。這裡只留記錄，是為了不讓下一個維護者把它誤判成
lock 那類的意外刪除、再「修」回來一次 ——
**同一天有一個真的是意外刪除（lock），有一個不是（這條），外觀一模一樣。**

**權威版本在哪**　`scripts/advisory/preamble.md` §5。這份沿革不是規格，別回頭抄它。

**留下的已知盲區**　繞牆擴充在 Bloomberg、WSJ、Barron's 上若生效不留痕跡，
所以「完整取得」分不出內容來自訂閱還是外掛，事後看卡片也回溯不了。
這是接受的取捨，不是待辦。

## 2026-08-19｜重寫：四份文件取代舊的三份

**動到哪些檔**
新增 `advisory/BRIEF.md`、`advisory/anchors.json`、`skills/advisory/SKILL.md`、
`scripts/advisory/preamble.md`、本檔。
取代舊的 `AGENT_BRIEF.md`、`MAINTENANCE.md`、以及不在 git 的排程 prompt。

**量測**

| | 舊 | 新 |
|---|---:|---:|
| 執行端每次讀的量 | ~35,000 token（AGENT_BRIEF） | ~15,800 token（四份合計，且採集端只讀 preamble 的 ~4,000） |
| 文件總量 | 116,806 字元（BRIEF＋MAINTENANCE＋CHANGELOG） | 17,367 字元 |

縮減主要來自搬家不是刪除：取數規格與來源眉角 → `preamble.md`；發布步驟 → `publish.py`；
門檻數字 → `anchors.json`；沿革與事故 → 本檔；待決 → GitHub issue。

**怎麼驗的**
拿舊規格卡的「必須原封保留」清單逐條做關鍵字覆蓋稽核，31 條中 27 條在四份文件裡找得到家，
4 條落在本檔（就是上面那四條維護判斷）。**稽核腳本要能重跑** ——
規格改動之後這 31 條的覆蓋率是可回歸的。

**怎麼倒回去**
舊的三份文件在 Google Drive 的「重寫規格保存包」，逐字保留、未經改寫。

**當時已知的風險**
新 BRIEF 只有舊的九分之一，**這個數字本身不是好消息** ——
它可能代表搬對了，也可能代表 judge 真正需要的東西被丟掉了。
第一週實跑要盯的是「執行端有沒有問出規格沒回答的問題」，而不是產出對不對。

**被否決的選項**
把門檻數字寫進檢查程式的 Python 常數、由 BRIEF 用 `--spec` 印出來。
否決理由：**採集 subagent 讀不到 Python 常數**，而它正是最需要配額數字的讀者。
三個讀者（檢查程式、派工步驟、採集前言）都能讀的東西只有資料檔。

---

## 2026-08-19｜保底層實跑：十個端點，四發

**動到哪些檔**　`kbcore/fetch_tw.py`（新）、`tools/fetch_advisory.py`（新）、
`kbcore/fetch.py`（route_of）、`requirements.txt`（新）、
`advisory-rewrite/.github/workflows/fetch-floor.yml`（新）、`preamble.md` §8。

**量測**　四發之後 10/10，全部有真資料。六個台股端點**第一發就全通** ——
包括舊系統用 Chrome 撞出兩個具名坑的那幾個（`navigate` 到 JSON 被當檔案下載、
跨子網域 `fetch` 被 CORS 擋）。**那兩個坑在伺服器端不存在，不是被繞過。**

**怎麼驗的**　看的不是退出碼，是**最後一列有沒有真的數字**。第三發是 10/10 ok、
`failed_essential: []`、workflow 綠 —— 而黃金那兩筆是免責聲明頁的 78 列 None。

**怎麼倒回去**　保底層是新增的，停用它只要停掉 workflow；Mac 那一輪不依賴它就能跑，
只是衛星組會回到「現場抓」。

**當時已知的風險**　XLSX 的工作表挑選是啟發式的（非空格最多的那張）。
SPDR 若哪天多加一張有資料的工作表，會挑錯而且不會報錯。

**被否決的選項**　用 stdlib 的 `zipfile` ＋ XML 自己解 XLSX，避免第三方依賴。
否決理由：日期序列號、共享字串、inline string 三種情況都要自己處理，
而這是每天要跑的生產路徑。改成把 `openpyxl` 明文寫進 `requirements.txt`——
**依賴要有一個宣告的家，不要散在某個環境裡。**

## 一個錯誤，一天犯四次

| # | 我讓什麼太寬鬆 | 結果 |
|---|---|---|
| 1 | 編碼串聯放 `latin-1` | 它什麼都解得開 → `raise ParseFailed` 是死碼 |
| 2 | `sniff` 先 JSON 再 CSV | CSV 幾乎不失敗 → `sniff` 的 `raise` 是死碼 |
| 3 | `parse_csv` 只看解不解得開 | 二進位檔解成一行亂碼欄名 → ok |
| 4 | `parse_xlsx` 只看讀不讀得開 | 免責頁 78 列全 None → **ok，必要項失敗是空的** |

前三次是分支打不到，第四次**產出了一個完整的成功狀態**：退出碼 0、workflow 綠、
`failed_essential: []`。沒有印出最後一列的話，那一輪會被當成保底層完成。

四次的共通問法只有一個：**我剛才那個 ok，是憑什麼判的？**
「讀得開」不是「有資料」，「解析成功」不是「拿到東西」。
**每一個成功判定都是一道門檻，門檻設得太低就會放行空的東西。**

→ 這條該進跨系統通則，它比「靜默失效」更精確：它指出靜默是從哪裡進來的。

---

## 2026-08-19｜首輪啟動前中止：規格落地了，執行環境沒有

首輪儀表板手動啟動，**在派工之前中止**。三個阻斷都不是內容問題，
共通點是：**規格與檢查都改對了，但改動停在 `~/outbox` 的 handoff 包裡沒有落地。**

**動到哪些檔**
新增 `scripts/advisory/preamble.md`、`advisory/CHANGELOG.md`（兩份都取自
`~/outbox/_handoff_day1.tar.gz`，逐位元組相同）。
新增 `~/outbox/_handoff_kbpublish_advisory.plist`（未套用，等人決定）。
`checks/advisory.py` **同日先不動、後改動** —— 見下方「被否決的選項」。

**量測**

| 阻斷 | 證據 | 若照跑會怎樣 |
|---|---|---|
| 1. publish 指向 kb-tracer | 兩份 plist 的 argv 都是 `/Users/macmini/kb-tracer` ＋ `kb-tracer`；`~/outbox/2026-08-19.receipt.json` 說 exit 0 already-published `f12531c`，而 `advisory-rewrite/data/` 是空的、git log 沒有任何 data commit | 投顧草稿被 tracer 的 suite 驗過，寫進 tracer repo 並蓋掉它的 index.json |
| 2. `watch_review` 會 FAIL 不會 SKIP | repo 版 `_watch_review` 只有 `wr is None → fail`；`p.get("prev") is None → skipped` 那兩行只存在於 handoff 版 | 首輪 exit 10，或被迫編造 watchReview |
| 3. `preamble.md` 不在 repo | 全 repo 搜不到，只在兩個 handoff 包裡 | 十五組採集 subagent 帶著空的來源清單出門 |

**怎麼驗的**
不是看檔案在不在，是**看它跟該有的版本一不一樣**。`cmp` 逐位元組比對落地結果與
handoff 來源；`awk` 切出 `_watch_review` 函式體再 grep，確認豁免那兩行真的不在。
`grep -c 'prev is None'` 回 1 差點被當成「修補已套用」—— 那個 1 是
`_cross_version_dupe` 的，而修補版寫的是 `p.get("prev") is None`，**字串形態不同、
grep 根本配不到**。用一個配不到的樣式去證明「東西不在」，會得到正確答案與錯誤的理由。

**兩個 handoff 包的 `preamble.md` §8 不一樣，而且新的那份推翻舊的那份**：
`_handoff_advisory_spec.tar.gz` 說「日變化自己算，前一日噸數寫在前一版的黃金保底卡裡」，
`_handoff_day1.tar.gz` 說那個跨檔依賴已經廢除、直接從 `raw/` 的序列算。
以 day1 為準。**handoff 包沒有版本號，靠檔案時間與內容判新舊是會出錯的。**

**怎麼倒回去**
兩個新增檔刪掉即可，沒有任何既有檔被修改。plist 是新檔名、未 load。

**當時已知的風險**
`~/outbox/2026-08-19.receipt.json` 與 `2026-08-20.receipt.json` 是 tracer 留下的，
exit 都是 0。**首輪等回執時如果只看檔名與 exit，會讀到別人的成功。**
真正該比的是 `at` 要晚於草稿寫入時刻。

publish.py 用 `outbox.glob("*.draft.json")` 掃**整個** outbox，一輪只帶一個
repo ＋ 一個系統 id。所以**兩套系統共用 `~/outbox` 本身就是設計缺陷** ——
先到的那個 job 會拿它自己的 suite 去驗別人的草稿。重新指向只是把問題換一邊，
沒有解決它。

`skills/advisory/SKILL.md` 同樣不在 repo（只在 handoff 包裡），目前由 plugin 快取
路徑供應。不擋執行，但它代表**執行端讀的那份規格不在版本控制裡**。

**被否決的選項，以及它同一天被推翻**

原本要否決的是：自己套用 handoff 的 `watch_review` 跳過修補，讓首輪能過關。
否決理由不是技術的 —— 那條檢查決定我自己的產出過不過，
**改它的人不該是被它擋住的人**。所以提出來讓人點頭，第一次的答覆是不改：
第一天就不寫 watchReview，把 exit 10 當成第一天的真實狀態記在案。

**幾分鐘後推翻，因為多讀了一段 `publish.py`。**
`draft_path.unlink()` 只在 `Exit.OK` 那條路徑上。exit 10 不刪草稿，
launchd 每 60 秒重掃一次 outbox、每次重驗、每次重寫回執 ——
「第一天乾淨地失敗一次」實際上是**每分鐘失敗一次直到有人手動移走草稿**。
把這件事講清楚之後，答覆改成套用修補。

兩件事值得記住。其一：**那個決定第一次做的時候，兩邊拿到的事實是不完整的** ——
不是判斷錯了，是資料不夠。其二：exit 10 的重試迴圈是**所有內容失敗共有的**行為，
不是 watchReview 特有的。它現在沒有任何文件寫著，第 8 步的退出碼表只說
「改草稿重寫，回步驟 6」，讀起來像一次性的事件。**下一個踩到的人不會有人提醒。**

→ 待決：退出碼表該補一行說明 exit 10／11 不刪草稿，或者讓 publish 在內容失敗時
把草稿移進 `~/outbox/rejected/`。後者比較好，因為它讓失敗自己停下來，
而不是靠讀文件的人記得。

---

## 從舊規格帶過來的三個矛盾（待決）

重寫時逐條讀舊規格卡發現的，**原值照抄未改** —— 重寫不重訂數字。

### 1. 兩處組名在舊規格裡對不上

下限表寫「金融併購與企業」「亞太（韓印東南亞）」，
配額表寫「金融、併購與企業」「亞太（韓國、印度、東南亞）」。

諷刺的是舊規格自己明文警告過：
「分組名必須與 QUOTA 的鍵逐字相符，全形括號與頓號差一個字就會被判分組名不在表內」。

**處置**：以 QUOTA 版為準（舊規格自述權威值在 QUOTA），`anchors.json` 的十五個 `name`
現在是全系統唯一的組名來源。

### 2. 黃金組零餘裕

配額 3 ＝ 下限 3（週末 2 ＝ 2），違反「配額一律高於下限」。

**待決**：黃金的下限有一則是保底數據卡（每天必定拿得到）。
若視為確定性素材則零餘裕合理；若不是，配額該提到 4。**第一週實跑後回頭判。**

### 3. 已停用來源的渲染資產同時出現在兩張清單

`ked`／`reuters` 的 CSS、`BADGE`、`SRCOK`
在舊規格卡裡同時被列進「必須原封保留」（舊封存要渲染）與「可以丟掉重來」（新系統不遷移舊封存）。

**待決**：取決於新站要不要吃舊封存。**這是外殼（`index.html`）的問題，不是資料的問題** ——
新的 `data/<date>.json` 一律用具名的來源代碼，不受影響。

## 2026-08-20 夜｜3 小時 45 分：一塊是派工拆錯了，一塊是在錯的機器上做的診斷

使用者的判準很清楚：舊版約兩小時，這一輪 3 小時 45 分，不能接受。
拆開來看有兩件互不相干的事，而**第二件是憑空多出來的**。

### 一、派工的「誰交」那一欄，重寫時掉了

`SKILL` 步驟 3 寫「依 `anchors.json` 的 `quota` 分派」，而 `anchors.groups`
只有 `name`／`tier`／`floor`／`quota`／`section` —— **沒有任何欄位說「誰交」**。
在那個輸入下，能分派出來的就只有「一組一個分身」。於是十三個分身、逐一跑完。

步驟 4 整段是「同上。」。「第一批」「第二批」在全份文件裡從來沒有被定義成哪幾組。
**警告留下來了**（「七組齊發會互相回收 Chrome 分頁」「2026-08-11 三組失敗率 65–70%」），
**機制沒有。** `preamble` 第七節的標題還寫著「台股官方端點（**F 組**）」——
`F 組` 是舊制七個來源型採集員的代號，代號是這一制留下的唯一痕跡。

> **一條規則的「理由」與「做法」要一起搬。** 只搬理由，下一個讀的人會同意它、
> 然後做出一個違反它的東西，因為他不知道原本的做法長什麼樣。

對過帳才確定舊表可以原樣放回去：**十五組的下限與配額，一個數字都沒有變。**
放回 `groups[].owners` 之後加總，得到 `A22・B18・C19・D14・E17・F12・G12 ＝ 114`
與週末 `89` —— 與舊規格分開寫的那行合計逐字相符。那行是獨立對照，不是套套邏輯。

**採集員的合計刻意不寫進 anchors**，要用就加總（同 `site_total._no_sums`）。

**來源歸屬是新寫的，不是還原的。** 舊規格只留代號，A–G 各讀哪些來源在另一份
`subagent_preamble.md`，而舊規格自述「v1–v13 的 prompt 已不可考」。
重建依據只有三條實錄：A 打 Bloomberg（8/09 風控）、E 讀 IBD（8/10 讀完六篇才發現全在窗口外）、
F 只做台灣且鉅亨 ≥4／MoneyDJ ≥3。其餘依題材配。
**真正要守住的不是我配得準不準，是「一家來源只屬於一個採集員」這個性質** ——
單來源 15 篇的上限與「不要同時打同一家」，只有在獨佔時才守得住。
那條由新的 `advisory.dispatch_wellformed` 驗，七種失效分支逐一測過：
漏指派、合計不等於配額、指給不存在的採集員、來源被兩人共用、採集員不在任何批次、
批次裡有幽靈、採集員沒有來源。

### 二、保底層那半小時，是拿錯的機器做的診斷

執行報告第五節第 1 點寫：

> `raw/2026-08-20.json` 在本輪執行時不存在……沙箱重跑 `tools/fetch_advisory.py`
> **十個端點全滅**……`FRED_API_KEY` 未設定

三個結論全錯：

| 報告寫的 | 實際 |
|---|---|
| 檔案不存在 | **存在**，1,577,408 bytes，`fetched_at` ＝ 台北 2026-08-20 **07:02:09** |
| `FRED_API_KEY` 未設定 | Actions 的 repo secret 有設，`fetch-floor.yml` 第 47 行在用它 |
| 十個端點全滅 | `failed_essential: []`、`failed_other: []` —— **十個全部成功** |

真正發生的是：**Mac 的本機 clone 還沒拉到那個 commit。** 這一輪刻意不跑 git，
所以工作區是一份會落後的影子；`fetch-floor` 07:02 推上 origin，本機要等 `publish`
下一次 rebase 才會有。步驟 1 寫的是「`raw/<今天>.json` 存在」——**它檢查的是影子。**

接著的動作把一個同步延遲放大成半小時的工：在沒有 `FRED_API_KEY`、對外走被擋代理的
沙箱裡重跑取數，拿到十個失敗，據此宣告保底層壞了，然後用 Chrome 一個一個手抓
**早就躺在 origin 的那十個端點**。連帶產出一條指向不存在的問題的待修事項。

> **這是今天第八次撞到同一族：方法量的不是它宣稱在量的東西。**
> 「我在沙箱裡跑了 `fetch_advisory.py`，它失敗了」與
> 「保底層失敗了」之間，隔著一整個環境。
> 而失敗的樣子非常有說服力 —— 十個端點、兩種明確的錯誤分類、一條看起來很具體的待修事項。
> **一個錯誤診斷的可信度，跟它對不對無關。**

修法寫進步驟 1：判準改成 `fetched_at` 與 `failed_essential` 而不是「檔案在不在」；
本機沒有就用 Chrome 讀 `raw.githubusercontent.com` 帶 cache-buster；
**明文寫「不要在這台機器上重跑 `fetch_advisory.py` 來判斷 Actions 跑得如何」**。

### 三、順帶處理的

- **`SKILL.md` 納入 `kb-core/skills/advisory/`。** `BRIEF.md` 第 12 行一直指著這個路徑，
  而這個目錄先前不存在 —— 舊規格的教訓是「改了數字卻沒重推 prompt，就是隔天的漂移」，
  而當時連可以對照的檔案都沒有。現在 `watch.no_code_drift` 也看得到它。
- **步驟 9：報告要交分段耗時。** 舊規格記得出「8/05 採集約 38 分鐘、補位 30 分鐘、
  撰寫約 25 分鐘」，新報告只有總時長。**沒有分段時刻，下一次壓時間只能用回合數猜**，
  而 `anchors._source_rule` 明訂「沒有出處的數字不算量測過」。
- **preamble 第六節補八項選擇器與路徑變更**（MarketWatch、IBD、Nikkei、TrendForce、
  Politico、鉅亨、MoneyDJ、華爾街見聞）。每一項都曾以「這家今天讀不到」的形式出現過，
  而實際上是選擇器沒對上或路徑搬家。
- **preamble 第一節重寫**：原本寫「你的配額只認你**這一組**」，那是分組制的說法。

### 預期

用報告裡的回合數估（`~`）：現況十三個逐一 ∝ ~1,251 回合；七個分兩批 ∝ ~400。
**這是估的，不是量的** —— 明天那一輪的分段耗時表才是第一個真數字。

還有一個未拆開的變因：這一輪窗口是 31.5 小時（使用者早上暫停排程、下午才開跑），
而非例行的 24.5 小時，素材多了約三成。**下一輪要在正常窗口下量，才能歸因。**

## 2026-08-20 夜｜總覽的第二層在重寫時掉了，而渲染器和寫的人各講一種方言

使用者說舊版「標題底下都還有相關敘述，比較完整」。拿站上二十天量一次，
那不是懷舊，是**一個有日期的迴歸**：

| 欄位 | 舊十九天 | 8/19（重寫第一天） | 8/20 |
|---|---|---|---|
| `pulse[].note` | 45 筆全有，中位 30、最大 40 | 有 | **五筆全缺** |
| `thermo.note` | P25 116、中位 146、最大 301 | 50 | **9** |
| `takeaways` 帶粗體 | 126/140 ＝ **90%** | 0/7 | **0/7** |
| `takeaways` 字數 | P25 117、中位 149 | — | 51–96，全部低於舊 P25 |

`thermo.note` 那一欄值得單獨看：全庫第二短的是 116 字，最短的是 9 字，
**中間沒有任何東西**。這不是「今天寫得比較簡潔」，是一整層不見了。

### 渲染器與寫的人講不同方言

`watchReview` 在站上有兩種形狀：

```
57 筆（舊制十九天）  (d, note, t, verdict)   t＝原文、note＝回顧
 8 筆（8/20）        (d, t, verdict, w)      w＝原文、t＝回顧
```

**重寫時改了欄位名，也對調了語意，而 `index.html` 沒跟著改。**
它讀 `w.t` 當原文、`w.note` 當回顧 —— 在新資料上，`t`（回顧）被畫在原文的位置，
而原文（`w`）**沒有任何地方讀它**。讀者看不到當初預測了什麼，
只看到一段沒有前提的回顧。兩邊都不會報錯，因為缺席的欄位在 JS 裡只是 `undefined`。

修法是讓渲染器**雙方言**，判準是「有沒有 `w`」，判讀集中在一個地方並註明理由 ——
**歷史封存不回頭改**，所以 adapter 是永久的，不是過渡措施。
拿 8/20（新）與 8/18（舊）各渲染一次、截圖看過：兩者都是兩層，舊的十九天沒被弄壞。

（截圖前先踩了一次自己的坑：hash 格式是 `#<日期>/<分頁>`，我寫成 `#/<日期>`，
於是日期被塞進分頁欄位、兩張截圖位元組數完全相同。
**兩個「不同」的輸出長得一模一樣，就是測試沒有製造出它宣稱模擬的狀態。**）

### 門檻是量出來的，不是抄的

新的 `overview_prose` 每一個數字都有出處，而且抓到一個抄來的死數字：
`lengths.pulse_note_max` 寫 30，**而實測四十五筆的中位就是 30、P75 34、最大 40** ——
照它訂會讓一半的歷史資料超標。已改為 `pulse_note_chars: [8, 40]` 並記下理由。

`advisory.overview_prose` 分兩級，理由沿用本檔既有的立場
（**為了湊字數注水比偏短更糟**）：

- **缺席擋發布**：`pulse[].note` 整欄不見、`watchReview` 少一層
- **偏薄只警告**：note 字數、`thermo.note` 長度、takeaway 粗體比例與字數

拿站上二十天全部跑過一次：十七天 PASS、8/04 與 8/07 各一條輕微 WARN、
8/19 完整命中三項、8/20 FAIL。**沒有誤殺任何一天舊資料**——
一條新檢查若讓歷史全紅，該被懷疑的是檢查而不是歷史。

### 通則

> **欄位改名要連同讀它的人一起改，而「讀它的人」包含前端。**
> 這一族今天出現過三次：`.kb-data-repo` 沒被 `git add`、
> `index_meta` 沒人寫所以 `updatedLabel` 停在兩天前、
> 以及這次 `w`／`t`／`note` 三個名字在兩端各自為政。
> 共同點是**缺席在 JS 與 JSON 裡都不是錯誤，只是 `undefined`**。

> **「舊版比較好」值得先當成迴歸來查。** 使用者說的是觀感，
> 但觀感底下通常有一個可以量的東西 —— 這次量完發現不只是「感覺比較細」，
> 而是三個欄位在兩天內從 90% 掉到 0%。

## 2026-08-20 夜｜稽核：窗口沒有人驗，以及七個數字有兩個家

拿舊系統那份「跨系統通則」當稽核表，逐條對重寫後的程式。

### 一、`window` 自己沒有任何檢查（已修）

`ts_in_window` 問「卡片落在窗口裡嗎」、`_mode` 問「起點那天是不是週末」——
**兩者都對 `window` 照單全收**。起點寫錯不會讓任何一條變紅，
只會安靜地換掉整輪的收錄門檻與下限表。

而 `from_hour_taipei`／`from_day_offset`／`overlap_hours` 三個錨
**先前沒有任何程式在讀** —— 死數字，跟今天稍早那個 `pulse_note_max: 30` 同一類。

量了站上十七個有 `window` 的日檔：起點時刻與日偏移 **17/17 全對**。
所以這不是「已經在漂移」，是「漂了也沒人知道」。

`advisory.window_wellformed` 擋四種：起點時刻或日偏移不符、終點不晚於起點、
終點不落在當日、**與前一版有斷口**。最後一條是唯一會造成永久損失的：
重疊是刻意的容錯，負的重疊代表那一段素材永久掉單，而且不會有任何一張卡看起來不對。

門檻是量出來的：窗口長度正常日 25.9–28.7 小時 → 帶取 24–30（8/19 的 32.5、
8/20 的 34.9 都是排程延後開跑，WARN 屬實）。
重疊正常日 1.9–4.7 —— **舊規格寫的「2–5 小時」在 8/08 那天（1.9）就已經被違反**，
而且重疊跟 podcast 的「24–26 小時」一樣**是結果不是設定**：
它等於前一天寫檔的時刻減今天的起點。改成 `[0, 6]`，下界那個 0 才是真的判準。

### 二、七個數字有兩個家（未修，待決）

拿 `anchors` 的 67 個葉節點跟七個讀者（檢查、工具、SKILL、preamble、BRIEF）機械對帳，
22 個沒有讀者。分開看是兩種東西：

**(a) 真重複** —— `card_vocab.tone` 與 `fixed_structure.pulse_dir_values`
是**逐字相同的同一份清單**（`['偏多','中性','偏空']`），而只有前者有人讀。

**(b) anchors 有、preamble 也硬寫** —— 七個數字：

| 數字 | anchors | preamble |
|---|---|---|
| 段落 ≥8、字數 ≥1,500 | `paywall_verdict.complete` | 第 59 行硬寫 |
| 段落 ≤3、字數 <800 | `paywall_verdict.blocked` | 第 60 行硬寫 |
| 工具連續失敗 5 次 | `collect_limits.tool_fail_streak_breaker` | 第 68 行硬寫 |
| 同篇失敗 3 次 | `collect_limits.article_retry_max` | 第 69 行硬寫 |
| 單來源 15 篇 | `collect_limits.articles_per_source_max` | 第 17、70、198 行硬寫 |

**這正是 8/17 那個字數 bug 的形狀**（brief §4 ＋ `check.py` ＋ prompt 三份副本，
收斂只收一半，隔天照樣漂移）。

難的地方在於這裡有兩條通則正面對撞：

> **單錨**：權威值住在會被執行的那個東西裡，文件只指路不抄值。
> **知識放在執行當下讀得到的地方**：採集 subagent 只拿到 preamble，**它讀不到 anchors**。

所以 preamble 非抄不可 —— 除非抄的動作由機器做。
三條可能的解法，代價各不相同，留待決定：

1. **派工時注入**：步驟 3 從 `anchors` 讀出來、寫進每個採集員的任務卡。
   preamble 只留規則、不留數字。代價是任務卡變長。
2. **preamble 由 anchors 生成**：一支小腳本把數字填進模板，
   `repo-lint` 驗生成物與 anchors 一致。代價是多一個生成步驟。
3. **明認 preamble 是權威、anchors 不再收這七個數字**。
   代價是檢查程式若哪天要驗這些門檻就沒有機讀的來源。

**先不動。** 今天已經改了很多，而這一條的三個選項會改變不同東西的形狀 ——
照「一次只動一個變因」的規矩，它該自己一輪。

### 三、還沒有上線驗證（未修，待決）

通則第十節：五套都要求發布後抓 `index.json` 且帶 cache-buster。
podcast 今天接上的 `healthcheck.py` 有「線上狀態」那一條，**advisory 沒有對應物**：

`tools/sentinel.py` 讀的是 **repo 的 checkout**（`path.read_text()`），不是站台。
所以 `sentinel.updated_fresh` 證明的是「repo 裡的內容夠新」，
**不是「Pages 真的把它端出去了」**。Pages 部署失敗的症狀會是：
回執 exit 0、哨兵全綠、網站停在昨天。

### 通則

> **一個沒有人讀的錨，跟一個寫錯的錨一樣危險** —— 前者讓你以為某件事被守住了。
> 今天靠運氣抓到兩組（`pulse_note_max`、`window` 三個），所以把它變成一次機械對帳：
> 拿 anchors 的葉節點去 grep 七個讀者。**這個對帳本身應該定期跑，不是一次性的。**

## 2026-08-20 深夜｜兩件待決都做了：上線驗證進 pages.yml、門檻改由派工注入

### 一、上線驗證：`tools/verify_live.py`，接在 `deploy-pages` 之後

整條鏈是：草稿 → publish（commit＋push）→ Actions 部署 → Pages CDN → 讀者。
回執證明到第二格；哨兵讀的是 **repo 的 checkout**（`tools/sentinel.py` 用 `path.read_text()`），
證明的是第三格的**輸入**。**第四格之後沒有任何東西在看** ——
失敗的樣子是網站凍在昨天，而回執 exit 0、哨兵全綠。

考慮過三個位置，兩個否掉：

- **接到哨兵上**：哨兵 03:00 UTC ＝ 台北 11:00，而投顧約 11:00–13:00 才發布 ——
  它每天看到的是前一天的狀態，當日部署失敗要隔天才叫。**位置不對。**
- **另寫一支 advisory 版 healthcheck**：那是第二套 healthcheck，
  而 advisory 沒有 podcast 那些跨日檢查要跑。為一條檢查養一支腳本不划算。

**驗證要貼著它要驗的那個動作**，所以放在 `deploy-pages` 之後。

**比雜湊，不比欄位。** 舊規格第十節記著 2026-08-03 那次「`days[0].date` 早就是當天日期，
只看日期會被騙」，於是 podcast 補了 `updatedLabel`。那是對的修法，
但它的形狀是**一張會過期的清單** —— schema 下次再長一個欄位，這張清單又要記得補，
而忘記補的時候不會有徵兆。雜湊問的是「線上那份跟我剛上傳的那份是不是同一個檔」，
**schema 怎麼變都成立**。

三種結果分得出來：**拿不到**（Pages 沒開／網址錯／404）與
**拿到但不是同一份**（部署沒生效／CDN 慢）處置不同，訊息要說得出是哪一種。
五種情形逐一測過：一致、線上是舊的、連不上、404、本地檔不存在。

**podcast 那邊有個陷阱要記住**：它的 `deploy.yml` 上傳的是 `path: '.'`（整個 repo），
所以 kb-core 的 checkout **必須排在 `upload-pages-artifact` 之後** ——
排在前面會把驗證程式一起發到站上。

### 二、六組門檻改由派工注入，`preamble` 不再抄

`paywall_verdict` 的四個數字、重試組數、以及三條上限的三個數字，
先前在 `anchors` 與 `preamble` **各寫一次**。那正是 2026-08-17 字數 bug 的形狀。

難處在兩條通則正面對撞：**單錨**要求文件只指路不抄值；
**知識放在執行當下讀得到的地方**指出採集 subagent 只拿到 preamble、讀不到 anchors。
所以數字非到兩個地方不可 —— 問題只是誰負責搬。

否掉的兩個做法：

- **生成 preamble**：preamble 是散文，模板化等於把散文搬進模板檔 ——
  **副本沒有消失，只是換了位置**，而且多一個沒人看著的建置步驟。
- **加一條檢查驗兩邊一致**：這是 podcast `shows.json 兩份` 的模式，今天證明有效 ——
  但那個案例**兩邊都是 JSON**，比對是 `dict == dict`。這裡一邊是 JSON、一邊是中文散文，
  而今天已經在文字比對上翻過一次車（節目名稱正規化誤判三次）。
  **用脆弱的方法去守一件事，得到的是一條會誤報的檢查，而誤報的檢查等於沒有檢查。**

**採用：派工時注入。** SKILL 步驟 3 從 anchors 讀出來寫進任務卡，preamble 只留規則。
這是三個裡唯一讓數字**真的只有一個家**的。

失效模式一起設計掉了：preamble 明寫
「**任務卡上沒有那些門檻，就停下來回報，不要自己訂一個**」——
把缺席變成大聲的（通則第一節：否定訊號要具名）。
再加一道：回報格式要求**把拿到的門檻原樣抄回來一行**，
而步驟 3 的完成條件包含「每一份回報都抄得出來」——
那幾個數字不存在任何檔案裡，只活在任務卡與回報中，這一行是事後唯一查得到
「當時用的是什麼標準」的地方。

### 三、順手：一份逐字重複的清單，以及一個沒人驗的值域

`fixed_structure.pulse_dir_values` 與 `card_vocab.tone` 是**逐字相同的同一份清單**
（`['偏多','中性','偏空']`），而只有後者有讀者。

刪掉重複那份的同時發現：**`pulse[].dir` 從來沒有任何檢查在驗值域** ——
值域寫在 anchors 但沒有讀者，等於沒有訂。`_fixed_structure` 補上，
拿今天那一版驗：把一筆 `dir` 改成 `t-red`（舊站 schema 漂移的樣子）會叫，缺 `dir` 也會叫。

### 通則：我的量測工具自己也會量錯東西

為了不再靠運氣找死錨，寫了一段「拿 anchors 的葉節點去 grep 讀者」的對帳。
**它給了三個不同的答案**：

| 怎麼問 | 答案 |
|---|---|
| 葉節點的名字有沒有被提到 | 22 個沒有 |
| 任何一層祖先有沒有被提到 | **1 個**沒有 |
| 有沒有**檢查在強制**它 | 21 個沒有（其中一半是解析假象或刻意的散文欄位） |

三個數字都是對的，因為它們回答的是三個不同的問題 ——
而只有第三個是我真正想問的。**第一個版本太嚴（父層被引用的葉節點全被誤判成孤兒），
第二個版本太寬（`lengths` 被提到，`lengths.essay_chars` 就算有人讀）。**

所以**今天不把它做成常設工具**。今天一整天的規矩是「不要交一條自己沒證明過的檢查」，
而這一段我證明不了 —— 它現在的樣子會給出好看但沒有意義的數字。
留在這裡當紀錄：**要問的是「有沒有檢查在強制它」，不是「有沒有人提到它」。**

## 2026-08-20 深夜｜哨兵會開警報，但不會收警報

翻 Actions 確認新的部署驗證有沒有跑（有，1 秒、綠）時，看到 advisory-rewrite
掛著 **1 個 open issue**，以及哨兵最近兩次都是紅的。

Issue #1 是機器人在 **2026-08-20T08:00:26Z（台北 16:00）** 開的：

> ❌ `sentinel.data_fresh` 最新一期是 2026-08-19，已經 40 小時（上限 36）—— 管線停了

**它判對了。** 那天早上使用者臨時有事、把排程暫停，當天那一版直到 17:54 才落地；
16:00 那個時點，管線確實已經停了 40 小時。這是一個乾淨的真陽性。

問題在後面：**條件在 17:54 自己解除了，而沒有任何東西關掉那個 issue。**

看 `sentinel-reusable.yml` 的「判定不綠就叫人」那一步：它會先
`gh issue list --search "$title in:title"`，有就 comment、沒有就 create ——
**去重做得很好，恢復完全沒做。**

於是狀態變成：issue 永遠開著，每次再失敗只是多一則留言，而回綠時一片安靜。
**一個永遠開著的 issue 就是雜訊，而雜訊訓練人忽略警報**（跨系統通則第一節：
「會固定響的警報就是雜訊」）。更糟的是它讓「現在到底好了沒」這個問題
只能靠翻最新一次 run 才答得出來 —— 而警報存在的意義本來就是省掉那個動作。

補上「判定回綠就收掉警報」：留一則「已恢復＋時間」再 close。
**紀錄留在 issue 裡不會消失**，所以關掉不等於湮滅證據 ——
下次同樣的事再發生，新開的那個 issue 旁邊就有一條可以對照的歷史。

### 通則

> **警報的生命週期有兩端，而第二端很容易被忘記**，因為寫警報的時候你在想的是
> 「怎麼讓它響」。去重是第二端的一半 —— 它讓警報不會重複，
> 但沒讓警報結束。**能開就要能關，否則開的那次也會慢慢失去意義。**

## 2026-08-21｜七採集兩批的第一輪，以及一條偽裝成產出檢查的設定檢查

### 一、時間砍掉一半，而且是量到的

窗口收盤時間的軌跡：09:45（08-17）→ 11:20 → 11:40 → 15:31 → 17:54（08-20）→ **09:29（08-21）**。
07:30 起跑、09:29 收，約兩小時 —— 七採集兩批那個改動有效。

連帶：`window_wellformed` 今天 WARN（與前一版重疊 10.9 小時，上限 6）。
**那是昨天收在 17:54 的殘影，不是今天的問題** ——
重疊 ＝ 前一版收盤時間 − 07:00，今天收在 09:29，明天會回到 2.5 小時。**會自己好。**

### 二、`dispatch_wellformed` 一個產出欄位都沒讀

用讀取追蹤量的（把 `doc` 包成會記錄自己被讀了哪些鍵的 dict）：
advisory suite 其餘 16 條各讀 3–13 個產出欄位，**這一條讀 0 個**。
它驗的是 anchors 自己合不合理 —— 每組每模式有負責人、指派加總等於配額、
每個採集員恰好在一個批次裡 —— 全部是設定，跟今天派了誰無關。

沒有拿掉它（設定壞掉確實該擋），但把 `covers` 改寫成「派工設定本身」，
並在 `blind_to` 第一條寫明它讀 0 個當日欄位。理由：

> 它會永遠 PASS 直到有人改 anchors，**卻佔著「17 條檢查」裡的一格**，
> 讓那份報告讀起來像當日產出被驗了 17 次。
> **「設定說七個採集員」與「今天真的派了七個」是兩個問題。**

後者目前沒有機器在問 —— 日檔裡沒有派工紀錄。**刻意先不加**：
要加就得讓 SKILL 吐出一份機器讀得懂的派工紀錄，那是一次 SKILL 改動＋重貼排程，
而那個失效態今天已經有間接訊號（跑得慢就晚收盤，見第一節那條軌跡）。
**先記在這裡，等有第二個理由再一起做。**

### 三、哨兵移到 15:00 之後第一次跑就對了

心跳 `2026-08-21T07:32:21Z exit 0 latest 2026-08-21 全部正常`。
**它排 07:00 卻 07:32 才跑** —— 延遲 32 分鐘，正好落在舊設定那一小時餘裕之內。
那就是為什麼舊排程會隨機紅：判決取決於 cron 準不準時。

## 2026-08-22｜三個分身各自合規、加起來不合規，以及一條寫反了兩天的時區規則

**動到哪些檔**：`scripts/advisory/preamble.md`（第六節選擇器表重寫＋新增第六之二節）、
`advisory/anchors.json`（`dedup_exempt` 由 2 條增為 5 條，新增 `_dedup_exempt_cost`）、
`skills/advisory/SKILL.md` 正本（步驟 1／2／3／5／6／7／9 各加一段，時間預算加實測軌跡）、
排程 `advisory-daily-0730` 的 prompt（整份重貼）、
`skills/maintain/advisory/FILES.md`（整份重寫）。
**沒有動**：`checks/advisory.py`、`systems/advisory.py`、`advisory/BRIEF.md`、
`anchors.groups`（動它會讓五圖的 `chart.theme_unique` 變紅）。

### 一、`threaded_cards_per_day` 是全站量，而撰寫第一次拆成三個分身

當天把撰寫拆成 macro／industry／politics 三個分身。深度卡有分配（3／3／1 ＝ 7，落在 6–8）；
`thread` 沒有 —— 任務卡寫的是「每個區塊不超過 4 張」，合計 11，
被 `advisory.index_entry_source` 擋下，`exit 10` 退件一次。

**每個分身各自都合規，加起來不合規。** 這是「拆分身」這個做法本身帶來的失效態，
不是任何一個分身寫錯 —— 所以防線加在派工（SKILL 步驟 6 新增全站量對照表），
不是加在分身。同一類的還有 `deep_card_count` 與 `site_total`。

**這一條本來一秒就驗得出來。** `tools/advisory_verify.py` 跑的就是 `exit 10` 那一關的
同一組檢查、完全無副作用，但當天沒有先在本機跑，拿 publish 當了 linter。
修完之後在本機跑一次是 17 PASS · 0 WARN · 0 FAIL。
**退件一次的成本是兩分半，本機驗證一次是十秒。** 已寫進步驟 7 的完成條件。

### 二、華爾街見聞的時區規則寫反了，而它錯的時候症狀是「那家今天沒新聞」

`preamble` 第六節原本寫「`article:published_time` 標成 `Z` 但**實為北京時間**，
當 UTC 讀會整整偏 8 小時」。當天採集員 C 在 5 篇文章上獨立驗到相反結果並自行修正，
維護端當場複驗 `wallstreetcn.com/articles/3780012`：
meta `2026-08-21T11:42:33.000Z`、頁面署名 `08-21 19:42` —— **11:42 UTC ＋ 8 ＝ 19:42。**

**照舊規則做，每一則的 `ts` 會早 8 小時，而不會有任何東西報錯**：
`ts_in_window` 只驗落不落在窗口裡，驗不出換算對不對。
8 小時足以把窗口內的稿子推到窗口外 —— **症狀是「那家今天沒新聞」，
而那看起來完全像是來源沒發稿。**

無法回答的是它從什麼時候開始錯的：可能一直錯、也可能站台改版過。
**沒有回頭改任何已發布的日檔** —— 若一直錯，過去那幾版的華爾街見聞卡片
可能各自漏收了一批窗口邊緣的稿子，但那是「漏收」不是「收錯」，
歷史不美化，且無法在不重跑採集的前提下判定。

### 三、黃金保底卡靠輪換網址閃過去重，四天四個網址

`base_card_groups` 要求黃金天天出卡，`dedup_exempt` 卻只豁免了兩條 FRED ——
**兩條規則沒有接上**，於是黃金保底卡的 `url` 四天換了四個：
`08-19 /usa/historical-data/` → `08-20` 改用一篇 Bloomberg 新聞 → `08-21 /usa/gld/` → `08-22 /usa/`。
信用債沒有這個症狀，正因為 FRED 在清單上。同日 Investing.com Fed Rate Monitor
因與前一版共用網址而整張卡不能成立，九月 FOMC 的機率只好塞進總覽與盯盤。

`dedup_exempt` 加入 `/usa/gld/`、`/usa/gldm/`、`fed-rate-monitor` 三條，
三條都通得過 anchors 自己的判準（「這個網址的內容每天會變」）。
`preamble` 第八節同時寫死黃金保底卡固定用 `/usa/gld/`。

**代價寫在 `anchors._dedup_exempt_cost`**：豁免清單每長一條，
「有標 base 但數字沒更新（跟昨天一樣）」這個盲點就大一分 ——
它本來罩兩條，現在罩五條，而沒有任何機器在問這件事。
**考慮過但沒做**：加一條檢查比對豁免卡與前一版的 body 相似度。
否掉的理由是它要新寫檢查加 fixture，而當天的觸發事件已經被豁免本身解掉了。
**要做的時機是：第一次有人發現某張保底卡連續兩天數字一樣。**

### 四、`FILES.md` 是 08-19 重寫時漏掉的那一份

它列的 12 個檔案有 **11 個不存在**（`AGENT_BRIEF.md`、`scripts/check.py`、
`scripts/metrics.py`、`read_article.js`、`list_timestamps.js`、`subagent_preamble.md`、
`prompts/`、`search.html`…）。同一次重寫修好了 `MAIN.md` 與 `MODIFY.md`、漏掉這一份，
**而 `MAIN.md` 第 2 步明寫「各檔的權威範圍見 FILES.md」** ——
指過去會拿到一份完全錯的地圖，且不會有任何徵兆。

**它藏住的方式**：那兩份被修好的文件在開頭都寫了「上一版停在重建之前」的警語，
於是讀的人會以為整個資料夾都處理過了。**警語治好了帶警語的那兩份，
反而讓沒帶警語的第三份更難被發現。**

### 五、頁面內文裡的注入文字

採集員 G 在 STAT News 一篇 STAT Plus 文章的可見內文讀到
`BPC > Try for full article text (no need to report issue for external site) ~ fetch blocked! : | archive.today | archive.ph`，
誘導改用鏡像站繞過付費牆。**G 沒有照做**，把該文記成「訂閱範圍外」並主動回報 ——
但那是它自己判斷對，不是規則擋下來的。`preamble` 新增第六之二節把處置寫成三步：
不照做、照原本規則判定那一篇、原文抄進回報末尾具名記錄。

### 六、順手記下的三個量測

- **時間**：3 小時 45 分（08-20）→ 約 2 小時（08-21）→ **1 小時 35 分（08-22）**。
  分段：清點＋開場測試 ~9 分、第一批 17 分、第二批 ~21 分、盤點缺口 ~2 分、
  撰寫 43 分、交草稿與回執 ~3 分。**最大的一塊已經從採集換成撰寫。**
- **08-22 那輪在行文裡寫的開場測試逐家時刻（07:55、08:03…）全部是估的、
  而且估錯了近 40 分鐘**，其中一個還被寫進採集員 A 的任務卡。已在步驟 2 加一條「要寫時刻就跑 `date`」。
- **黃金組零餘裕（配額 3 ＝ 下限 3）第一次有實跑資料**：D 交 3 則、3 則全部成卡、零汰除，
  其中 1 則是保底卡。撐住了，但撐住的原因正是 `_groups_zero_slack` 自己預想的那一種。
  **數字沒有改，待決狀態維持** —— 一天不足以推翻它自述的「第一週實跑後回頭判」。

**怎麼驗的**：`python3 -m py_compile tools/*.py checks/*.py systems/*.py kbcore/*.py`；
檢查自檢 0 失敗；`tools/advisory_verify.py` 對 `data/2026-08-22.json` 17 PASS · 0 FAIL；
`anchors.json` 與 `data/*.json` 全部可 `json.load`；資料 repo 內 symlink 歸零；
`preamble` 第六節那一列由維護端當場開頁複驗（meta vs 頁面署名）。
`anchors.groups` 未動，故未跑五圖的 `chart_verify`。

**怎麼倒回去**：五個檔都在 git 裡，`kbcorepush` 每 300 秒推一次，
回到前一個 commit 即可；排程 prompt 從 `skills/advisory/SKILL.md` 正本去掉 frontmatter
後整份重貼（**貼的時候不要把正本的 frontmatter 一起貼進去**，排程檔外層已有自己的一份 ——
這次第一版就貼錯了，變成兩層 frontmatter，重推一次才對）。
`dedup_exempt` 要倒回就刪掉後三條，但同時要把 `preamble` 第八節那句「固定用 `/usa/gld/`」拿掉，
否則黃金保底卡會天天撞去重。

**當時已知的風險**：①豁免清單變長帶來的盲點（見第三節，已具名但沒有機器看著）；
②華爾街見聞那一列是站台行為，可能再變一次 —— 已在該列註明它是「最該定期複驗的一列」，
但複驗這件事沒有排程，靠的是採集員回報時會提；
③`FILES.md` 重寫後，`maintain` 技能裡的副本仍是舊的 —— 技能子檔無法從工作階段更新，
要打包 `.skill` 重裝，**在那之前從技能讀到的 `FILES.md` 還是錯的**。

## 2026-08-22 晚｜配色改用台新企業識別，以及外殼從來不在自動路徑上

**動到哪些檔**：`advisory-rewrite/index.html`（`:root` 整組換色、`--blue` 更名 `--brand`、
12 處引用、頁首色帶、品牌圓點、兩處小字對比）、
`systems/advisory.py`（`staged_paths` 由 `["data"]` 改為 `["data", "index.html"]`）、
`skills/maintain/advisory/MODIFY.md`（網站呈現那一列補上發布路徑）。
**沒有動**：任何 `data/YYYY-MM-DD.json`、`checks/advisory.py`、`anchors.json`。

### 一、外殼改了不會上線，而每一個訊號都說成功

`staged_paths` 是 `["data"]`，publish 每分鐘只 `git add data`。
**所以在 2026-08-22 之前，改 `index.html` 的結果是：檔案留在本機、回執 exit 0、
17 條檢查全綠、commit 推上去了 —— 而網站沒變。**
這與 2026-08-21 五圖 `charts/` 那一條是同一個形狀，差別只在那次是「檔案在遠端不存在」，
這次是「改動沒上線」。**publish.py 的 4b 對帳段抓不到它**：那一段問的是
`ls-files --others`（有沒有沒進版控的檔），而 `index.html` 是**已追蹤、已修改**，
不在 `--others` 的定義裡。

改成 `["data", "index.html"]`。與 `raw/` 的差別是寫入者數量：`raw/` 有 Actions 與 publish
兩個寫入者搶同一個 main，`index.html` 只有維護工作階段一個。

**怎麼驗的**：照 MODIFY.md 在 `/tmp` 另建 bare origin ＋ 工作 repo，**不碰真 repo**。
在工作 repo 的 `index.html` 打上 `E2E-MARK`、資料檔原封不動，跑 `publish.py` →
回執 `exit 0 @ already-published`，`git --git-dir=origin.git show HEAD:index.html`
確認 `E2E-MARK` 在遠端。**接著跑對照組**：把 `staged_paths` 改回 `["data"]`、
打上 `E2E-MARK-2`、再跑一次 → 遠端沒有 `E2E-MARK-2`。
**有對照組才知道差別來自 `staged_paths`，而不是來自「我剛好也 commit 了」。**

真 repo 上線的方式是**重送一份與已發布內容位元組相同的草稿**：
`already=True` 會跳過寫入、但 `git add` → commit → push 照跑（publish.py 第 2 節
自己寫著這是刻意的）。回執 `exit 0 @ already-published`、commit `127c083`，
local 與 origin 的 main 一致，`raw.githubusercontent.com` 讀回確認台新紅已在遠端。

### 二、配色：品牌紅只當結構色

台新新光金控手冊 A-5-1 的標準色是台新紅 Pantone 186 `#D70C18`、
Dark Gray Cool Gray 11 `#464646`、Taishin Gray Cool Gray 2 `#D8D8D8`；
輔助色 Light Red `#E38383`、Medium Gray `#656565`。

**唯一真正的難處是台新紅與網站的偏空紅 `#dc2626` 對比只有 1.10:1** —— 並排是同一個紅。
三個方案：A 品牌紅只當結構色、B 順便把漲跌改成台股慣例（紅漲綠跌）、C 紅只用在頁首頁尾。
**選 A**，漲跌維持綠漲紅跌不動，兩個紅靠位置區隔（結構元件 vs 數值）。
理由寫進 `index.html` 的 `:root` 註解，含一句「不要把 `--brand` 用在數值上」。

**B 案沒有被否決，只是沒有今天做。** 它的代價是過去 23 天的歷史檔會跟著換色、
視覺不連續；它的好處是讀者是台灣投顧，而綠漲紅跌是美股慣例。
**要做 B 的時機是：有人回報看盤時方向讀反了。**

`--blue` 更名為 `--brand` 而不是只改值 —— 這個 repo 最恨「名字說藍、值是紅」。
連帶把整組帶藍調的中性色（`--bg` `--line` `--dim` `--faint`）換成中性灰，
**那才是網站讀起來「藍」的主因，不是那個 accent**。

**順手修好一個既有缺陷**：`--faint` 原本 `#8a93a5` 對白底 3.36:1，不到 WCAG AA 內文，
而它用在所有 11–12.5px 的 metadata。改 `#727272` 後 4.81:1。
改完全站實測：內文 15.91、次要 5.83、弱 4.81、連結 5.31、白字 on 紅 5.31、
chip 紅字 on 淡紅底 4.81 —— 全部 ≥4.5:1。

**字型不用動**：手冊要求中文思源黑體、禁止襯線，而網站的
`-apple-system, "PingFang TC", "Noto Sans TC", system-ui, sans-serif`
裡的 `Noto Sans TC` 就是思源黑體的 Google 版本名稱，整份也沒有任何襯線字體。

**怎麼驗的**：`node --check` 抽出 inline script 通過；殘留檢查確認 14 個舊藍系色碼
一個不剩；對比度逐組實算；`advisory_verify` 17 PASS · 0 FAIL。
**視覺是使用者自己開預覽檔確認的，不是我驗的** —— Chrome MCP 的 `navigate`
會把 `file://` 硬改成 `https://file://`，沙箱裡也沒有可以自己算圖的瀏覽器。
做法是把最近三天的真資料內嵌成一份離線 HTML（覆寫 `window.fetch` 回傳內嵌物件），
使用者雙擊開啟確認後才上線。**這個限制值得記住：外殼改動的視覺確認沒有自動路徑。**

**怎麼倒回去**：`index.html` 與 `systems/advisory.py` 都在 git 裡，回到 `127c083` 之前那顆。
注意兩者要一起倒 —— 只倒 `index.html` 會讓下一次 publish 把舊配色推上去（因為
`staged_paths` 仍含 index.html），只倒 `staged_paths` 則新配色會留在遠端但之後改不動。

**當時已知的風險**：
①**17 條檢查一條都不看外殼** —— `index.html` 改壞了會在下一次 publish 直接上線，
而回執照樣 exit 0。這是 `staged_paths` 加入 index.html 後**新增**的曝險面，
在那之前外殼壞掉至少不會自己上線。考慮過但沒做：加一條檢查驗 `index.html`
抽出 script 後 `node --check` 通過、且 `:root` 的必要變數都在。
否掉的理由是它要新寫檢查加 fixture，而今天的改動已經人工驗過；
**要做的時機是：第一次有人把外殼改壞並上線。**
②**編輯到一半被 publish 撞上** —— publish 只在 outbox 有草稿時才動，
實務上一天一次、約台北 07:30–09:00，窗口很窄但不是零。
③**`--brand-bg` 與 `--brand-line` 是手冊沒有的衍生色**，若品牌方日後給出官方淡色，
這兩個值要換掉；已在 `index.html` 註解裡標明它們是衍生的。

## 2026-08-22 夜｜「關於與方法」壞了 20/22 天沒有人回報，先修好再撤掉

**動到哪些檔**：`advisory-rewrite/index.html`（`tabAbout` 的 `L()` 修正 → commit `fb2e5f9`；
移除分頁與 `tabAbout` → commit `8caf5e8`）。
**沒有動**：`data/*.json`、`BRIEF.md`、`anchors.json`、`checks/advisory.py`。
**`about` 欄位照常產出**，schema 不變，BRIEF 第二節與第十節都不用改 —— 撤掉的是呈現，不是資料。

### 一、非空字串的 `.length` 為真、`.map` 不存在

`tabAbout` 裡：`const L=(t,arr)=>arr?.length ? arr.map(...) : ""`。
而 `about` 的 `run`／`limits` 有兩種方言：舊站十八天與重寫版的部分日子存**字串**，
只有 8/19–8/20 存陣列。字串通過了 `.length` 的守衛、卻沒有 `.map`
→ `arr.map is not a function` → **整頁炸掉**。

實測 22 天裡 **20 天炸**（只有 8/19、8/20 正常）。
`paint()` 不在 try 裡，所以點分頁時是拋未捕捉的錯誤；初次載入則被 `main()` 的
try 接住、畫成紅色錯誤框。**兩種路徑都不會留下任何紀錄。**

修法是把三個欄位一律正規化成陣列再走同一條路。
驗法是用 node 把 `tabAbout` 抽出來對 22 個真日檔各跑一次：**22/22 渲染成功**（原本 2/22）。
**這比截圖可靠** —— 截圖只證明今天那一份可以，跑完 22 天才知道兩種方言都吃得下。

順帶：舊站那十八天的 `about` 有 `access` 欄，`tabAbout` 從來沒讀它 ——
寫進去三週、一次都沒出現在畫面上。撤掉分頁之後這件事不再重要，此處記一筆。

### 二、為什麼修好了還是要撤

**「壞了三週沒有人回報」是這一頁有沒有人看最可靠的證據**，比任何對它價值的推測都硬。
而 `checks/advisory.py` 對 `about` 一條檢查都沒有，所以也沒有機器在看。

但**撤之前先修好**，理由是：撤掉一個從沒看過運作樣子的東西，撤的是黑盒不是決定。
修好之後看到它其實是四塊讀者不同的東西：
`timeliness`（窗口起訖）與 `limits`（合規）是給讀者的；
`run`（執行模型、七個採集員代號、熔斷次數）是**維運遙測**，
而遙測的家是回執與 CHANGELOG，不是投顧同仁早上那三分鐘；
`notes` 兩者都有（今天那一份裡最有價值的是九月 FOMC 的欄位更正）。

**合規沒有缺口**：頁尾那一行「內容為新聞彙整與研究輔助，非投資建議。
報價口徑以各卡片標註為準。」就是 `limits` 的濃縮版，且每一頁都在。
**舊深連結不會壞**：`paint()` 對不在清單裡的分頁會退回第一個，
`#2026-08-22/about` 實測退回 `#2026-08-22/overview`、無錯誤框。

**兩顆 commit 是刻意的**：先推修正（`fb2e5f9`）再推移除（`8caf5e8`），
這樣「它修好之後長什麼樣」留在 git 歷史裡，而不是被移除那一顆吃掉。
要復原只需 revert `8caf5e8`。

### 三、被否決的另外三個做法

- **直接刪掉不修** —— 那會把 bug 藏起來而不是解決它，而且是拿沒看過的東西做決定。
- **保留分頁只修好** —— 沒有回答「這一頁的讀者是誰」，遙測還是會繼續上畫面。
- **把 `timeliness`／`limits` 搬到頁尾** —— 我原本建議這個，但頁尾已有濃縮版，
  再搬一次是重複；`timeliness` 的核心（新鮮度）在頁首「最後更新」已經有了。
  **想把窗口起訖也放上頁首的話，那是一行的事，等有人問再做。**

**怎麼驗的**：node 對 22 個真日檔跑 `tabAbout`（修正後 22/22）；
`node --check` 抽出 script 通過；殘留檢查 `tabAbout` 歸零；
上線後實測分頁剩五個、`#/about` 優雅退回總覽、無錯誤框。

**怎麼倒回去**：revert `8caf5e8` 分頁就回來（且是修好的版本）。
若連修正一起倒，回到 `fb2e5f9` 之前 —— 但那會讓 20/22 天重新炸掉，不建議。

**當時已知的風險**：①`about` 現在**只存在於 JSON**，沒有任何 UI 讀它 ——
下次有人改 `about` 的形狀不會有任何畫面壞掉來提醒，而 `checks` 也不驗它。
**它現在是純粹的下游契約**（House View 與稽核讀），這件事沒有寫在 BRIEF 裡，
只寫在這裡與 `index.html` 的註解裡。②同樣的 `L()` 型別陷阱可能還在別處：
本輪只修了 `tabAbout` 一個，`tabOverview` 與 `tabSection` 沒有逐一檢查。

## 2026-08-22 夜｜風險溫度改折線圖，而「給它一個定義」被明確否決

**動到哪些檔**：`advisory-rewrite/index.html`（新增 `thermoChart()`、
`.gauge` 換成 `.tchart`／`.chartwrap`、加 `編輯判斷` 標籤）→ commit `ebc45ee`。
**沒有動**：`data/*.json`、`anchors.json`、`checks/advisory.py`、`BRIEF.md`、`SKILL.md`。
**資料層一個位元組都沒改** —— 折線圖的資料源是 `data/index.json` 的 `days[].thermo`，
那是本來就已經載入的跨日記憶欄位，不需要打開任何舊日檔。

### 一、這個數字沒有公式，而且維持沒有公式

被問到「風險溫度怎麼算的、脈動怎麼判的」，答案是**兩個都沒有公式**，
是當期撰寫者讀完素材後的判斷。系統只驗形態、不驗正確性：
`thermo.level` 要是 0–100 的整數字串、`note` 80–320 字、`pulse` 恰好五筆且鍵名順序固定、
`dir` 三選一、`note` 8–40 字。`checks/advisory.py` 的 `blind_to` 逐字寫著
「thermo 是合法整數但與當天實況無關」「pulse 的 dir 天天翻轉」——**寫 71 或寫 43，17 條檢查一樣綠。**

提了三個處理方向：①刻度誠實化 ②給它一個定義（四面向各 0–25、加 `thermo.parts`、
配一條驗「加總等於 level」的新檢查）③加上「此為編輯判斷」的揭露。
使用者一度說「全做」，**中途喊停並否決了②**：分數維持主觀認定即可。

**②被否決是對的，理由值得記下來**：把一個誠實的判斷公式化，會讓它變成假精確的模型。
四個面向（利率與長端／信用與企業面／地緣與能源／股市風險偏好與籌碼）是從 22 天的
`note` 歸納出來的、確實存在，但「歸納得出四條軸」跟「每條軸該給幾分」是兩件事，
後者沒有任何錨。**在還沒有人拿這個數字做決策之前就把它結構化，是在為稽核而稽核。**
真要做的時機是：有人開始把 thermo 當輸入而不是當摘要讀。

### 二、折線圖：y 軸的取捨才是設計，不是圖表類型

原本是一條 0–100 的漸層色條，`width:71%`。問題是**實測 17 個值全部落在 58–74**，
從沒低於 58、也沒高於 74，所以「71」在視覺上讀起來像「風險走完了七成」，
實際上它只是在一個 16 點寬的帶子裡動 —— **刻度的解析度是假的。**

改折線圖之後這個問題自動消失（看得到整段歷史就看得到它在哪一段），但換來一個新取捨：

- **固定 0–100**：誠實，但 16 點的變動畫在 100 點的軸上是一條平線 —— 用可讀性換誠實。
- **純自動縮放**：可讀，但會把 1 點的變動放大成一座山 —— 相反方向的失真。
- **有下限的自動縮放（採用）**：`lo = min(50, floor((min-5)/5)*5)`、
  `hi = max(80, ceil((max+5)/5)*5)`，兩端都標值。日變化讀得出來，
  而標籤（50／65／80）讓人不會誤以為刻度是 0–100。資料哪天走出去，軸會自己擴。

**沒有 thermo 的五天（07-30、08-02、08-03、08-07、08-08）讓線斷開，不補點。**
那是 2026-08-11 之前把 level 寫成散文留下的斷點，`_source_rule` 明訂
「禁止在兩個實測點之間內插」，事後補值等於編造。
實測渲染出 **2 段 polyline**，斷口就在 08/06 與 08/09 之間 —— 斷口本身是那段歷史的一部分。

線的顏色用 Medium Gray `#656565` 而不是台新紅：`index.html` 的 `:root` 註解
自己寫著「不要把 `--brand` 用在數值上」，而風險溫度是數值。

同時加了一行 11.5px 的「編輯判斷」在數字底下 —— 這是三個方向裡的第③項，沒有被否決。

**怎麼驗的**：把 `thermoChart()` 從 index.html 抽出來、用真的 `index.json` 在 node 跑，
確認線段數 2、當日點存在、y 標籤 50／65／80、x 標籤 07/30→08/22；
邊界測試三種（全 null → 回空字串、只有一點 → 回空字串、值超出 50–80 → 軸自動擴到 5／100）。
`node --check` 通過。上線後實測 DOM：`.tchart` 1030×227px、2 個 polyline、1 個 circle、
`aria-label` 帶區間 58–74。

**怎麼倒回去**：revert `ebc45ee`，漸層色條會回來。

**當時已知的風險**：①**軸會隨歷史擴張而改變**，同一個 71 在不同日子的圖上高度不同 ——
這是有下限自動縮放的代價，標籤有標所以不算誤導，但跨期截圖比對時要注意。
②封存天數變多之後（例如 100 天以上）點會變密，目前沒有做抽樣或捲動。
③圖是 inline SVG、沒有互動（不能 hover 看某一天的值），**刻意如此** ——
index.html 是單檔、不引入任何 CDN，加互動要嘛寫一堆事件、要嘛引函式庫，
兩個都比這個問題大。要看某一天的值，日期列的膠囊上本來就標著。

---

## 2026-08-23｜八件維護：一次採集事故、一個時序缺口，以及一條盲點檢查終於等到它的觸發事件

**動到哪些檔**：`scripts/advisory/preamble.md`（第四節改寫、選擇器表補七列補強三列、
新增第七之二節、六之二改寫）、`checks/advisory.py`（新增第 18 條
`advisory.exempt_card_freshness`）、`kbcore/env.py`（`REQUIRED_BY_LABEL` 補兩支）、
`launchd/kbprefetch-advisory.sh` 與 `com.kenny.kbprefetch.advisory.plist`（新檔）、
`launchd/README.md`（表加一列、新增一節）、`skills/advisory/SKILL.md`（步驟 1 的表改成三層、
步驟 3／5／6／7／9 各補一段）、排程 prompt `advisory-daily-0730`（整份取代）、
`skills/maintain/advisory/{MAIN,FILES,MODIFY}.md`（17→18、加一列）。
**沒有動**：`anchors.json`、`BRIEF.md`、`data/*.json`、`index.html`、`publish.py`、`systems/advisory.py`。

### 一、NYT 整輪報廢：它繞過了規則的字面，沒有繞過規則要防的東西

當輪採集員 E 沒有逐篇 `navigate`，改用同網域 `fetch()` 對 20 個 NYT 文章網址做時間戳預篩
（兩批、中間沒有延遲）。之後 **NYT 每一個文章頁都渲染成空殼**：`document.title` 變成
`nytimes.com`、內文 0 段、`<script>` 只剩 2 個，而**列表頁與 `fetch()` 本身都還正常**。
換分頁、換文章、reload、從列表頁點進去帶 referrer，四種變體全部一樣。NYT 當日零成卡。

`preamble` 第四節當時寫的是「**預篩時間戳不要逐篇開頁**」——那句話在 `fetch()` 出現之前
剛好等價於「不要打太多次」，之後就不等價了。**規則數的是開頁方式，風控數的是請求次數。**

改成不分工具一律：每個對外請求間隔 500–600ms、單一來源預篩總數 ≤20 次、與文章頁計數分開算、
不要連發兩批中間不喘息。**同輪對照組是乾淨的**：E 出事後對 Politico／The Hill／IBD／Barron's
全部加了延遲，那四家全程無異狀；第二批與補位輪照做，七家再無一家出事。

**這件事只會出現在回報的最後一段。** 沒有任何檢查看得到它 —— 症狀是「這家今天讀不到」，
與選擇器失效、與付費牆長得一模一樣。所以 `preamble` 現在要求回報時寫出「對這家發了幾次請求」，
那是事後唯一分得出來的線索。

### 二、豁免清單的盲點等到了它的觸發事件，於是那條檢查現在存在

`anchors._dedup_exempt_cost` 在 2026-08-22 寫下了動手時機：
**「要做這條檢查的時機是：第一次有人發現某張保底卡連續兩天數字一樣。」**
2026-08-23 就是第一次 —— Investing.com Fed Rate Monitor 的降息／維持機率
（61.0%／39.0%）與前一版逐字相同，因為週末 CME 期貨休市、頁面 `Updated` 停在前一天。
當輪執行者自己發現並寫進了 `about.notes`，**但沒有任何機器在問這件事**。

新增 `advisory.exempt_card_freshness`：對 `dedup_exempt` 上的每一個網址，
比對當日卡片與前一版卡片的**數字集合**（`body` ＋ `bullets` 抽出的所有數字）。
比集合而不是比整段文字，因為盲點的原話就是「**數字**沒更新」。

**是 WARN 不是 FAIL，而這是設計不是妥協**：機器分不出「真的沒更新」與「忘了更新」。
週末休市、FRED 落後兩個交易日、SPDR 歸檔落後一天，都會讓數字合法地重複。
判成 FAIL 會逼執行者去改數字，**那比重複更糟**。

### 三、`raw/` 每天早上落後一天，而我第一次的診斷是錯的

當輪報告寫的是「本機 clone 永遠落後，因為這一輪不跑 git」。**查 mtime 才發現不對**：
`raw/2026-08-23.json` 的 mtime 是當天 09:31，正是那一輪回執的時刻 ——
`publish.py` push 前會 rebase，origin 上的東西（含 Actions 產的 `raw/`）就是那時候進來的。

真正的形狀是**時序**：Actions 約 07:00 落地 → 輪次 07:35 開跑（此時本機最新的是昨天的）
→ 09:31 publish 才拉下來。**每天固定在需要它的前兩小時差一步。**
錯的診斷會變成錯的待修事項（「叫 publish push 後順手 fetch」——那解不了任何事），
而那比沒有待修事項貴。

新增 `com.kenny.kbprefetch.advisory`（07:20），**只用 curl 抓到 `~/.advisoryfetch/raw/`，
不跑任何 git**：`kbpublish` 每 60 秒一次，任何 git 指令留下的 `index.lock` 都會擋住它，
而那一次的症狀會是**發布失敗**而不是預抓失敗 —— 錯的地方會叫、出錯的地方不會。
與 `kbprefetch-chart.sh` 同一個紀律。腳本會驗 `date` 是今天、`failed_essential` 是空的
才落地（不合格 exit 10）：**拿到一份日期不對的檔案而以為保底層正常，比抓不到更糟。**

**當天在機器上實跑了一次**（沒有等隔天 07:20 —— 那時候沒有人在場）：exit 0、
1,396,732 bytes 落地、驗證欄位全對。**而這一跑抓到一個 `bash -n` 抓不到的 bug**：
log 那一行寫的是 `log "落地 $DEST（… bytes）"`，全形括號直接接在變數後面，
bash 把 `DEST（` 整串當成變數名、展開成空字串，於是**路徑印丟了**。
語法完全合法，所以 `bash -n` 是綠的；要跑一次、而且要看輸出，才看得見。
已改成 `${DEST}` 並在該行留下註解。**這支腳本的 log 就是「今天到底有沒有跑」的唯一證據，
少印一個路徑等於少一半價值。**

### 四、正本改了、prompt 沒重推，於是那一節安靜地不存在了

`skills/advisory/SKILL.md` 正本 17,870 bytes、排程副本 16,890，差的是**整節「用量：量它，不要估它」**。
後果實測：`metrics/usage.csv` 只有 header ＋ 一列 `2026-08-22,broker-research`，
**advisory 一列都沒有**，08-23 那一輪也沒跑 —— 因為排程拿到的那份沒有這一節。
這正是本檔開頭與 `MAIN.md` 都警告過的形狀。本次整份重推，並在該節末尾記下這件事本身。

### 五、注入字串跨站了，而上一次學到的辨識法今天全部失效

08-22 記載的是 STAT News 一站、鏡像站 `archive.today｜archive.ph`、位置在可見內文。
08-23 三處同時被推翻：鏡像站換成 `archive.li`（STAT News）與 `archive.is`（**WSJ**）；
STAT News 那次**不在 `.article-content` 內文區塊裡**（逐段掃內文沒有命中）；
而它已經出現在付費訂閱的大報上。`preamble` 六之二 改成**認句型、不認站名也不認網域**。

### 六、哨兵紅了 18 小時沒有人看見，因為紅的是它自己

`sentinel/heartbeat.json` 停在 2026-08-22T07:20:49Z、`latest_date` 停在 08-22，
而 index.json 已經是 count 23 / 08-23。`com.kenny.kbwatch` 每 4 小時有在跑（log 有紀錄），
但 `watch.external_binaries` 持續 FAIL：`com.kenny.kbpublish.bubble` 不在 `REQUIRED_BY_LABEL` 裡。
**一個永遠紅的看門狗把真訊號埋掉了**，而它紅的理由與它守的東西無關。

登記進去了。**推定值當天就在機器上驗掉了**（工作階段掛載不到 `~/Projects/ai-bubble-monitor`，
所以是在終端機跑的）：掃 `auto_publish.py` 與 `gate.py` 的所有 list 字面值首元素，
只有 `git` 與 `composite` 兩個，而 `composite` 是 `gate.py:76` 的必要 JSON 鍵清單、不是指令；
git 共 12 個呼叫點（auto_publish 10、gate 2），**整個 repo 沒有任何 `shell=True`**，
所以 PATH 白名單的推理成立、沒有第二條經由 shell 展開的路徑。
用 `env -i` 只留該 plist 宣告的 PATH（`/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin`，
**沒有 `/opt/homebrew/bin`**）實測，git 解析到 `/usr/bin/git`。
**它不依賴 Homebrew 是運氣不是設計** —— 哪天那支程式加了一個只有 Homebrew 才有的指令，
這條檢查會叫，而那正是它該做的事。

**驗這一條的過程本身值得記**：我第一版的 grep 只比對了一份自己列的指令白名單
（git／ssh／rsync／curl／node／npm／bash／sh／osascript），它印出「只有 git」——
**而那證明不了「沒有別的指令」，只證明了「白名單裡的只有 git」。**
改成不限白名單重掃才跑出 `composite`，才知道要再看一行才能下結論。
白名單式的查核會給出一個看起來完整、實際上只覆蓋你想得到的那些的答案。

**怎麼驗的**：`py_compile` 全綠；檢查自檢 0 失敗（新檢查的 fixture 與 near_miss
擺在邊界兩側、只差一個數字）；`advisory_verify` 對已發布的 `2026-08-23.json`
**18 PASS · 0 WARN · 0 FAIL**；**新檢查拿 23 期封存全部回測**，22 次比對全部 PASS
且逐期印出它實際比了幾個豁免網址（最多一期 4 個）——
**證明它有讀到東西，不是對著空集合全綠**（2026-08-21 五圖那條檢查就是這樣假綠的）；
再拿 08-23 當自己的前一版跑一次，**三張豁免卡全部 WARN**，把黃金那張的數字改掉後
只剩兩張 —— 真實卡片文字、兩個方向都證明過。
`kbprefetch-advisory.sh` 過 `bash -n`；plist 用 `plistlib` 解析出 Label／07:20／PATH 宣告；
腳本的驗證區塊拿今天的真檔跑回 `1`、拿昨天的檔冒充今天跑回 `0`。
`anchors.json` 與 24 個日檔全部 `json.load` 通過；資料 repo symlink 歸零。

**怎麼倒回去**：`preamble.md`、`checks/advisory.py`、`kbcore/env.py`、`SKILL.md`
四個檔 revert 本日 commit 即可。launchd 那兩個新檔刪掉、
並把 `~/Library/LaunchAgents/com.kenny.kbprefetch.advisory.plist` bootout ＋ 刪除
（**沒裝就不用**，見下）。排程 prompt 要手動貼回舊版 —— 它不在版控裡，
唯一的備份是本次之前的 `skills/advisory/SKILL.md`（frontmatter 兩行不同）。

**當時已知的風險**：
①**新的 plist 還沒安裝。** 工作階段碰不到 `~/Library/LaunchAgents/`，
所以 07:20 那一支**明天不會跑**，步驟 1 會落到第 ② 層（本機、昨天的）再落到第 ③ 層（Chrome）——
**與今天一樣，不會更糟，但也還沒變好。** 安裝指令在 `launchd/README.md`，要在機器上手動跑。
②`kbpublish.bubble` 的 `["git"]` 是推定值，見上。碰得到那個 repo 時要拿 `auto_publish.py` 複驗。
③新檢查比的是數字集合，**只改一個無關緊要的數字就能繞過**它 ——
那已經寫進 `blind_to`。它防的是「忘了更新」，不是防人。
④`preamble` 這一輪長了約 40%（選擇器表從 11 列到 18 列、多了一整節）。
**採集員的任務卡沒有變長，但它要讀的那份變長了** —— 若下一輪的採集成本明顯上升，
第一個要查的就是這件事，而不是別的。
⑤這一輪動到了共用底盤（`kbcore/env.py`）。**自檢是全域的**——`selftest()` 跑遍
九個 suite 合計 85 條、0 失敗，所以「有沒有哪條檢查壞掉或永遠 PASS」這個問題已經答完。
沒答的是**拿真資料實跑**：chart／podcast／research 的資料 repo 沒有掛載進這個工作階段，
那三組只驗到 import 與自檢，沒有驗到「對著今天的產出跑起來會怎樣」。
`groups` 一個字都沒改，所以 `chart.theme_unique` 不受影響（MODIFY 只在動 `groups` 時要求回跑五圖）。

---

## 2026-08-24｜publish 把自己剛寫的檔判成外人：一個 `.strip()` 擋掉四套系統的發布

**動到哪些檔**　`tools/publish.py`（第 241 行 `.stdout.strip()` → `.stdout.rstrip("\n")`，
加 20 行註解寫它藏住的方式）、`kbcore/env.py`（`REQUIRED_BY_LABEL` 補
`com.kenny.kbpublish.convergence`）、`metrics/MEASURE.md`（「怎麼跑」整節重寫、
advisory 那一列由「沒有回執」改成「回執在 outbox 根目錄」）、
`tools/usage_report.py`（`OUTBOX_DIR` 加 `"advisory": ""`，呼叫端由 `if not sub` 改 `if sub is None`）、
`scripts/advisory/preamble.md`（第六節：CNBC／IBD／Mint／Korea Herald 四列改寫、
新增 The Hill 與 The Economist 兩列、新增「`[BLOCKED]` 是工具層攔截」與
「電子報彙整頁不是文章」兩節）、`skills/advisory/SKILL.md`（時間軌跡、步驟 1 的層①、
步驟 3 的前一版網址清單、步驟 5 的補位額度、步驟 8 的 exit 15 列、步驟 9、用量那節）、
排程 prompt（整份重推）。

**量測**　`worktree-dirty` 在整份 `publish.log` 裡**只出現 4 次、全部在 2026-08-24**；
`tools/publish.py` 的 mtime 是 08-24 07:30:13，落在 08-23 最後一次成功發布（09:31）與今天之間。
回執從 09:44 起每 60 秒一次 `exit 15`，`detail` 說「repo 有 1 個未提交的變更……：`ata/index.json`」。
`.git/index` 裡沒有任何 `ata/` 開頭的項目（36 個 entry 全部列出來對過），
唯一的候選是 `data/index.json` —— **而它就在 `staged_paths`（`["data", "index.html"]`）底下**。
修好之後同一份草稿在 10:04 發布成功（`commit 8a498ec`），本機與 `origin/main` 對齊。
`metrics/usage.csv` 到 08-24 為止只有 3 列（broker-research 08-22、chart 08-23、podcast 08-23），
**advisory 一列都沒有**。launchd 對帳：kb-core 有 plist 但機器上沒裝的有
`kbfile.research`、`kbwatch.research` 兩支；機器上裝了但 kb-core 沒有的是 `kbpublish.bubble`
（plist 正本在 ai-bubble-monitor repo，已知）；**裝了但沒登記進 `REQUIRED_BY_LABEL` 的是
`kbpublish.convergence`**。

**怎麼驗的**　依 MODIFY 的硬規矩在 `/tmp` 另建 bare origin ＋ 工作 repo，沒有碰真 repo。
先用真的 `dirty_outside()` 分岔驗解析：`" M data/index.json"`（未 strip）回 `[]`、
`"M data/index.json"`（被 strip 後）回 `["ata/index.json"]` —— **函式是對的，壞的是呼叫端**。
再把測試 repo 種成 advisory-rewrite 的形狀、餵今天真正的草稿跑完整條 publish，
回執 `exit 0 @ pushed`、`git ls-tree` 在 bare origin 裡確認 `data/2026-08-24.json` 與
`data/index.json` 都真的進去了、`index.json` 第一筆是 `2026-08-24`（90 張）。
收尾跑完 MODIFY 的驗證清單：`py_compile` 四個目錄 exit 0、`selftest()` 失敗數 0、
`advisory_verify` 對已發布的 `data/2026-08-24.json` **18 PASS · 0 WARN · 0 FAIL**、
`anchors.json` 與 25 個日檔全部 `json.load` 通過、`index.html` 抽 script 後 `node --check` exit 0、
資料 repo 內 symlink 0 個。排程副本與正本用六個新字串比對過，**兩邊都在、舊的兩句都不在**。

**怎麼倒回去**　`publish.py` 那一行改回 `.stdout.strip()` 即可回到今天早上的行為
（但那會讓四套系統再度卡在 exit 15）。`env.py` 刪掉 `kbpublish.convergence` 那一行、
`usage_report.py` 把 `"advisory": ""` 拿掉並把 `if sub is None` 改回 `if not sub`，
兩者都是單點。文件類（`MEASURE.md`／`preamble`／`SKILL.md`／CHANGELOG）全部只增不刪語意，
倒回去是把新增段落拿掉。排程 prompt 要倒回去得整份重推。

**當時已知的風險**
①**`kbfile.research` 與 `kbwatch.research` 兩支 plist 仍然沒裝在機器上** ——
工作階段做不到 `launchctl`，這是 Mac 上的手動步驟，指令在 `launchd/README.md`。
在裝上去之前，broker-research 那一套沒有看門狗。
②`kbpublish.convergence` 的 `["git"]` 是**從 plist 的 ProgramArguments 推定**的
（它跑 venv python ＋ `tools/publish.py`，與其他 `kbpublish.*` 同一條路徑），
不是像 bubble 那樣掃過原始碼。要複驗就掃 `tools/publish.py` 的 `subprocess` 呼叫點。
③**這一輪沒有量到用量。** `usage_report.py` 在 Cowork 的沙箱那一側跑不了
（逐字稿在 Mac 上、掛載會被明文拒絕），而修好的 `MEASURE.md` 要求在 Mac 的終端機跑 ——
所以 `usage.csv` 今天**還是沒有 advisory 那一列**。文件已經對了，但那一步仍然是人要做的。
④`~/.advisoryfetch/` 在 Cowork 取不到這件事**只寫進了文件，沒有修**。
步驟 1 的層①目前是一條每天固定要繞過去的死路（約 5 分鐘、7 次工具呼叫）。
兩個解法（加進連接資料夾／改寫到 `~/outbox`）都需要人決定，這一輪沒有動。
⑤`publish.py` 的呼叫端**至今沒有測試**。今天的 bug 出在
「純函式有 fixture、呼叫它的那三行沒有」，而修法只是改對那三行 ——
**同一個形狀還會再來**。真正的防線是把 `git status` 的輸出當成一種輸入形狀來測，
那件事今天沒做。
⑥今天的退件（跨版去重擋掉兩張卡）已經寫進 `SKILL.md` 步驟 3，
但**沒有任何機器在派工時強制帶上前一版的網址清單** —— 它仍然靠執行者記得。

---

## 2026-08-24 續｜用量量測終於有了執行者，而它自己先示範了一次「只在生產跑得動的分支」

**動到哪些檔**　新增 `launchd/kbusage.sh` 與 `launchd/com.kenny.kbusage.plist`；
`kbcore/env.py`（`REQUIRED_BY_LABEL` 補 `com.kenny.kbusage`，空陣列）；
`tools/usage_report.py`（`--since` 補上 `_norm_iso` 正規化）；
`metrics/MEASURE.md`（新增〈怎麼跑：自動〉與 sidecar 格式，原本的手動流程降為備援）；
`skills/advisory/SKILL.md` 與排程 prompt（用量那節改成「寫 sidecar」）；
`launchd/README.md`（表格加一列）。

**量測**　`metrics/usage.csv` 到今天為止只有 3 列，**advisory 一列都沒有** ——
它每天都跑，而收尾那一步是七套裡唯一沒有自動化執行者的（抓、發布、看門狗、轉檔、
預抓、推 kb-core 都有 launchd，只有量測靠人記得）。實測 Cowork 的邊界：
逐字稿在 Mac 的 `local_<uuid>/.claude/projects/…`，**沙箱掛不到**（`Bash` 看不到）、
`Glob` 看得到 143 個 jsonl、`Read` 讀得動但主逐字稿是整場累積（`session_cost.py`
量過一場維護對話 567 輪／187M），**讀進上下文就炸掉**。
所以量測只能由「能在 Mac 上跑 python 的東西」做，而這台機器上那種東西只有 launchd。

**怎麼驗的**　`kbusage.sh` 開三個環境變數的縫（`KBUSAGE_HOME`／`KBUSAGE_KB`／`KBUSAGE_PY`，
預設值就是生產路徑），在沙箱裡用合成逐字稿跑完整條：
成功（寫入一列、刪 sidecar）、冪等（同日再放一次 sidecar → 偵測到已有該列、不重複寫、
CSV 維持 2 行）、壞 sidecar（缺欄位 → 搬 `.bad`、exit 12）、逐字稿不存在（保留 sidecar、exit 12）、
回執未到（保留、exit 12）、陳舊（08-01 的 sidecar 距今 23 天 → 搬 `.failed`）、
空輪次（exit 0 且**日誌行數不變**）。界線也驗到了：合成的四筆裡，
`--since`（台北 07:35）切掉前一筆、`--until-receipt` 切掉後一筆，`main_turns` 正好是 2。
另跑 `bash -n`、`plistlib.load`、`py_compile`、`selftest()`（0 失敗）、
`advisory_verify`（18 PASS · 0 FAIL）。正本與排程副本用六個標記字串比對，兩邊都是 8。

**怎麼倒回去**　刪掉那兩個新檔、把 `env.py` 的 `kbusage` 那一行拿掉，其餘不受影響
（`kbusage` 不存在時 sidecar 只是沒人撿，不會壞任何事）。
`usage_report.py` 的 `--since` 正規化要倒回去就把那一段拿掉並把四處 `since` 改回 `a.since`。
文件類只增不刪語意。排程 prompt 要倒回去得整份重推。

**當時已知的風險**
①**`launchctl load` 還沒做，這一支現在還沒在跑。** 連同欠裝的
`kbfile.research`、`kbwatch.research`，機器上有三支待裝。**在裝上去之前，
sidecar 會一直堆在 outbox 裡沒人撿** —— 那不會壞任何東西，但也不會有新的一列。
②**這一支自己就示範了「只在生產跑得動的分支等於沒驗過」。** 初版用
`stat -f %m`（macOS 方言）算 sidecar 年紀，在 Linux 上 `$(A || B)` 把 A 的
檔案系統輸出也吃進變數、數值比較安靜失敗；更糟的是**那一行排在 `rc_all="$rc"` 前面**，
於是整個失敗分支被跳過、腳本回報 exit 0 —— **量測失敗而 launchd 看不到**。
改成先記失敗、再用 sidecar 自己帶的 `date` 算年紀（不依賴 `stat` 方言，
也不受複製與備份重設 mtime 影響）。**這個缺陷是負向測試抓到的，不是讀程式讀出來的。**
③`--since` 從來沒有被正規化，而 `--until` 有 —— 同一個檔案裡已經寫著
「界線是字串比較，格式不對不會報錯、只會安靜不切」，卻只套用在一個旗標上。
**同一個坑修一半比沒修更難發現**，因為文件會說「界線已經處理過了」。
今天補上，但也要記住：**它從加進去到被發現，中間沒有任何一輪會報錯。**
④sidecar 的格式現在只有一個家（`MEASURE.md`）。**中途一度有兩份** ——
`SKILL.md` 裡抄了一份 JSON 範例，當場認出來是同一種副本並撤成指路。
`kbusage.sh` 的檔頭註解裡仍有一份說明性的範例，那是**給讀腳本的人看的**、
不是給執行者照抄的；若之後格式要改，這裡是第二個要一起改的地方。
⑤這一套只解決「量測誰來跑」。**「這一輪值不值得」仍然沒有人在問** ——
`usage.csv` 累積起來之後要有人去看，而那件事沒有排程。

---

## 2026-08-24 續二｜第一次真的跑，就推翻了同一天上午才寫下的「回執比自述可靠」

**動到哪些檔**　`metrics/MEASURE.md`（sidecar 的 `until` 由選填改為建議必填、
〈上界不用 sidecar 給〉整節改寫成〈上界用 `window.to`，不要用回執〉並附四期實測表）；
`launchd/kbusage.sh`（檔頭說明改寫；沒給 `until` 時退回回執並**在日誌記一筆說上界是鬆的**）；
`skills/advisory/SKILL.md` 與排程 prompt（`until` ＝ 步驟 7 的 `window.to`）。

**量測**　`launchctl bootstrap` 之後手動放一張 sidecar 試跑，第一列 advisory 進了 CSV：
`46,079k／287 輪／12 個子代理／1,164 個子代理輪次`。**但那個數字偏高** ——
上界取的是回執 `at`（10:04），而交草稿是 09:41，混進 23 分鐘的維護對話。
回頭比四期的 `window.to` 與回執 `at`：08-21 差 1 分、08-23 差 −1 分、08-24 差 24 分、
**08-22 差 613 分鐘**。原因是 `~/outbox/<日期>.receipt.json` **每一次 publish 都覆寫同一個檔**，
所以 `at` 是「那個日期最後一次 publish 的時刻」，不是「那一輪落地的時刻」。

**怎麼驗的**　合成三筆逐字稿（01:00Z 之前兩筆、01:50Z 一筆），回執設在 02:04Z、
`until` 設在 01:41Z：給 `until` 算出 `3k／2 輪`，不給退回回執算出 `43k／3 輪`，
且日誌恰好出現一次「沒給 until」的警告。**差 14 倍，而兩個數字都很合理。**

**怎麼倒回去**　三個檔的那幾段拿掉即可；`kbusage.sh` 的邏輯本來就是
「有 `until` 就用、沒有就退回回執」，這一輪只動了預設的建議與日誌，沒動控制流。

**當時已知的風險**
①**「回執的時刻是量測過的，比任何自述都可靠」這句話是今天上午我自己寫進 `MEASURE.md` 的，
下午就被第一次真跑推翻。** 它錯的方式很典型：平常只差一兩分鐘，
**所以它壞掉的那天不會有人發現**，而算出來的數字仍然很合理 ——
與這套系統一路記的「安靜的錯」同一個形狀。教訓不是「回執不可靠」，是
**「覆寫式的檔案不能當時間戳用」**，而 outbox 裡每一個 `<日期>.receipt.json` 都是覆寫式的。
②`window.to` 也不是完美的上界：它是**交草稿**的時刻，而輪次在那之後還有等回執、
讀回執、寫報告。那段通常一兩分鐘，今天因為 publish 壞掉是 23 分鐘 ——
**用 `window.to` 會少算那一段**。兩害相權取其輕：少算一段固定的小尾巴，
比多算一段長度不可控的別人的工作好。
③今天這一列（46,079k）**還沒重切**，等人跑那兩行指令。在那之前
`usage.csv` 的第一列 advisory 是偏高的，拿它跟 chart／podcast 比會得到錯的結論。

---

## 2026-08-28｜保底層連兩天漏跑、中文來源被英文門檻系統性誤判，以及一個蓋了三天的雙向漂移

當天早上那一輪照常出刊（112 張卡、`exit 0`、commit `c5d1db5`、1 小時 43 分、補位 0 輪），
下午回頭處理它交出來的待修清單。**清單本身有兩條是錯的**，而修它們之前得先發現這件事。

**動到哪些檔**
`launchd/kbprefetch-advisory.sh`（快取由 `~/.advisoryfetch/raw/` 搬到 `~/outbox/floor/`；
三次 curl 失敗後改在 Mac 本機跑 `tools/fetch_advisory.py`；落地的檔標 `produced_by`）、
`launchd/com.kenny.kbprefetch.advisory.plist`（只改註解，**ProgramArguments 沒變、不必重裝**）、
`kbcore/env.py`（那一列的註解，值仍是 `["curl"]`）、
`advisory/anchors.json`（`paywall_verdict` 新增 `complete_cjk`／`blocked_cjk`；
`dedup_exempt` 加 TWSE `t187ap04_L`；`collectors.roster` 把 MarketWatch 由 A 移給 D；
黃金 quota 平日 3→4、週末 2→3，`owners` 同步）、
`scripts/advisory/preamble.md`（第三節新增〈中文來源用另一組數字〉，
第六節十二列選擇器改寫，第六之二的 `[BLOCKED]` 觸發條件與注入字串兩節改寫，
電子報彙整頁表加四列，第七之二 EIA／SPDR／LBMA／FRED 四處，第八與第九節）、
`scripts/advisory/slice_preamble.py`（`SECTION_OWNER` 補一列、改一列）、
`scripts/advisory/preamble/{A..G}.md`（**全部重新生成**）、
`skills/advisory/SKILL.md` 與排程 prompt（整份取代兩次，見下）。

**量測**
①`fetch-floor` 最近 14 筆的 `run_started_at`：08-19 到 08-25 的七次排程班延後
**16／18／19／20／16／19／21 分鐘**，非常穩定；**08-26 與 08-27 那兩班整班沒有出現**
（序列由 `08-25T23:03Z` 直接跳到 `08-27T04:00Z`）。
②當日中文稿正文實測：鉅亨 5–8 段／454–972 字元（10 則）、MoneyDJ 5–6 段／674–978、
華爾街見聞 12–21 段／740–1,276。對照英文的 Fierce Biotech 5–7 段、Bloomberg 12、NYT 34。
③`preamble.md` 由 08-25 的 19,193 字元長到 **27,622 字元（+44%）**；
七份切片 11,983–17,273 字元，省 **37–57%**。
④`publish.py` 的 `QUIET` 是 **10 秒**；當日草稿寫入到回執 `exit 0` 隔 **84 秒**。
⑤正本 SKILL 與排程副本比對：**兩邊各自領先** —— 正本有前言切片機制、
`tabs_context_mcp` 禁令與兩條完成條件；副本有 usage sidecar 的 `since`／`until` 三段。

**怎麼驗的**
`py_compile` 全綠；`selftest()` 失敗數 0；`advisory_verify` 對已發布的 2026-08-28
是 **17 PASS · 1 WARN · 0 FAIL**（那一條 WARN 是當期 `thermo.note` 354 字，
發布後才看到、依規不改寫）；`anchors.json` 與 `data/*.json` 共 29 個全部 `json.load` 通過；
資料 repo 內符號連結 0；`slice_preamble.py --check` 七份全部「一致」；
`bash -n` 過預抓腳本；`plistlib` 讀得開那份 plist。
**排程同步用行數對帳**：正本 511 行、排程檔 511 行 —— 相同才算對。

**怎麼倒回去**
預抓腳本把 `CACHE` 改回 `$HOME/.advisoryfetch/raw`、刪掉「第二條路」那一整段即可
（驗證區塊與退出碼語意都沒動）。anchors 的四處各自獨立，拿掉任一處不影響其他。
`preamble.md` 改回舊選擇器就重跑一次 `slice_preamble.py`。
排程 prompt 拿 `skills/advisory/SKILL.md` 的舊版整份再貼一次。
**七份切片是生成物，不要手動回復** —— 改正本再重跑那支。

**當時已知的風險**

①**這一輪最重要的產出不是任何一個修改，是「早上那份待修清單有兩條是錯的」。**
「把 cron 提前 60–90 分鐘」是只看了兩筆 Actions 紀錄就下的診斷，而真相是漏跑 ——
**提前 cron 對漏跑一點幫助都沒有**；「`publish.py` 的 `.strip()` bug 仍未修」
是憑 08-24 的記憶寫的，當下讀一次檔就會看到它已經是 `rstrip("\n")`、
連事故經過都寫在註解裡。兩條都已寫進 SKILL 步驟 9，規則是
**每一條待修事項都要指得出它根據今天讀到的哪一個輸出**。

②**本機自取備援還沒有在真實條件下跑過。** 它依賴 `~/.venvs/kb/bin/python` 裝了
`openpyxl`（SPDR 回 XLSX，而 `SPDR:GLD` 在 `ESSENTIAL` 裡）與
`~/.config/fred/api_key` 存在。兩個假設都有實據（前者在 `requirements.txt`、
五圖那套天天在用那個 venv；後者是 `setup-keys.sh` 寫進去的），
但**沒有一個是今天量到的** —— 這台機器的 shell 在這個工作階段碰不到。
腳本對兩者都有具名的失敗訊息，落到 `~/outbox/floor/prefetch.log`（那個檔輪次讀得到）。
**驗證方法是明天早上看第①層有沒有東西、`produced_by` 是什麼。**

③**前言切片這一輪才第一次真的上線，而它已經蓋了三天。** 機制在 08-25 就寫好了、
`C.md` 也生成了，但排程 prompt 一直沒同步，於是 08-26／08-27／08-28 三輪都照舊發完整版
——**功能存在、沒有人用，而且不會有任何東西報錯**。這是雙向漂移的實際代價。
現在七份都生成了，但**七份同時上線違反了 SKILL 自己寫的「切片一次只上一組、
其餘各組行為完全不變」**。那句話是為了控制變因；今天一次全上，是判斷
「正本已經長到 27.6k、每天七個採集員各扛一份的成本比控制變因的價值高」。
**若明天出現無法歸因的採集品質下滑，第一個要回退的就是這裡。**

④**CJK 門檻的兩組數字只有一天的樣本。** 段數下限 5 取的是當日中文樣本的下緣
（鉅亨 AIDC 那篇就是 5 段），字數 600 是拿 1,500 除以 2.5 的資訊密度比推的 ——
**那個 2.5 是判斷，不是量測**。若接下來幾輪出現「中文稿普遍卡在 5 段」的回報，
代表下限訂得太貼；反過來若開始有明顯被截斷的稿子通過，代表訂得太鬆。

⑤**MarketWatch 搬走之後，A 只剩三家外部新聞源。** 當日 A 交 22 則的組成是
Bloomberg 15（讀滿上限收工）＋ CNBC 6 ＋ MarketWatch 1，所以搬走的是它幾乎沒用到的那家；
但這也意味著 **Bloomberg 再被風控一次（如 2026-08-09），A 會比以前更難補**。
那一天的解法是降 A 的配額或臨時借一家，**不是讓兩個人同時打 Bloomberg**。

⑥**`~/outbox/floor/` 這個新目錄沒有任何檢查在看它。** `publish.py` 只掃根目錄的
`*.draft.json`、`kbusage.sh` 只掃 `*.usage.json` 與 `*/*.usage.json`，都不會誤撿；
但同樣地，**它長期不更新也不會有人叫**。目前唯一的訊號是輪次步驟 1 那張表。

⑦**`fetch-floor` 的 cron 一個字都沒改，而 Actions 那一側今天完全沒動。**
workflow 檔在 `advisory-rewrite` 裡，而 `publish.py` 只 stage `data` 與 `index.html` ——
在本機改它會讓工作區變髒、下一輪發布直接 `exit 15`。**要改得由人在 GitHub 上改。**
本輪的判斷是不必改：本機自取備援已經讓保底層不依賴那個排程器。

---

## 2026-08-29｜黃金第二次用「每天換網址」閃過去重，而 BRIEF 指的那個豁免清單並不存在

當天早上那一輪照常出刊（117 張卡、`exit 0`、commit `746d0cd`、1 小時 47 分、補位 0 輪），
**而且它自己撞上了本次要修的東西**：交稿前驗證回 `exit 10`，LBMA 定盤卡的網址與前一版共用。
下午回頭查，發現那不是單一事件。

**動到哪些檔**
`advisory/anchors.json`（`dedup_exempt` 加 LBMA `gold_pm.json` 與 `gold_am.json` 兩條；
`_dedup_exempt_source` 補 08-29 的理由；**`_dedup_exempt_cost` 兩處過時一併更正**）、
`advisory/BRIEF.md`（第五節那條指向不存在的 `DEDUP_EXEMPT`，改指 anchors，並補「網址固定不是內容固定」）、
`scripts/advisory/preamble.md`（第六節十六列選擇器、第六之二三條、第七節台股端點三處、第七之二三處、第四節分頁那條）、
`scripts/advisory/slice_preamble.py`（`ROW_OVERRIDE` 補 `Fed 官方演說`）、
`scripts/advisory/preamble/{A..G}.md`（**全部重新生成**）、
`skills/maintain/advisory/MAIN.md` 與 `MODIFY.md`（刪掉不存在的徽章表檢查項，共四處）。
**`checks/advisory.py` 一個字都沒改** —— 它本來就從 anchors 讀，加豁免不必動它。

**量測**
①**LBMA 網址四天換三個**：08-25 `lbma.org.uk/prices-and-data/lbma-precious-metal-prices`、
08-26 `gold_pm.json`、08-27 `gold_am.json`、08-28 `gold_pm.json`，08-29 再用 PM 那條被擋下。
**08-27 那張卡的標題與主數字都是 PM 定盤、網址卻指向 AM 端點** —— 讀者點進去看不到卡片描述的那件事。
②逐日跨版比對 08-22→08-29 七個接縫，非豁免的共用網址各為 0 條 —— **因為換了網址，所以檢查照樣全綠**。
③`checks/advisory.py` 全檔搜不到 `DEDUP_EXEMPT` 這個識別字（BRIEF 指的那個家不存在）。
④`index.html` 全檔搜不到任何以來源名為 key 的對照（`Bloomberg`／`CNBC`／`鉅亨`／`Nikkei`／`WSJ` 零命中），
`.t-*` 也沒有任何 CSS —— **MAIN.md 叫人查的那張徽章表不存在**。
⑤venv 實測：`pip install -r requirements.txt` 當場補裝了 `openpyxl` **與 `yfinance` 及其整串依賴**
（numpy／pandas／protobuf／requests…）—— **缺的不只一個套件，是整份 requirements 從來沒同步過**。
⑥`preamble.md` 27,597 → 35,430 字元；七份切片 23.8–32.9k → 27.0–38.6k。

**怎麼驗的**
`python3 -m py_compile` 全綠；`selftest()` 0 失敗；
`advisory_verify.py` 對 `data/2026-08-29.json` 跑 **18 PASS · 0 WARN · 0 FAIL**（加豁免前後都是）；
`anchors.json` 可 `json.load`；`data/*.json` 29 個全部可 `json.load`；
`slice_preamble.py` 重跑七份全部生成；
**`/Users/macmini/.venvs/kb/bin/python -c "import openpyxl"` 回 3.1.5**（使用者在 Mac 上跑的，本工作階段碰不到 shell）。

**怎麼倒回去**
anchors 的 `dedup_exempt` 砍掉最後兩條即回到 08-28 行為（其餘三個檔的改動都是文件，不影響執行）。
`slice_preamble.py` 的 `ROW_OVERRIDE` 若回退，要連同 preamble 的 Fed 那一列一起拿掉，否則切片會炸——
**而那個「炸」是對的**：它今天就是這樣擋下我的漏配置，沒有讓某一組瞎掉。
七份切片是生成物，改正本再重跑。

**當時已知的風險**

①**豁免清單一次長兩條，盲點跟著大兩格。** `advisory.exempt_card_freshness` 只在數字集合
**完全相同**時 WARN，而 LBMA 的 AM 與 PM 是兩條不同序列、卡片常同時引用兩者，
**只要其中一條動了就不會觸發** —— 換句話說這兩條的「沒更新」比 FRED 那兩條更難被抓到。
反過來說，不收 AM 就等於把 08-27 那種「改指 AM 閃去重」的動機留著。**兩害相權取其輕，但代價是真的。**

②**08-28 那一筆的 `_dedup_exempt_cost` 漏改，今天才發現。** 那次加了第六條卻沒把
「現在罩著五條」改成六條，也沒把「目前沒有任何機器在問」改掉（`exempt_card_freshness` 08-23 就做好了）。
**同一段文字連續兩次沒跟上，代表它不會自己被想起來** —— 已在該段明寫「每次加豁免都要回頭改這個數字」，
但更穩的做法是讓檢查程式自己去數，本輪沒做。

③**preamble 又長了 7.8k，而它是每天七個採集員各扛一份的東西。** 切片把單份壓到 27–38k，
但正本本身已經 35.4k、16 節。**下一次再長，該考慮的是把第六節那張表拆成獨立檔按需載入，
而不是繼續往下加列。**

④**今天寫回的 20 條全部來自單一輪次的回報，沒有一條是跨輪覆驗過的。**
其中「Mint 模板不能靠路徑前綴判定」直接推翻了 08-28 才寫上去的半條規則 ——
**兩天兩個相反的結論，代表這一家的樣本還不夠**。若下一輪 `storyContent` 又在 `/market/` 底下正常，
要記的是「兩組都跑」這個做法本身，而不是任何一邊的路徑規則。

⑤**Actions 的 `fetch-floor` 這一輪一樣沒動，而它的失效形態比 08-28 記的更複雜。**
今天拉的 14 筆顯示：08-27 那班在台北 12:00 跑、08-28 那班在 14:21 跑，
**兩班都跑了，但都在該輪收工之後** —— 檔案進了 `raw/` 卻對當輪無用；08-29 那班則完全沒出現。
所以「漏跑 vs 延後」這個二分法本身不夠用，**真正該問的是「有沒有趕在該輪開跑前落地」**。
本輪的判斷仍是不改 cron、先讓本機備援兜底（openpyxl 已補），**觀察一週**。

## 2026-08-30｜Actions 從來沒有漏跑：連續三份報告把「還沒到」讀成「沒有來」

**動到哪些檔**

- `advisory-rewrite/.github/workflows/fetch-floor.yml` —— cron 由 `43 22 * * *`（台北 06:43）
  改為 `15 16 * * *`（**台北 00:15**），並補上一段註解說明**台北 00:00 是硬下限**的理由。
- `kb-core/scripts/advisory/preamble.md` —— 第六節八列改寫／補寫（ECB、IBD、CNBC、WSJ、
  Bloomberg、Mint、MoneyDJ、STAT News）、Barron's 散文補一條 slug 改寫、
  **新增 `### 發稿日曆：哪幾家在哪幾天本來就不發稿`**、六之二補 08-30 那次注入的處置紀錄。
- `kb-core/scripts/advisory/slice_preamble.py` —— `SECTION_OWNER` 補上 `### 發稿日曆` 一列。
- `kb-core/scripts/advisory/preamble/{A..G}.md` —— 七份切片全部重生（生成物）。
- `kb-core/advisory/anchors.json` —— `collectors._roster_notes` 補黃金組的邊界條件。**數字一個都沒動。**
- `kb-core/skills/advisory/SKILL.md` —— 步驟 1 的「延後 vs 漏跑」整段重寫、步驟 3 完成條件
  加「改派要重算兩組合計」、步驟 5 加「結構性偏薄的組要補派工不是補輪數」、
  步驟 9 更正兩次錯誤診斷並加第三次、時間預算補 08-30 那一輪。
- 排程 `advisory-daily-0730` 的 prompt —— 整份取代。

**量測**

① **`fetch-floor` 最近 16 筆 `run_started_at`：11 個排程班對 11 次執行、全部 `success`、一班都沒漏。**
延遲分兩個時代 —— 08-19 到 08-25 七班是 **+16～21 分**（22:58–23:03Z，非常穩定）；
08-26 班 → 08-27T04:00Z（+5h17m）、08-27 班 → 08-28T06:21Z（+7h39m）、
08-28 班 → 08-29T03:52Z（+5h10m）、08-29 班 → **08-30T00:44Z（+2h01m，台北 08:44）**。
② `kbprefetch-advisory` 在台北 07:20／07:22／07:24 各試一次、07:26 放棄 ——
舊 cron 給的餘裕只有 **+37～43 分**，**後四班一班都涵蓋不到**；新 cron 給 **7 小時 05 分**，涵蓋其中四班。
③ **今天 `raw/2026-08-30.json` 確實產出了**（1,403,365 bytes），只是在該輪發布時才由
`pull --rebase` 帶回本機（mtime 09:49 ＝ 當輪回執時刻）。
④ 生技健護的週末實交量（08-09 起）：**3／4／3／2／3／4／2，下限 2 從未被破**。
⑤ `preamble.md` 35,430 → **41,962 字元**（正本），17 節；七份切片 10.7k（C）–40.2k（D），
**A／C／E 拿不到新增的〈發稿日曆〉**（它們手上沒有表列的任何一家）。
⑥ SKILL 正本 511 → **598 行**，排程檔 **598 行**（相同 ＝ frontmatter 那五行正好被系統生成的五行取代）。

**怎麼驗的**

`python3 -m py_compile tools/*.py checks/*.py systems/*.py kbcore/*.py` 全綠；
`selftest()` 0 失敗；`advisory_verify.py` 對 `data/2026-08-30.json` **18 PASS · 0 WARN · 0 FAIL**；
`anchors.json` 可 `json.load`；`data/*.json` 30 個全部可 `json.load`；
`slice_preamble.py --check` **exit 0、七份全部「一致」**；
資料 repo 內 `find . -type l` 歸零；
排程檔行數與正本逐一比對（598／598）。
**`index.html` 沒動，故未跑 `node --check`；`groups` 沒動，故未跑 `chart_verify.py`。**

**怎麼倒回去**

cron 改回 `43 22 * * *` 即回到 08-29 行為（**但那會把餘裕退回 +37～43 分**）。
其餘六個檔全部是文件與生成物：`preamble.md` 回退後要連同 `slice_preamble.py` 的
`SECTION_OWNER` 那一列一起拿掉、再重跑切片，否則切片會炸 —— **而那個炸是對的**，
它今天就是這樣擋下我漏配置〈發稿日曆〉的歸屬，沒有讓某一組安靜地少一節。
`anchors.json` 只加了一段散文，刪掉即可。

**當時已知的風險**

① **cron 不能再往前挪，而那條限制是隱形的。** `tools/fetch_advisory.py` 第 119 行用
**執行當下的台北日期**當輸出檔名（`dt.datetime.now(+8).strftime("%Y%m%d")`）。
挪到台北 00:00 之前，這一班就會寫成 `raw/<昨天>.json`，**而 prefetch 找的是 `<今天>.json`，
於是它永遠找不到、也永遠不會報錯**。已寫進 workflow 註解與 SKILL 步驟 1，
但**真正的修法是改成用 `argv[2]` 明確傳日期，本輪沒做** —— 沒做的理由是那會改變
`fetch_tw_backdated()` 的回溯起點語意，而週末樣本不足以驗證它。

② **新 cron（台北 00:15）只涵蓋四班中的三班，最壞那一班（+7h39m）仍會落空。**
落空時本機自取兜底 —— 今天實測 10/10、耗時 3 秒。**但那條備援在 08-29 剛失敗過一次**
（venv 缺 `openpyxl`，`failed_essential` 非空故未落地），修好之後只跑過一次成功。
**單一成功樣本不足以稱它可靠。**

③ **把 cron 提前也可能讓 TWSE 的晚間資料還沒發布。** 新時刻台北 00:15 距離
`MI_MARGN` 的「當日晚間即時公布」只有幾小時餘裕，而 TWSE 自己的說明就寫了
該表完成時間受成交量值與各授信機構傳輸順遂與否影響。**今天是週日、沒有測到平日的行為**，
下一個平日輪要特別看 `session_date` 與 `looked_back` 這兩欄。

④ **這一筆更正的是「已經更正過一次」的診斷，而且是連續第三次同型錯誤。**
08-28 憑兩筆寫「延後 317 分、建議提前 cron 60–90 分」；同日拉 14 筆改寫成
「08-26 與 08-27 整班沒有出現，是漏跑」；08-30 拉 16 筆才確認**兩班都跑了、只是還在排隊**。
而 08-30 當輪的報告與**已發布的 `about.notes`** 又寫了「Actions 連續第二輪未供稿」，
證據只有 `prefetch.log`（它只能證明「07:26 之前抓不到」）。
**依「已發布不改寫」，`data/2026-08-30.json` 不動**，更正只寫在這裡與 SKILL 步驟 9。
**三次的共同形狀不是資料不足，是拿一個近似的觀測當結論** —— 而近似觀測都很便宜。

⑤ **生技健護沒有改設定，那是刻意的，但它建立在七個樣本上。**
今天的執行報告原本寫「週日輪結構性缺料、建議 `floor.weekend` 由 2 降到 1」，
**查了七次週末輪才發現下限從未被破**（2–4 則），所以只改 `preamble` 的發稿日曆與
SKILL 步驟 5 的補位判準，設定一個字不動。**風險是七個樣本裡只有兩個是「2」**，
若接下來的週日輪連續掉到 2 以下，該回頭重讀這個決定 —— 判準是**下限被破**，不是「補得辛苦」。

⑥ **`slice_preamble.py` 的歸屬判準這次被我自己測試了一次，而它擋住了。**
新增的〈發稿日曆〉橫跨 D／B／F／G 四個採集員的來源，我第一次跑忘了配置，
它直接非零退出、沒有安靜地塞給全體。**但這也暴露一件事**：判準寫的是
「表上出現了誰的來源」，而該節裡還有 CME 一列 —— **CME 不在 roster 的任何來源清單裡**，
所以它不影響歸屬，也代表**沒有任何採集員被正式指派 CME**（實務上由 D 在用）。
本輪沒動 roster，但這是一個名不副實的歸屬。

### 補記：cron 那一改差點變成「本機改了、但兩邊都沒生效」

**`.github/` 不在 advisory 的 `staged_paths`（那是 `["data", "index.html"]`）。**
所以在本機 `~/advisory-rewrite` 改 workflow 會同時踩到兩個坑，而且**兩個都不會報錯**：

1. **對 Actions 沒有任何效果** —— Actions 讀的是 origin，本機 clone 改了也不會上去。
2. **它還會擋住下一次發布** —— `publish.py` 的 `dirty_outside()` 會發現列管路徑外
   有未提交的變更，直接失敗。那正是 `SKILL.md` 步驟 8 `exit 15` 那一列描述的形狀，
   而 publish 每 60 秒跑一次，等於**每分鐘威脅一次隔天的輪次**。

本輪的實際經過：先在本機改了 → 查 `staged_paths` 時發現上面兩點 →
**立刻把本機檔還原**（用 Python ＋ zlib 純讀 `.git/objects` 逐位元組比對 HEAD 確認乾淨，
沒有跑任何 git 指令）→ 改走 **GitHub 網頁編輯器**直接 commit 到 `main`。
落地後從 `raw.githubusercontent.com` 獨立複驗：85 行、`cron: "15 16 * * *"`、
註解區塊在第 0 欄、YAML 主體縮排完整、`${{ secrets.FRED_API_KEY }}` 與 `git push` 都在。

**兩條可重複使用的操作教訓：**

- **GitHub 網頁編輯器（CodeMirror 6）會把多行貼上的縮排咬壞。** 實測輸入
  `aaa:` / `␣␣bbb: 1` / `␣␣␣␣ccc: 2` / `ddd: 3`，得到的是 `ccc` 變 6 格、`ddd` 變 6 格 ——
  **自動縮排會把前一行的縮排疊加到你自己帶的縮排上**。所以**不要用打字的方式送多行 YAML**。
  合成 `ClipboardEvent` 也沒用（CM6 擋 `isTrusted` 為 false 的貼上），
  外部設 `document.getSelection()` 同樣沒用（CM6 會用自己的內部狀態覆蓋回去）。
  **可行的做法只有兩種**：①全部維持在同一個縮排層級（例如整段都是第 0 欄的 `#` 註解，
  這時繼承進來的縮排是 0，不會疊加）②單行替換，完全不含換行。本輪兩種都用上了。
- **要在 CM6 裡精準選取一段文字，用「真實雙擊 ＋ `shift+Right` 數次」。**
  雙擊讓 CM6 自己建立選取（它才會認），再用方向鍵擴充到確切字數
  （本輪：雙擊 `43` 選到 2 字元，再 `shift+Right` 9 次 ＝ `43 22 * * *` 共 11 字元）。
  **每一步都要回讀 `getSelection().toString()` 確認**再打字 —— 本輪前兩次嘗試
  （座標點擊、DOM Selection）都是「指令回報成功、文件其實沒變」，
  只有回讀選取內容才看得出來。

**這一條的風險**：`.github/` 仍然不在 `staged_paths`，所以**下一次改 workflow 會再撞一次**。
本輪沒有把它加進去，理由是那會讓 publish 從此自動 commit `.github/` 底下的任何改動 ——
那是一個比「偶爾手動改一次」更大的權限。**如果未來 workflow 改動變頻繁，再回頭權衡。**

### 補記二：十二條實測補寫，以及我自己在同一天種下又拔掉的一個「安靜生效」的 bug

**動到哪些檔**：`scripts/advisory/preamble.md`（七列補寫今日實測）、
`scripts/advisory/slice_preamble.py`（**新增一道護欄**）、七份切片重生。

**補寫的十二條**（全部來自 2026-08-30 當輪的採集回報，先前只寫了使用者列出的八條）：
OGJ 的 Nuxt epoch 正則配到 0 組 ＋ DOM 退路 ＋ `/latest` 不存在；
華爾街見聞渲染 3–15 秒（把 30–70 秒改讀成上界、改用輪詢條件而非固定秒數）、
`information-flow` 回 37 筆而非硬上限 28、premium 路徑重導；
Bloomberg 的整點毫秒佔位時間戳、以及短訊落在「不完整也不算被擋」的中間地帶；
MarketWatch 的絕對時間戳 ／ 不載入更多 ／ 區段頁置頂是常青稿；
Fierce 區段頁不是一律 4 筆（首頁 10 筆最便宜）；
The Economist 的 `/latest` 302 與版面頁 `/topics/` 改寫；
Nikkei RSS 50 筆而非 151（錨點法仍有效）；CME 輪詢 500ms 即滿足。

**我種下的 bug，以及它為什麼安靜**

〈發稿日曆〉那一節我插在**選擇器表的前面**。`split_sections` 按 `^#{1,3} ` 切節，
於是**整張選擇器表被切進了〈發稿日曆〉那個子節**，後果有兩個，**兩個都不報錯**：

1. `filter_table` 只對開頭是 `## 六、來源` 的那一節生效 —— 表離開之後**不再被過濾**，
   B／D／F／G 各自拿到**全部 21 列**（包含別人的來源）。
2. `owner_of` 按節分配 —— 〈發稿日曆〉的 owner 是 {B, D, F, G}，
   所以 **A／C／E 三份切片的選擇器表整張消失**。

**`--check` 是綠的**，因為它比的是「磁碟上那份」與「重生成那份」，兩邊一起錯。
是切片的**字元數對不上**才露出來：改完 Bloomberg（A 的來源）之後 A 的大小一個位元組都沒變。
**那一輪沒有排程跑過，所以沒有實害** —— 但下一輪 A、C、E 會在沒有任何選擇器表的情況下開工，
而它們的症狀會是「這家今天讀不到」，也就是這份前言整節都在防的那件事。

**修法**：把〈發稿日曆〉移到選擇器表**之後**（`### [BLOCKED:` 之前）。
**再加一道護欄**：`split_sections` 現在會斷言「`**選擇器與路徑` 這個標記仍在 `## 六、來源` 那一節裡」，
不在就非零退出並指出它跑到哪一節去了。**實測過會炸**：故意把〈發稿日曆〉搬回表前面，
它印出「選擇器表跑出「## 六、來源」了，它現在在：### 發稿日曆…」並拒絕生成。

**怎麼驗的**：修完逐份清點選擇器表 —— A 5 列（Bloomberg／CNBC／ECB／美國財政部／Fed 官方演說）、
B 2（WSJ／The Economist）、C 2（Nikkei／華爾街見聞）、D 4（MarketWatch／TrendForce／OGJ／Tom's Hardware）、
E 3（IBD／Politico／The Hill）、F 2（鉅亨／MoneyDJ）、G 4（Mint／Fierce／Korea Herald／STAT），
**合計 22 列、無重疊無遺漏，且每一份只含自己的來源**；今日七條實測也各自落在對的人手上
（Bloomberg→A、Economist→B、見聞與 Nikkei→C、OGJ 與 MarketWatch→D、Fierce→G）。
`--check` exit 0、七份全部「一致」。切片節省率回到 46–64%（出事時 B/D/F/G 只省 4–17%，那也是徵兆之一）。

**當時已知的風險**：這道護欄只認**一張**表（選擇器表）。
`### 電子報彙整頁` 與 `### 發稿日曆` 底下也各有一張表，**它們是刻意整張給特定人、不過濾的**，
護欄不管它們 —— 所以「在那兩節前面再插一個 `###`」這種同型錯誤仍然抓不到。
真正通用的作法是讓每一節宣告自己該不該被過濾，本輪沒做。
## 2026-09-01｜把「尾端沒動」讀成「來源壞了」：第四次同形狀，而這次只差一次查詢就看得出來

**動到哪些檔**：`scripts/advisory/preamble.md`（發稿日曆新增 LBMA 一列；CME 那一列補「0 列不等於連不上」；
選擇器表 MarketWatch 那一列補等待條件；電子報表 WSJ 那一列加 `/pro/` 前綴；Kitco 那一列修正日期呈現的記載）、
`scripts/advisory/preamble/{A..G}.md`（生成物，已重跑 `slice_preamble.py`）、
`skills/advisory/SKILL.md`（步驟 3 加「素材數當場數」；步驟 6 加官方數據卡的 `date`／`ts` 規則與同版 `url` 自檢，
並改寫該步的完成條件）。**`advisory/anchors.json` 一個數字都沒動**，`checks/` 與 `systems/` 未動。
**排程 prompt 未動也不需要動** —— 2026-08-31 起它是指標式的，改 `SKILL.md` 就等於改了排程。

**量測**：LBMA `gold_pm.json` 全序列 14,672 筆。2026 的六個英國銀行假日（01-01、04-03、04-06、05-04、05-25、
**08-31**）在序列中**全部缺席**，假日隔天 05-26 有；2025 的三個對照（08-25、05-26、04-21）也全部缺席。
CME 輕原油結算頁：輪詢後 `table tr` 14 列、`TRADE DATE` 為 `Friday, 28 Aug 2026`、頁註 `31 Aug 2026 08:48:58 PM CT`。
MarketWatch `/latest-news` 立刻讀 `body.innerText` **0 字元**、`.element--article` **0 個**、
`document.title` 只有 `marketwatch.com`；**等 3–4 秒後** 3,666 字元、標題正常、首頁 9,387 字元。
`slice_preamble.py`：正本 55,956 → 57,575 字元（+1,619），七份切片節省率 44–65%。

**它當初藏住的方式**：2026-09-01 那一輪的採集員 D 回報「LBMA 尾端仍停在 08-28，成因待查」，
我把它照抄進執行報告的待修事項、也寫進了**已發布的** `about.notes`（「成因待查」）。
**那是一個近似觀測（尾端沒有往前走）被當成了結論（來源異常）。** 真正該問的問題是
「這個序列在英國假日長什麼樣」，而它只要對同一份 json 多跑一次過濾就答得出來 ——
**2026-08-31 是最後一個八月的週一，英國夏季銀行假日，倫敦金銀市場休市，那天根本沒有定盤。**
這是 `SKILL.md` 步驟 9 那個形狀的**第四次**（前三次是 08-28 的「Actions 漏跑」、
08-30 的「Actions 連續第二輪缺席」、以及 08-30 自己更正 08-28 時又犯的那次），
差別只在前三次錯在「筆數不夠」，**這一次筆數是夠的、我沒有去問**。
**同輪還有一個雙胞胎**：D 回報 CME 與 MarketWatch「工具層完全無法連線、各 4 次嘗試皆在導航層被拒」，
我同樣照抄進 `about.notes`。**一小時後複驗兩家都正常** —— MarketWatch 那個「連不上」的形狀
（title 只剩網域、body 0 字元）**就是沒等渲染**，而那正是這份前言整節都在防的那件事。
**兩件事的共同點是：我把採集員的回報當成量測，而它其實是回報。**

**怎麼驗的**：`py_compile` 全綠；`selftest()` 0 失敗；
`advisory_verify.py` 對已發布的 `data/2026-09-01.json` 仍是 **17 PASS · 1 WARN（`card_lengths`）· 0 FAIL**
（本輪未動 `checks/`，這一項是回歸基準不是新結果）；`slice_preamble.py --check` exit 0、七份全部「一致」，
並**實測過它的非零退出**（故意在 `C.md` 尾端加一行 → exit 1，重生成後回 0）；
新內容逐份清點落在對的人手上：MarketWatch 等待條件只進 D、WSJ `/pro/` 進 A／B／D／E、
LBMA 發稿日曆列進 B／D／F／G（那一節本來就整節給這四個人，不過濾）。

**怎麼倒回去**：`preamble.md` 的五處都是「整列尾端追加」或「插入一整列」，
把新增的字串刪掉再跑一次 `slice_preamble.py` 即可，不影響其他列；
`SKILL.md` 三處是新增段落，刪掉即還原（**注意步驟 6 的完成條件被改寫過一句，
還原時要一併把「且全站沒有任何兩張卡共用 `url`」那半句拿掉**）。
`advisory/CHANGELOG.md` 刪掉本筆。**已發布的 `data/2026-09-01.json` 全程未動**，沒有倒回去的問題。

**當時已知的風險**：
①**LBMA 那一列列的是 2026 的英國假日日期，它會過期。** 2027 年的日期沒有寫進去，
明年 1 月第一次撞到時，看到的仍會是「尾端沒動」。真正不會過期的作法是讓保底層去抓一份假日表，
本輪沒做 —— 所以那一列的第一句刻意寫成判準（「先查那天是不是英國銀行假日」）而不是日期清單。
②**同版 `url` 重複這條仍然沒有機器在看。** 本輪只把它寫進 `SKILL.md` 的完成條件與撰寫分身的任務卡，
而 `advisory.cross_version_dedup` 的 `blind_to` 第二條「同一版內部重複」原封不動。
寫進檢查程式是對的做法（成本是一個 `Counter`），本輪沒做，因為那要動 `checks/advisory.py`
與它的 fixture，不在使用者確認的範圍內。**在它被寫進檢查之前，這一條靠的是執行者記得。**
③**`?ref=` 那種「加參數造出第二個連結」連未來的 url 去重檢查也擋不掉**，
要擋只能正規化 query string，而那對 `dedup_exempt` 裡帶參數的 TWSE／TPEX 端點是有害的。本輪不處理，僅記錄。
④`skills/maintain/advisory/MODIFY.md` 的〈排程 prompt〉那一節仍寫著「先改正本、再整份貼過去」，
**那是 2026-08-31 之前的做法**；照它做會把剛拆掉的逐字副本重新造回來。本輪已發現、未修（見下一筆）。
## 2026-09-01 續｜維護文件叫人去做一件 08-31 才剛拆掉的事，以及「同版重複」終於有機器在看

**動到哪些檔**：`checks/advisory.py`（新增 `advisory.same_version_dedup` 與 `_norm_url()`；
`cross_version_dedup` 的 `blind_to` 補兩條、行為未動）、`checks.lock`（129 條）、
`skills/maintain/advisory/MAIN.md` 第 1 步第 2 項、
`skills/maintain/advisory/MODIFY.md`（〈改哪裡〉表兩列、〈排程 prompt〉整節、〈驗證〉清單末三條）。
**`anchors.json` 未動、`systems/` 未動、資料 repo 未動。**

**它當初藏住的方式（第一件）**：`MODIFY.md` 的〈排程 prompt〉寫著
「先改 `skills/advisory/SKILL.md` 正本，再整份貼過去」，`MAIN.md` 第 1 步寫著
「排程裡那份是副本，兩邊要一致」。**兩句在 2026-08-31 就已經不成立** ——
那天排程 prompt 改成指標式、刪掉了逐字副本，理由正是那份副本製造過兩次事故。
**照維護文件做，會把剛拆掉的副本重新造回來**，而且做完之後兩份都存在、
看起來還很「同步」——這正是 08-31 記載的第二次事故（雙向漂移）的起點。
**這一條是自己咬自己的尾巴：維護文件停在它自己記載的那次改動之前。**
發現的方式很偶然：我今天要改 `SKILL.md`，讀 `MODIFY.md` 找程序時撞到的。
**沒有任何檢查在看維護文件與它所描述的系統是否一致**，這一點沒有變。

**它當初藏住的方式（第二件）**：`advisory.cross_version_dedup` 的 `blind_to` 第三條
逐字寫著「同一版內部重複」——**它從第一天就誠實地說了自己看不到這件事，而沒有人補上那條**。
`blind_to` 的設計意圖是把盲點寫出來讓人決定要不要補，但寫出來之後如果沒有人回頭讀，
它就變成一份**已經被承認、因此不再刺眼**的清單。2026-09-01 那一輪一次出現三處同版重複，
三處都是主線落檔時肉眼發現的；其中一處用 `?ref=press-credentials` 在網址後面加參數，
**連「字串相同」都繞過了**。

**量測**：拿 33 期已發布封存回測 `same_version_dedup`：
**9 期有非豁免的同版重複**（07-30 9 筆、08-02 6、08-03 5、08-04 1、08-08 2、08-09 4、
08-12 1、08-16 2、08-21 3），**最後一次是 08-21，08-22 之後到今天沒有再發生**。
最誇張的是 07-30 的 `wallstreetcn.com/news/global` 出現 6 次、`bloomberg.com/markets` 4 次
——**那兩個還是列表頁，本來也該被 `url_is_article` 擋下，兩條檢查一起漏掉了同一批卡**。
豁免側只有 08-21 一次（兩張卡共用 `spdrgoldshares.com/usa/gld/`），本條刻意放行。
網址正規化的影響面：全庫 3,330 個相異網址中只有 13 個會被改寫（全部是 TPEX 的 `date=`），
**且跑過「正規化會不會改變任何一期的跨版判定」——0 期**，所以它是嚴格的改進不是行為變更。

**怎麼驗的**：`py_compile` 綠；`selftest()` 0 失敗（新檢查的 `fixture` 是同一篇加 `?ref=x`
→ 必須 FAIL，`near_miss` 是兩個不同網址 → 必須 PASS，**邊界就在 1 與 2 之間**）；
`tools/verify.py --selftest` 報出「新增的 check id」後跑 `--write-lock`，`checks.lock` 129 條、
`advisory.same_version_dedup` 在第 13 行。
回測三期各自的結果：**07-30 FAIL 9 筆、08-21 FAIL 3 筆（訊息點名三菱電機那一對）、
09-01 PASS**。今天這一期的完整報表由 17 PASS 變成 **18 PASS · 1 WARN · 0 FAIL、退出碼 0**。
（07-30 直接跑 `advisory_verify.py` 會在 `_floors` 崩掉，因為那期沒有 `window` 欄位——
**那是既有的舊檔不相容，與本次新增無關**，所以改用直接呼叫 `_same_version_dupe` 回測。）

**怎麼倒回去**：刪掉 `checks/advisory.py` 裡 `# ── 7 之二` 那一整段（含 `_TRACKING_PARAMS`、
`_norm_url`、`_same_version_dupe` 與它的 `register`），把 `cross_version_dedup` 的 `blind_to`
還原成三條，再跑一次 `tools/verify.py --write-lock`。
維護文件三處都是「加註 ＋ 刪節線」，把新增段落刪掉、刪節線內的字恢復即可。

**當時已知的風險**：
①**這條擋得住症狀，擋不住病因。** 同版重複的成因通常是任務卡要求的則數大於素材裡真正不同的
事件數；擋下來之後分身只會改成「少交一則」，那會變成 `group_floors` 的問題而不是這條的問題。
真正的防線在派工（`SKILL.md` 步驟 3 的「素材數當場數」），**這條是最後一道網，不是第一道**。
②**`_norm_url` 的追蹤參數是白名單，站方自訂的參數（例如 `?src=`）仍然繞得過。**
不敢整段砍 query string，因為 `dedup_exempt` 裡的 TWSE／TPEX 端點靠 `date=` 才指得到那一天。
③**正規化只做在這條，沒有做在 `cross_version_dedup`。** 所以「加追蹤參數繞過跨版去重」
仍然有效，已寫進那條的 `blind_to`。沒有一起改是因為那會改到一條每天在跑的檢查的語意，
本輪已量到「0 期會受影響」但仍選擇分開處理——**要改就單獨改一次、單獨驗一次。**
④**維護文件與它描述的系統之間仍然沒有任何機器在對帳。** 這次是靠人撞到的。
可以做的是在 `MAIN.md` 第 1 步加一項「Read 排程 prompt 並確認它仍是指標式」——本輪已加，
但那仍然是「要有人照著做」，不是機制。

---

## 2026-09-02｜規格是對的、寫在文件裡、而它整輪沒有發生：把併行從行文搬進完成條件

**動到哪些檔**：`skills/advisory/SKILL.md`（步驟 3 完成條件改寫成四條、第一條是併行，
並補上當輪的耗時歸因）；`scripts/advisory/preamble.md` 七處
（第二節分頁禁令加一條帶前提的例外、第四節加「篇數當場記」、
`[BLOCKED]` 那一節加兩種觸發物與一條「靜默截斷」、
第六節選擇器表的 Bloomberg／Kitco／OGJ／MoneyDJ 四列）；
`scripts/advisory/preamble/*.md` 七份切片重新生成。**沒有動任何數字，`anchors.json` 未變。**

**量測**：

**①併行沒有落地，而它先前只寫在行文裡。** 2026-09-02 那一輪主線把七個採集員**一個一個發**，
一次都沒有併行。分段實測：第一批四人序列 **64 分**（07:41–08:45）、第二批三人 **63 分**（08:45–09:48），
全輪 **3 小時 26 分**（07:35–11:01）。照 `anchors.collectors.batches` 併行，兩批各約 20–25 分，
**這一輪因此多花約 60 分鐘**。規格本身沒有問題——`anchors.collectors.dispatch` 與 `SKILL.md` 步驟 3
的行文都寫得很清楚。**問題是它寫在行文裡。** 這與 2026-08-26 切片一次都沒被讀到、
以及 2026-08-20 那輪十四個分身逐一執行（3 小時 45 分）是**同一個形狀**，
而 08-26 那次的結論當時就寫下來了：**「一行指示埋在四百行裡不會改變行為，寫進完成條件才會。」**
這一次是那句話自己的反例——它被寫進了 CHANGELOG，但沒有被寫進步驟 3 的完成條件。

**②分頁群組會整個消失，而現行禁令防的是另一件事。** 採集員 D 遇到兩次、G 在開工第一次呼叫就遇到，
錯誤原文 `This session's tab group no longer exists (tabs were closed)`；**此時 `tabs_create_mcp` 直接報錯，建不出分頁**。
而 `preamble.md` 第二節 2026-08-29 加的禁令寫的是「不要用 standalone `navigate` 代替 `tabs_create_mcp`」，
證據是當天有採集員這樣做、**拿到群組的共用第一個分頁、把另一組正在讀的 Barron's 導走**。
**兩者的前置條件相反**：08-29 是「群組還在，所以有別人的分頁可以拿」，09-02 是「群組不在了，沒有別人的分頁」。
G 明確驗過復原後的新分頁是從 `chrome://newtab/` 起的。**代價已量到**：D 第二次遇到時漏補了
TrendForce 一篇的末段（SanDisk 營收數字），那一則的最後一個數字沒有進卡。

**③`[BLOCKED: Cookie/query string data]` 的兩種新觸發物，當日 8 次全中這兩種。**
（A 3 次、C 1 次、G 2 次、其他 2 次，**全部靠「拆成分次呼叫」規避成功、零素材損失**。）
第一種是**回傳值裡含 CSS 選擇器的字面**：A 在 Bloomberg 回傳裡帶了 `[class*="Body"] p`
被吃掉、改回裸數字即通過；同形狀在 `home.treasury.gov` 再中一次，觸發物是
`a[href*="/news/press-releases/"]`，**把選擇器裡的斜線拿掉就通過** —— 所以礙事的可能是
`/` 與 `?` 這些 query-string 形狀的字元，而不是「選擇器」這個概念。
第二種是**同一次回傳裡混了時間戳格式與長篇正文**：G 在 Mint 與 Fierce 各中一次，
兩次都沒有 URL、沒有 HTML、沒有 query string。**兩種都印證「拆成分次呼叫」是唯一
不需要猜對觸發物的規避法。** 另外 B 量到一種**不是被擋、但長得像的失效**：
一次回 22 條路徑只印出 11 條就 `[TRUNCATED]`、沒有任何 `[BLOCKED]` 字樣，
**看起來就是「只有這 11 條」**，B 靠索引對不上才發現。

**④Bloomberg 的「`article p` ＝ `main p`」只在部分文章成立，而它錯的方向會讓你高估。**
A 在 **10 篇上同時量兩組**：只有 1 篇相等，其餘 **9 篇 `main p` 一致多出 3–12 段**
（7 vs 12、8 vs 12、11 vs 15、10 vs 14、12 vs 15、15 vs 19、8 vs 14、8 vs 11、25 vs 37），
多出來的全部是側欄與推薦區。**這與 Korea Herald `article p` 高估兩三倍那一列是同一個形狀**：
選錯容器會把短稿判成完整取得。

**⑤Kitco 的「網址日期桶與時間戳差一天」不是錯誤，成因是時區渲染。**
`Published` 欄用瀏覽器本地時區渲染，**讀到的就已經是台北時間**。兩項獨立證據：
AM Report 內文寫 `early U.S. trading Tuesday`（≈08:30 ET）、頁面顯示 `Sep 01, 2026 - 8:32 PM`
（08:32 ET ＋12 ＝ 台北 20:32，逐字吻合）；另一篇 `11:11 PM` ＝ 11:11 ET。
**09-01 記下的那個「路徑 2026-08-31、時間戳 09-01 04:18」因此完全說得通** ——
網址桶用美東日期、頁面時間戳已是台北，兩者本來就會差一天。

**⑥OGJ 的 Nuxt 正則連續第二輪配到 0 組（08-30、09-02），DOM 退路連續第二輪可用**（首頁 26 組 unique）。
**但 DOM 退路有一個 08-30 沒記到的坑**：`closest()` 會把日期節點配到鄰居的連結上——
09-02 有一則標的是 `Sept. 1, 2026`、開頁後文章頁是 `Aug. 26, 2026`，
文章 id `55400944` 也明顯小於 8/31 那批的 `5540188x`，白開 1 篇。
另量到用 `fetch()` 取區段頁只能拿到 SSR 子集：`/refining-processing` fetch 解出 2 組、navigate 3 組，
**漏掉的那一組正是當日窗口內唯一那則**。

**⑦MoneyDJ 列表頁「字數」欄部分復活，但這一輪刻意不改判準。**
連續三輪（08-29／08-30／08-31）全為 0，09-02 量到 **20 筆非 0、52 筆為 0**，
而**非 0 的全部集中在 `mb070100` 一個分類**，其餘四類仍全 0。
**所以「全站失效」這個描述不準確，正確的是「分類別失效」** ——
但**單日單分類不足以推翻連續三輪**，本輪只在該列記一筆待覆測，判準維持「一律退回開頁判定」。

**⑧A 對 Bloomberg 讀了 18 篇、超出 15 篇上限 3 篇。** A 自陳是中途口頭盤點時漏算了最前面兩篇。
全程無風控徵兆（有節流、無擋、無 429 樣態），**這一次沒有代價**。已記進當期 `about.run`。

**怎麼驗的**：`slice_preamble.py` 重生成後 `--check` 七份全部「一致」、**exit 0**；
逐項確認新增段落落到對的切片上——**不綁來源的三條（分頁例外、篇數當場記、BLOCKED 兩種觸發物）
七份全有，Bloomberg 只在 A、Kitco 與 OGJ 只在 D、MoneyDJ 只在 F**，與設計相符。
`py_compile` 全綠；`selftest()` 0 失敗；
`advisory_verify.py` 對當期 `data/2026-09-02.json` 仍是 **19 PASS · 0 WARN · 0 FAIL**
（本輪未動檢查與數字，這一項是回歸確認不是新證據）；
`anchors.json` 可 `json.load`；排程 `advisory-daily-0730` 重讀後仍是 **47 行指標式、路徑指得對、`enabled` 為真**。

**怎麼倒回去**：`SKILL.md` 步驟 3 把四條完成條件與其後的耗時歸因整段刪掉、
恢復成原本那四行散文。`preamble.md` 七處都是「在既有段落尾端追加」或「加註 ＋ 刪節線」，
把追加段落刪掉、刪節線內的字恢復（Bloomberg 那列要把開頭改回
「正文 `article p`（＝`main p`）」、末欄日期改回 `08-31`；Kitco 與 OGJ 的末欄改回 `09-01`／`08-30`），
**然後一定要重跑 `slice_preamble.py`** —— 只還原正本而不重生成，等於還原沒有生效。

**當時已知的風險**：
①**「寫進完成條件」這個修法本身沒有被驗證過。** 它的前身（08-26 把切片路徑寫進完成條件）確實有效，
但那是 n=1，而**這一次要防的行為發生在完成條件被讀到之前** ——
派工是先發生的，完成條件是事後對帳。所以它擋得住「事後發現沒併行」，
擋不住「當下忘記併行」。**真正的機制解是讓派工這個動作本身只有一種發法，而本輪沒有做到那一步。**
下一輪若仍然序列執行，該檢討的就不是文件而是「主線有沒有能力在一則訊息裡發多個呼叫」。
②**分頁復原程序寫了兩個順位，而第二順位與 08-29 的禁令在字面上仍然衝突。**
靠的是「群組是否存在」這個前提來分辨，而**那個前提要執行者自己判斷**。
寫了「必須確認新分頁是從 `chrome://newtab/` 起的」當護欄，但那同樣是要有人照著做。
③**BLOCKED 的觸發物清單越長，越像在收集症狀而不是理解機制。** 目前已列七、八種形狀。
**真正穩的只有一條「拆成分次呼叫」**，其餘都是事後歸納，而且 08-28 已經有一次
「先前寫的『不要含 URL』太窄了」的前例。**下次再擴之前，先問要不要把清單收斂成那一條。**
④**MoneyDJ 那一欄的「待覆測」沒有人在追。** 它寫在 preamble 的表格裡，
而 preamble 沒有任何機制會提醒下一輪去量。**如果下一輪沒有人想起來，這筆待覆測就會永遠掛著** ——
與 2026-08-19 那個「配額等於下限，待決」一樣，那一筆是九天後才被判掉的。
⑤**Bloomberg 那個等號被推翻，但先前依賴它寫成的卡沒有回頭查。**
09-02 之前若有採集員用 `main p` 量字數，短稿可能被誤判成完整取得。
**沒有回測，因為歷史封存裡不存段數與字元數**——那是量測當下的東西，事後補不回來。

---

## 2026-09-03｜維護文件自己漂了五處，其中兩處會叫維護者做錯事

**動到哪些檔**：`skills/maintain/SKILL.md`（入口表後三套的「排程 taskId」欄，
以及底下那段「後三套在這台機器上沒有排程」的說明）；
`skills/maintain/advisory/MAIN.md`（第 1.1 步 CHANGELOG 的閱讀方向、
第 1.4 步的檢查條數、第 1.6 步補上哨兵節奏）；
`skills/maintain/advisory/FILES.md`（`skills/advisory/SKILL.md` 那一列的「排程裡那份是副本」、
`checks/advisory.py` 那一列的「18 條」）。
**沒有動任何數字，`anchors.json` 未變；沒有動任何程式；沒有動執行路徑上的任何檔。**

**量測**：

**①檢查條數：文件寫 18，實際跑 19。** `MAIN.md` 第 1.4 步寫「十八條檢查」、
`FILES.md` 寫「18 條」；`grep -c 'id="advisory\.' checks/advisory.py` ＝ **19**，
`advisory_verify` 的摘要行也是 `19 PASS`。第 19 條是 **2026-09-01 加的
`advisory.same_version_dedup`** —— 而那一次的 CHANGELOG 標題就叫「『同版重複』終於有機器在看」，
**同一次修改動了 `MAIN.md` 的指標式段落，卻沒回頭改這兩個數字**。
處置不是把 18 改成 19，是**把數字拿掉**：它數得出來，手寫一個就是第二份副本。

**②入口表說後三套「本機無排程」，實際三套都 enabled 而且在跑。**
`list_scheduled_tasks` 實查：`bubble-weekly-0900`（每週一、lastRun 08-31）、
`convergence-weekly-1500`（每週一、lastRun 08-31、草稿寫進 `~/outbox/convergence/`
由 `com.kenny.kbpublish.convergence` 發布）、`houseview-weekly-1630`（每週五、lastRun 08-28）。
這句話**至少從 2026-08-24 `convergency-weekly-2100` 被取代那天起就不成立**。
**它錯的方向特別糟**：它叫維護者把三套當成死的，而它們每週在跑、其中一套還會發布 ——
**「以為它沒在跑」比「以為它在跑」更危險，因為前者不會有人去看它的產出。**

**③`MAIN.md` 叫人讀 CHANGELOG「最上面那筆」，而這個檔是舊在上、新在下。**
最上面是 2026-08-19，最下面才是最新的。同一份 `MODIFY.md` 的〈收尾〉第 1 條寫的是
「**最下方**加一筆」，而且還特別註記過 2026-08-22 修正過同一個方向錯誤 ——
**兩份文件對同一個檔的方向講反了，而先修好的那一份沒有回頭修另一份。**

**④`FILES.md` 仍寫著「排程裡那份是副本／整份貼過去」。**
2026-09-01 那一筆（標題就是「維護文件叫人去做一件 08-31 才剛拆掉的事」）
把 `MAIN.md` 與 `MODIFY.md` 都改成指標式了，**唯獨漏了 `FILES.md`**。
照它做就是把 08-31 剛拆掉的逐字副本重新造回來，
而那個副本自己製造過兩次事故（偏移五行、雙向漂移）。
**一次收斂只收兩份、留下第三份，這就是 2026-08-17 字數 bug 的形狀在文件層的重演。**

**⑤心跳的節奏沒有寫在叫人去看心跳的那一步旁邊，我今天因此差點犯第五次同形狀的錯。**
`heartbeat.json` 停在 `2026-09-02T11:46:19+00:00`、`day_count` 33（當日發布後應是 34），
看起來像壞了。查下去才確定：心跳由 `advisory-rewrite/.github/workflows/sentinel.yml`
寫、cron `0 7 * * *` ＝ **每天台北 15:00**，查的當下是台北 10:00，**那一班還沒到**；
09-02 那一班本身還延遲了 4 小時 46 分。**本機沒有任何東西在跑 `tools/sentinel.py`**
（沒有 plist 指向它；`com.kenny.kbwatch` 跑的是 `watch_sentinel.py`，那是看門狗、只讀不寫）。
`watch.sentinel_alive` 在沒有 `cadence_hours` 時退回 24 小時才轉紅，
**所以「還沒到」與「真的停了」之間有一段看起來完全一樣的區間**。
這是 08-28、08-30、09-01 記過三次的同一個形狀的第四次，
差別只在**這一次在寫進報告之前就被攔下來了**。

**⑥順帶更正當日執行報告的一條待修事項（它是錯的）。**
2026-09-03 的執行報告第 2 條寫「`TWSE:REV_L`／`REV_O`／`CONF` 三個端點全失敗，
`REV_O` 回 `<html>`，**回 HTML 通常是路徑被改**，值得查一次 URL」。
**當天稍後實測三個端點：全部 HTTP 200、content-type 正確、同一個 URL 沒有變**
（`t187ap05_L` 與 `t187ap04_L` 回 `application/json`，後者 63,000 bytes、
出表日期 1150903 就是當天；`t187ap05_O.csv` 回 `text/csv`、139,913 bytes）。
**所以那不是路徑問題，是取數時點問題** —— 保底層那一班的 `fetched_at` 是
`2026-09-02T19:23:33Z` ＝ **台北 03:23**，落在 TWSE 深夜維護窗內。
**而這與同日發現的 SPDR 那一格是同一個根因**：`fetch-floor.yml` 的 cron 是台北 00:15，
實際延到 03:23，**既早於 SPDR 的紐約歸檔（台北 04:00–04:45），也撞上 TWSE 的維護窗**。
**兩個症狀、一個病。** 修法尚未決定（見下方風險欄），本筆只更正診斷。

**怎麼驗的**：①`grep -c 'id="advisory\.' checks/advisory.py` 與 `advisory_verify` 摘要行兩側對照；
②`list_scheduled_tasks` 全表，逐筆看 `enabled`／`lastRunAt`；
③`grep -n '^## 2026-' advisory/CHANGELOG.md` 的頭尾兩端；
④`grep` `FILES.md` 的該列原文；
⑤`sentinel.yml` 的 cron ＋ `heartbeat.json` 的 `at` ＋ `grep` 全 repo 找誰跑 `tools/sentinel.py`（無）；
⑥對三個 TWSE 端點各發一次實際請求，記 status、content-type、長度、前 120 字。
改完另跑：`advisory_verify` 對已發布的 `data/2026-09-03.json` 仍 **19 PASS · 0 WARN · 0 FAIL**、
`anchors.json` 與 `data/*.json` 全部 `json.load` 可讀。

**怎麼倒回去**：四個檔都是純文件、不在任何執行路徑上，`git revert` 該 commit 即可，
**不需要重跑任何東西**。技能快取那一份**沒有動**，所以即使正本改壞了，
現在正在跑的 `maintain` 技能行為完全不變。

**當時已知的風險**：
①**正本與技能副本現在不一致**（改動前三個檔的位元組數兩側完全相同）。
`MODIFY.md` 的驗證清單把這一組列為「同步組」，而**同步只能靠人工把整個 `maintain`
目錄打包成 `.skill` 重裝** —— 在重裝之前，維護者從技能讀到的仍是舊的那五處。
**這一段不一致是刻意留下的，但它自己就是一個會被忘記的狀態。**
②**⑥那個根因已經診斷出來，但沒有修。** 保底層每天仍會在台北 03:23 左右取數，
所以 SPDR 那一格明天大機率再次拿到與前一版相同的一列、三個 TWSE 端點也可能再失敗一次。
今天是靠派工時把「這一列可能是過期的、請你現場複驗」寫進採集員 D 的任務卡才補上的，
**而那是一個每天都要有人記得寫的臨時處置，不是修好**。
兩個候選修法各有代價，都還沒選：把 `fetch-floor.yml` 的 cron 往後挪會**壓縮對 Actions 延遲的餘裕**
（現在 7 小時 05 分，挪到台北 05:00 只剩 2 小時 20 分，而實測延遲曾達 7 小時 39 分）；
讓 Mac 本機的 07:20 預抓補抓那幾個端點則要**改 `fetch_advisory.py` 讓它吃 ident 子集**，
那是動到執行路徑上的程式。
③**這一輪只查了 advisory 這一套的維護文件。** 另外六套的 `MAIN.md`／`FILES.md`
有沒有同型的漂移（尤其是那三套被寫成「無排程」的），**沒有查**。

---

## 2026-09-03 續｜兩個症狀一個病：保底層在凌晨取數，所以補抓放在 07:20

**動到哪些檔**：`tools/fetch_advisory.py`（新增 `--top-up` ／ `--only` 補抓模式與
`parse_flags`，位置參數行為不變）；`launchd/kbprefetch-advisory.sh`
（驗證閘門通過之後、原子 `mv` 之前插入補抓）；`launchd/README.md`
（`kbprefetch.advisory` 那一列：補上補抓，並更正一條放了六天的舊路徑）；
`skills/advisory/SKILL.md`（步驟 1 加 `top_ups` 讀法表、步驟 3 的黃金那一句加例外）。
**沒有動任何數字，`anchors.json` 未變；沒有動 `checks/`、`systems/`、`publish.py`。**

**量測**：

**①症狀有兩個，根因只有一個：取數時點。** 2026-09-03 那一輪同時撞到兩件事 ——
保底快取的 SPDR 尾列是 `01-Sep`，**與前一版逐字相同**（GLD 1,046.64 公噸／396.75 美元）；
`TWSE:REV_L`／`REV_O`／`CONF` 三個一起失敗，`REV_O` 回 `<html>`。
當天的執行報告把後者診斷成「路徑被改，值得查一次 URL」——**那是錯的**。
當天稍晚對三個端點各發一次實際請求：**全部 HTTP 200、content-type 正確、URL 一個字都沒變**
（`t187ap05_L` 與 `t187ap04_L` 回 `application/json`，後者 63,000 bytes、出表日期 1150903 就是當天；
`t187ap05_O.csv` 回 `text/csv`、139,913 bytes）。
兩者的共同點寫在 `fetched_at`：`2026-09-02T19:23:33Z` ＝ **台北 03:23**，
**既早於 SPDR 的紐約歸檔（台北 04:00–04:45），也落在 TWSE 的深夜維護窗內。**

**②被否決的選項：把 `fetch-floor.yml` 的 cron 往後挪。** 一行改動就能同時解掉兩個症狀，
但它會**壓縮 Actions 對排程延遲的餘裕**：現在 cron 台北 00:15、到 07:20 預抓有 7 小時 05 分，
挪到台北 05:00 只剩約 2 小時 20 分 —— 而 2026-08 實測的延遲分佈是 16 分到 **7 小時 39 分**，
後四班有三班會涵蓋不到。**而且 `SKILL.md` 已經記著「不能再往前挪」**（跨台北 00:00 會寫成
`raw/<昨天>.json`，且不會有任何東西報錯），所以這個旋鈕兩邊都很窄。
**選 07:20 補抓是因為那一班本來就在正確的時間點上**，不必動 Actions 的任何設定。

**③補抓的三條性質，每一條都對應一個想像得到的失效：**
· **補抓失敗保留原值。** 原值可能過期，但它至少是資料；補抓失敗若把它清掉就是
「補一次讓情況變糟」。沙箱實跑驗過（對外全被 403 擋）：5/5 全部保留、`items` 與原檔
**逐位元相同**、`failed_essential` 仍為 0、退出碼 0。
· **`failed_essential`／`failed_other` 拿合併後的整份重算**，不是拿補抓的子集算 ——
只算子集會讓沒被補抓的失敗項從清單裡消失，而它們還在 `items` 裡失敗著。
· **補抓在驗證之後、`mv` 之前**，落地的一定是補過的完整檔；寫入走同目錄 `.tmp` 再 rename。
另外 `fetched_at` **刻意不因補抓而改變** —— 它記的是凌晨那一班，而補抓只換掉幾個 ident。

**怎麼驗的**：`py_compile` 全綠；`selftest()` 失敗數 **0**；
`advisory_verify` 對已發布的 `data/2026-09-03.json` 仍 **19 PASS · 0 WARN · 0 FAIL**；
`anchors.json` 與 35 個日期檔全部 `json.load` 可讀；`bash -n` 通過；資料 repo 無符號連結。
**位置參數回歸**（Actions 那一行不能變）：`parse_flags(['x','raw'])` → `(None, None, ['raw'])`、
`['x','raw','20260903']` → `(None, None, ['raw','20260903'])`。
**錯誤路徑退出碼**：無參數 12、`--top-up` 沒帶 `--only` 12、不認得的 ident 12、檔案不存在 14。
**兩條路徑各實跑一次**：失敗路徑用沙箱的真實網路封鎖，成功路徑用 monkeypatch 過的
`fetch_one` —— 四個 ident 全部由 `failed`／`ok` 換成新值、`failed_other` 由三筆清成空、
**沒被要求的 `TWSE:BFI82U` 逐位元未動**、無殘留 `.tmp`。

**怎麼倒回去**：`git revert` 該 commit。補抓是**加法**：拿掉它之後
`fetch_advisory.py` 的位置參數路徑與腳本的其餘部分完全是改動前的樣子，
已經落地的 `top_ups` 欄位對下游是可忽略的多餘鍵。

**當時已知的風險**：
①**成功路徑沒有在真實網路上跑過。** 沙箱對外全被 403 擋，所以「補抓真的抓得到當日那一列」
這件事**是用 monkeypatch 證明邏輯、不是證明端點**。第一次真正的驗證會是明天 07:20 那一班 ——
**要看的是 `~/outbox/floor/2026-09-04.json` 的 `top_ups[-1].replaced` 是不是五個都在裡面**，
以及 SPDR 那一列的日期是不是 `03-Sep`。
②**補抓讓 07:20 那一班變慢。** 五個端點、其中兩個 SPDR 是歷史序列（實測回傳數萬列後截斷），
而這一班的預算是「07:20 起跑、最晚 07:24 收工，仍早於 07:30／07:35 的輪次」。
**沒有量過補抓要花多久**，如果它把收工推到 07:30 之後，輪次會讀到一份還沒補完的檔
（或更糟：讀到 `mv` 之前的狀態 —— 這一點被原子寫入擋住了，但時序本身沒有被擋）。
**明天要量的第二件事就是這一班的總耗時。**
③**`failed_other` 的排序在補抓路徑變成字典序**，而主路徑仍是迭代序。
兩份檔案並排比對時會看到順序不同，那不是資料變了。沒有統一，因為統一要動主路徑。
④`launchd/README.md` 那條舊路徑（`~/.advisoryfetch/raw/`）**放了六天沒有人發現** ——
它是 2026-08-28 搬家時漏改的。這次順手更正，但**同一次搬家有沒有漏改別的地方，沒有查**。

## 2026-09-04｜`replaced` 不等於資料前進，以及固定網址那一類已經靠不相鄰躲過七次

**動到哪些檔**：`advisory/anchors.json`（`dedup_exempt` 加十條 CME 頁、
`_dedup_exempt_source` 與 `_dedup_exempt_cost` 兩段補述）；
`tools/fetch_advisory.py`（`top_up()` 加 payload 指紋，新增 `top_ups[].unchanged`）；
`scripts/advisory/preamble.md`（十二處：第三節升格一條通則、第六節選擇器表新增
Washington Post 一列並補 Nikkei／見聞／Korea Herald／MarketWatch／STAT News 五列、
電子報彙整頁表新增 WaPo 一列並補 WSJ 前綴黑名單的邊界、第六之二節記 WSJ 第二次、
第七節加兩條待查、第七之二節記 CME Brent 的兩個頁面）；
`scripts/advisory/slice_preamble.py`（`### 電子報彙整頁` 的 owner 清單加 Washington Post）；
`skills/advisory/SKILL.md`（步驟 1 的 `top_ups` 表加第四種狀態；步驟 3 加計量制三判準、
EIA 週更網址清單、STAT+ 抽驗）。**七份 `preamble/<代號>.md` 已重生成。**
**沒有動 `groups`、沒有動任何下限或配額、沒有動 `checks/`、沒有動 `publish.py`、沒有動外殼。**

**量測**：

**①`replaced` 與「資料前進了」是兩件事，而它們在檔案裡長得一模一樣。**
`~/outbox/floor/2026-09-04.json` 的 `top_ups[-1]` 回報五個 ident **全部 `replaced`**、
`kept_original` 空的 —— 看起來完全成功。而 `items['SPDR:GLD'].data.rows[-1][0]` 是
**`02-Sep-2026`**，**正是前一版（09-03 那期）兩張黃金保底卡用過的那一列**
（GLD 1,056.62 公噸／402.78 美元）。是當輪派工端在 D 的任務卡上寫了「請現場開產品頁複驗」、
採集端在台北 08:09 讀到頁面逐字標示 `Updated between … 4:45 p.m. New York time September 3, 2026`，
才確認 `03-Sep` 早就上站，本期兩張卡因此用的是 9/3 那一列、`exempt_card_freshness` 沒有觸發。
**這一條是 2026-09-03 那筆 CHANGELOG 自己留下的待驗問題**（原文：「要看的是
`top_ups[-1].replaced` 是不是五個都在裡面，以及 SPDR 那一列的日期是不是 `03-Sep`」）——
**前半答對、後半答錯**，而只看前半會得出「補抓成功」這個結論。

**②同一筆待驗問題的第二半答對了：補抓沒有把 07:20 那一班拖慢。**
`prefetch.log` 逐字：`07:20:02 === 開始預抓` → `07:20:03 補抓開始` → `07:20:04 補抓結束` →
`07:20:04 落地`，**全程 2 秒**，遠在「最晚 07:24 收工」的預算之內。
09-03 記的風險②（「沒有量過補抓要花多久」）就此關掉。

**③五天的 GLD 末列日期，每天前進一列、而且每天都比產品頁少一天。**
floor 檔逐日：08-31→`27-Aug`、09-01→`28-Aug`、09-02→`31-Aug`、09-03→`01-Sep`、09-04→`02-Sep`。
**端點確實每天在更新，只是慢一步。** 但**這不足以推論「archive 落後一整天」**：
09-01 到 09-03 那三份的 `fetched_at` 都在**紐約收盤之前**（15:1x–15:2x ET），本來就不可能有當日列；
唯一一次收盤後的觀測是今天的補抓（NY 19:20 ET）＝`02-Sep`，
而產品頁那一次是 **49 分鐘後（NY 20:09 ET）、且是不同的端點**。
**所以「archive 落後一整天」與「archive 貼得比 07:20 晚」尚未分辨，兩者都沒有寫進文件當診斷。**

**④固定網址的官方數據頁已經靠「不相鄰」躲過七次。**
掃 08-30 到 09-04 六天的已發布卡片，**七個未豁免的固定網址各出現過兩次**：
CME 天然氣結算（08-30、09-03）、EIA 汽柴油零售（08-31、09-02）、EIA 天然氣分區庫存（08-31、09-04）、
EIA 天然氣週報（09-01、09-03）、EIA 石油週報（09-01、09-03）、CME WTI 報價（09-02、09-04）、
CME 天然氣報價（09-02、09-04）。**七組全部隔了一天以上，而 `cross_version_dedup` 只比前一版。**
另有八個只出現過一次、下一次必撞。今天那次 FAIL（EIA 天然氣週報 09-03→09-04 相鄰）
**是第一次剛好撞上，不是第一次違規。**

**⑤攔截字串的假陽性是第四次，四家、四種樣子。** 見聞（既有，導覽區塊的「会员」）、
STAT News（08-30，掃 `body` 反而漏掉正文第 10 段那句）、CNBC（08-31，導覽列固定有
`INVESTING CLUB`，於是每一篇都命中）、Nikkei（09-04，全頁掃 `/subscription/i` 命中而正文完整
10 段／2,162 字元）。**兩個方向的錯是相反的**：掃全頁會把好文誤判成被擋，只掃正文會漏掉
不在正文區塊裡的那一種 —— 而 08-23 的 STAT News 與 WSJ 注入文字**正是在正文區塊外**。

**⑥計量制門檻第三次失效，而且 `preamble` 早就寫對了。**
E 複驗 Barron's 兩篇：7 段／282 字元與 7 段／354 字元，**皆含攔截字串、
`meta[name="user.type"]` 為 `freeRegister`**，八組選擇器全試過。
段數 7 > 3，照任務卡的字面門檻**兩篇都不構成被擋**。
而三判準 08-31 就已經完整寫進 `preamble` 第六節了 —— **失效的是任務卡，不是前言**，
當日沒有出事是因為 E 讀了正確的那一份。

**⑦STAT News 的注入計數這一輪等於沒有推進，而那不是偶然。**
`preamble` 第六之二節在數「連續第幾輪」，08-31 是第四輪並寫著「第五次就要判這個來源留不留」。
G 在 `/feed/` 預篩階段就靠 `STAT+:` 前綴濾掉付費稿（**正確且省請求**），
而**前四次注入全部出現在 STAT+ 訂閱牆頁面**。所以「兩篇免費稿零命中」推不出「這一輪乾淨」。
**同輪的注入文字反而出現在 WSJ，是該站第二次（首次 08-23）** ——
它推翻了一個正在成形的印象：「只有 STAT News 會出現」開始像規律，而規律一旦成形，
認網域的做法就會重新變得誘人。**五輪之中沒有一次是靠網域清單擋下的。**

**怎麼驗的**：`py_compile tools/*.py checks/*.py systems/*.py kbcore/*.py` 全綠；
`selftest()` 失敗數 **0**；`advisory_verify` 對 **2026-09-04 與 2026-09-03 兩期**都是
**19 PASS · 0 WARN · 0 FAIL**（特別驗前一期，是要確認新增的十條豁免**沒有回溯改變舊期的結果**）；
`anchors.json` 與 **36 個日期檔**全部 `json.load` 可讀；`dedup_exempt` 長度 8 → **18**；
資料 repo 符號連結 **0**；`slice_preamble.py --check` **七份全部「一致」、exit 0**；
`grep -c 'id="advisory\.'` 仍是 **19**（沒有增刪檢查，不必寫 lock）。
`anchors.json` 改動走 **JSON 物件而非字串替換** —— 先驗過該檔與
`json.dumps(indent=1, ensure_ascii=False)` **逐位元相同**，所以重新輸出不會有格式漂移。
`top_up()` 的兩條路徑各實跑一次：**失敗路徑**用沙箱真實的網路封鎖（兩個 ident 全進
`kept_original`、`unchanged` 空、`items` 與原檔逐位元相同、退出碼 0）；
**成功路徑**用 monkeypatch 過的 `fetch_one` 餵一個逐位元相同、一個改了末列日期 ——
`replaced` 兩個都在、**`unchanged` 只有那個沒變的**，沒被要求的 `TWSE:BFI82U` 逐位元未動、無殘留 `.tmp`。
**位置參數回歸**：`parse_flags(['x','raw'])` → `(None, None, ['raw'])`、
`['x','raw','20260903']` → `(None, None, ['raw','20260903'])`。
**錯誤退出碼**：無參數 12、`--top-up` 沒帶 `--only` 12、不認得的 ident 12、檔案不存在 14。
七份切片的分派逐條核對過：新增的攔截字串通則 **7/7 全體都有**（第三節是 ALL）、
WaPo 選擇器列**只有 B**、Nikkei 與見聞兩條**只有 C**、Korea Herald 與 STAT 兩條**只有 G**、
MarketWatch **只有 D**、CME Brent **A 與 D**、台股兩條待查**只有 F**、
WSJ 第二次注入**7/7 全體**（第六之二節是 ALL）。

**怎麼倒回去**：三個獨立的動作，可以分開回退。
①`dedup_exempt` 那十條：從陣列刪掉即可，`cross_version_dedup` 會立刻恢復擋它們
（代價是能源組每隔幾天被擋一次）。②`fetch_advisory.py` 的指紋：`git revert` 該 commit ——
它是**加法**，`unchanged` 對只讀 `replaced` 的下游是可忽略的多餘鍵，
拿掉之後 `top_up()` 與改動前完全一樣。③文件三處：`git revert`，
**但 `preamble.md` 回退後一定要重跑 `slice_preamble.py`**，否則切片會停在新版而正本回到舊版
——`--check` 會抓到，但只有在有人跑它的時候。

**當時已知的風險**：

①**十條豁免讓 `exempt_card_freshness` 的盲點面積變成兩倍多，而它對這十條近乎不帶資訊。**
CME 報價盤中連續變動，「數字與昨天完全相同」在交易日幾乎不可能、在週末則必然
—— 平日恆綠、週末恆黃，**兩種狀態都不用看**。真正要人看的是第三種：
交易日出了卡、數字卻與前一版相同，那多半是取到了前一交易日的結算頁。
這一條寫進了 `_dedup_exempt_cost`，**但沒有任何機器在問它**。

②**EIA 那四個網址改由派工端擋，那是治標，而治標的失效方式是安靜的。**
它要求每一輪的執行者記得在 D 的任務卡上列出前一版用過的那幾個。
2026-09-04 那一輪正好示範了忘記會發生什麼（被本機驗證擋下、修正約 5 分鐘），
**但那一次是因為剛好相鄰才被擋** —— 隔一天忘記，不會有任何東西報錯。
**沒有選擇放寬判準，是因為「每一張卡都是今天新採的」是 BRIEF 第五節的定義，不是旋鈕。**

③**`unchanged` 只在補抓那條路徑上有，主路徑（整份重抓）沒有對應的欄位。**
主路徑本來就沒有「原值」可比，所以不是遺漏；但若哪天有人把主路徑也拿來當補抓用，
這個欄位不會出現，而缺席與「內容有變」在下游長得一樣。

④**指紋比的是整份 `data`，答得出「有沒有動」、答不出「新的那一列是哪一天」。**
選它是為了不對每一種 ident 的形狀各寫一個取法（那是這個檔已經有過一次的漂移形狀），
代價是當 SPDR 落進 `unchanged` 時，還是要人去看末列日期才知道差幾天。

⑤**新加的 Washington Post 選擇器列只有一輪、五個頁面的實測**（`article p` 21／10／33 段），
而 `main p` 混入側欄那一條只有資料中心那一篇（33 vs 56）撐著。
**一篇的觀測不足以支撐一個關於整站的結論** —— 這正是 08-31 那一輪犯過三次的錯，
所以那一列的末欄寫的是 `09-04`，下一輪碰到 WaPo 的採集員要當它是「上一次實測」而不是定論。

⑥**Nikkei 那條刻意沒有照採集員的建議寫。** C 提的是「短稿一律加量裸 `p`」，
而裸 `p` 多出來的是圖說與署名 —— 那是 Bloomberg 09-02 與 Korea Herald 兩列
警告過的同一個形狀（把非正文算進正文量、讓短稿看起來達標）。
改成只記差額、處置沿用「壓線不等於被擋」。**代價是採集端會多幾則落在中間地帶要人判**，
而如果下一輪有人因此把真的被擋的稿記成原生短稿，這個決定就要回頭重看。

## 2026-09-06｜產品頁抓不到、archive 落後一天：SPDR 那個懸了兩輪的歧義，是靠另一個端點分開的

**動到哪些檔**（六個，三類）：

- **程式**：`kbcore/fetch_tw.py`（`ROUTES` 新增 `SPDR:GLD_NOW`／`SPDR:GLDM_NOW`）、
  `launchd/kbprefetch-advisory.sh`（`TOPUP_IDENTS` 由五個加到七個）。
- **流程**：`skills/advisory/SKILL.md`（步驟 1 的 SPDR 診斷定案 ＋ 待驗表 ＋ Mac 端驗證指令；
  步驟 6 補「一般卡 `date` 通則」）。
- **文件**：`scripts/advisory/preamble.md`（第六節五處、發稿日曆 CME 拆兩列、第六之二節換判準
  ＋ 更正 WSJ 次數）、七份切片重生成、`skills/maintain/advisory/FILES.md`（`dedup_exempt` 條數
  與「有沒有機器在看」兩處更正）。

---

### 一、SPDR：兩輪都沒判出來的事，這一輪用「同一天的另一個端點」判掉了

**懸而未決的是**：`historical-archive` 落後，究竟是「archive 落後一整天」（要換來源）
還是「archive 貼得比 07:20 晚」（把補抓往後挪即可）。**兩者的處置相反**，
而 09-04 那次只證明了「台北 07:20 時 archive 沒有 03-Sep」——
產品頁那一次是 **49 分鐘後、而且是不同的端點**，分不出來。
09-04 的 CHANGELOG 因此寫下「在累積到足夠班次之前，不要把任何一個寫成診斷」。

**而累積班次不是答案。** 09-06 這一輪連續第三次落在 `unchanged`，
單看這個數字仍然分不出那兩種可能 —— 三次不夠、三十次也不會夠，
**因為「補抓那一班取不到」與「那一天根本還沒上站」在 `top_ups` 裡長得一模一樣**。

**分開它們的是一個更便宜的觀測**：同一天、同一個供應商的**另一個端點**有沒有那筆資料。
09-06 台北 08:14 實測：archive 末列 `03-Sep-2026`，
而 `api.spdrgoldshares.com/api/v1/data` 的 `total_tonnes.date` 是 `September 4, 2026`。
**04-Sep 是週五、當天是週日** —— 若只是貼得晚，archive 最遲在週六 07:20 就該有它。
**缺的是一個超過 24 小時的資料點。定案：archive 端點自己落後。**

> **這一節真正該記住的不是結論，是「班次數答不出這個問題」。**
> 08-28、08-30、09-01 三次「把還沒到讀成沒有來」的教訓長出來的規矩是「去數班次」，
> 而這一次它不適用 —— **數班次能分辨「延遲」與「漏跑」，分辨不了「延遲」與「來源落後」。**
> 後者要的是**橫向**的第二個觀測，不是縱向的更多次。

### 二、「改抓產品頁」是錯的，而錯得很容易被採納

上一輪的執行報告建議「`kbprefetch-advisory.sh` 的 SPDR 改抓產品頁」。
**那個提議會失敗，而且失敗的樣子是空值不是報錯。**

先探原始 HTML（不執行 JS）：**二十四個欄位全部是 `AWAITING`**，噸數、收盤、NAV 一個都沒有。
再從頁面內 `fetch(location.href)` 搜 `1,052.056` —— **命中 0 次**、`AWAITING` 命中 24 次。
**產品頁是前端渲染的，curl 抓不到它。** 採集員 D 讀得到，只是因為 Chrome 執行了 JS。

**這個提議之所以危險，是因為它有一個看起來很強的證據撐著**：D 確實在產品頁上讀到了
`September 4, 2026`。**「人在瀏覽器裡讀得到」與「腳本抓得到」是兩件事**，
而報告裡那句「改抓產品頁」把它們當成同一件。**同一個形狀在本檔出現過**：
`preamble` 第七節「不開網頁版表單頁」那一條，以及「本機重跑 `fetch_advisory.py`
判斷保底層」那一條 —— 都是把某一個環境下的可讀性當成通則。

**正確的端點是追那一頁的 resource timing 找出來的**（網路面板只看到 trustarc 與 grafana，
資料端點藏在 `performance.getEntriesByType('resource')` 裡，而且工具層會把帶 query 的
完整網址吃成 `[BLOCKED: Cookie/query string data]` —— 要拿它只能把 host 與 pathname
分開回傳）：`api.spdrgoldshares.com/api/v1/data?product=gld&exchange=NYSE&lang=en`。
**與現行 archive 同一個 host、同一組 query 參數，只差路徑。**

### 三、修法：補而不是換，而且它現在是「已部署、待驗」

**新增而不是取代。** archive 給 120 列的跨期序列（黃金卡的四日序列吃它），
`/api/v1/data` 只有當日一筆。**兩個都要** —— 拿掉 archive 不會有任何東西報錯，
只會讓跨期序列在某一天安靜地變成單點。

**附帶好處**：`_NOW` 的每個欄位自帶 `date`（`data.total_tonnes.date`），
所以「資料前進了沒有」從此是**顯性的**，不必再靠 09-04 那個 `unchanged` 指紋去推。
**指紋那條沒有拿掉** —— 它答的是 archive 的問題，兩者不重疊。

**⚠️ 未驗的那一段要寫清楚**：09-06 是從**瀏覽器內**（帶 Referer）取得
`200`／`application/json`／2,378 位元組；**同一個網址用無 Referer 的 HTTP 取回是空的**。
那可能是工具層、也可能是 header 把關，**沙箱沒有網路、也不該用 curl 繞，所以判不了**。
兩個 ident 因此都宣告 `empty_ok`：**取不到時 archive 那條照常落地，不會擋掉整份保底檔**。
**驗法寫進了 SKILL 步驟 1 的一張兩列表** —— 下一輪看 `top_ups` 時順手判，
以及一行可以在 Mac 終端機直接跑的 `curl -w '%{http_code} %{size_download}'`。

### 四、`date` 通則：規則對、檢查對，而任務卡把它寫成了兩條互相矛盾的話

09-06 那一輪的四張撰寫任務卡寫了「`date` 一律 `2026-09-06`」，
而 `advisory.date_ts_consistent` 逐卡比 `card["date"] == ts.astimezone(台北).date()`。
**窗口本來就跨兩天，所以同一個日期檔裡本來就會混著兩個 `date`**
（09-05 那期 108 張卡：70 張 `09-04`、38 張 `09-05`）。

**根因是 SKILL 步驟 6 只寫了官方數據卡的特例、沒寫一般卡的通則**，
於是派工端把同一條規則寫成了兩條。**三個撰寫分身各自獨立指出了它**
（兩個照卡片做並具名回報、兩個逕自改成正確的），主線在組稿時用一行
`date = ts.astimezone(TPE).date()` 統一修正 —— **沒有造成當期損失，
但那又是一次靠分身不照做才擋下來的，不是靠設計。**

**這與 08-31 那三次（開場測試把一次抽樣寫成整站結論）是同一個形狀**：
規格是對的、檢查是對的，而**派工端寫任務卡時把它翻譯錯了**，
擋下來的是分身的獨立判斷。處置也一樣 —— 把通則補在特例旁邊，**兩段要一起讀**。

### 五、STAT News：門檻到了，而判的結果是停止數它

08-31 寫下的條件是「第五次出現就判這個來源還值不值得留」。09-06 由派工端指定
採集員 G 刻意開一篇 STAT+ 檢查，計數到了。**判斷的材料在這一輪才齊：**

- **五次全部在 STAT+ 訂閱牆頁面** —— 我們本來就讀不到、從來不成卡的一層。
- **免費稿五次全部乾淨**，它從未污染過任何一則真的進到產出的東西。
- 它是**生技健護少數的免費來源**，而那一組在週末輪本來就只交得到下限 2。

**結論：來源留著，換掉的是計數本身。**

**⚠️ 換掉它的真正理由是：這條線索在正常作業下測不到，而那是因為預篩做對了。**
G 在 `/feed/` 用 `STAT+:` 前綴濾掉付費稿是對的、也省請求，
而**前五次注入全部出現在被濾掉的那一層**。於是它只有在派工端每輪刻意花一次請求
去開一篇不會成卡的 STAT+ 時才推得動 —— **那正是 `preamble` 在 OGJ 那一列
警告過的形狀：每一輪都要先白花一次呼叫去確認一件已知的事。**

新判準只有兩條，都是「出現在哪裡」而不是「出現幾次」：
①注入文字出現在**免費稿**或任何一則打算成卡的頁面；②**句型變了**。
**派工端不必再指定「刻意開一篇 STAT+」。**

**同節順帶更正一處算錯**：09-04 那段寫「WSJ 第二次出現（首次 08-23）」，
而 08-28 那段自己就寫著「在 WSJ 與 STAT News 各撞到一次」——**是第三次**。
更正的方向不影響結論，只讓「只有 STAT 會出現」那個印象比當時以為的更站不住。

### 六、preamble 其餘五處（全部是「失敗的樣子跟沒新聞一模一樣」）

| 列 | 改了什麼 | 它先前會怎麼騙人 |
|---|---|---|
| The Economist | 補「文章頁要輪詢」 | `navigate` 後立刻量，`article p` 與 `main p` 雙雙 0 段、`title` 只有 `economist.com`；輪詢後 26 段／7,841 字元。**WSJ 與 MarketWatch 兩列都記了這個坑，這一列先前沒有** |
| CME | 拆成**金屬**與**能源**兩列 | 金屬結算已到 `04 Sep`、能源三頁停在 `02 Sep`（破快取複驗過、同頁時鐘自報 `05 Sep 19:24 CT`）。合併寫法會讓下一輪把「能源結算沒前進」誤判成故障 |
| Mint | 「跳過前 23 筆」→「標記但不丟棄」 | 09-06 實測前 23 筆全部比第 24 筆之後**新**（09-05 13:04 → 09-06 03:11 對 09-04 12:19），當輪採用的三則有兩則來自前 23 筆。**08-30 那條沒有錯，錯的是把「共用」當成「舊」** |
| Tom's Hardware | `<title>` 要用 `[\s\S]` | CDATA 前後各有一個換行；單行字元類回空字串，而 `<pubDate>` 同時正常 —— 症狀是「有 50 筆時間戳但一個標題都沒有」 |
| OGJ | DOM 退路改「向上走 `parentElement`（≤6 層）」 | `closest('div,li,article')` 拿到 33 個日期節點但 **0 組配對**。「DOM 退路是主路徑」沒錯，錯的是實作，而它失敗的樣子仍是「這家今天沒新聞」 |

另有兩處是**再次證實、不改判準**：**Korea Herald 第六次**（補上機制：膨脹的絕對量固定在
28–31 段，**所以稿子越短倍率越誇張** —— 09-04 的 7 段短稿 5 倍、09-06 的 27 段長稿 2 倍，
這解釋了為什麼誤判全部集中在短稿）；**MoneyDJ 字數欄第三次覆測全為 0**
（09-02 那次 `mb070100` 非 0 沒有重現，「兩輪一致才改判準」的條件不成立，維持開頁判定）。

---

**量測**（全部是改後量到的，改前的正本字元數當時沒量，所以下面不寫「成長多少」這種需要兩個點的數字）：
`preamble.md` **71,962 字元／18 節**；七份切片 A 35,775／B 29,316／C 25,512／D 41,462／
E 26,021／F 31,411／G 31,085 字元（省 42–65%）。`advisory/CHANGELOG.md` 2,348 行。
`ROUTES` 由 8 條增為 **10 條**、`TOPUP_IDENTS` 由 5 個增為 **7 個**、
`dedup_exempt` 仍是 **18 條**（本輪沒動它）。
選擇器表 26 列、發稿日曆表 14 列（CME 由 1 列變 3 列，含一列刪節線的舊寫法）。

**怎麼驗的**：`py_compile tools/*.py checks/*.py systems/*.py kbcore/*.py` 全綠；
`selftest()` 失敗數 **0**；`advisory_verify` 對 **2026-09-06 與 2026-09-05 兩期**
都是 **19 PASS · 0 WARN · 0 FAIL**（特別驗前一期，是要確認新增的兩個 ident
**沒有回溯改變舊期的結果**）；`anchors.json` 與 **37 個日期檔**全部 `json.load` 可讀；
`grep -c 'id="advisory\.'` 仍是 **19**（沒有增刪檢查，不必寫 lock）；
資料 repo 符號連結 **0**；`slice_preamble.py --check` **七份全部「一致」、exit 0**。
**七份切片的分派逐條核對過**：The Economist 那一條**只有 B**、Mint **只有 G**、
Tom's Hardware 與 OGJ **只有 D**、Korea Herald **只有 G**、MoneyDJ **只有 F**、
CME 兩列（發稿日曆）**B／D／F／G 四份**（A 在該表上沒有任何來源，所以拿不到那張表，
**這是對的** —— 需要它的是 D）、第六之二節的 STAT 新判準 **7/7 全體**。
`sh -n launchd/kbprefetch-advisory.sh` 通過。
**`/api/v1/data` 的回應形狀是實測的**（瀏覽器內帶 Referer）：`200`、`application/json`、
2,378 位元組，`data.total_tonnes.value = 1,052.056`、`data.total_tonnes.date = September 4, 2026`、
另有 `total_ounces`／`close_usd`／`nav_share_usd`／`shares_outstanding` 各自帶 `date`。

**怎麼倒回去**：三個獨立的動作，可以分開回退。
①**兩個 `_NOW` ident**：從 `ROUTES` 刪掉、`TOPUP_IDENTS` 改回五個即可。
**它是純加法** —— 兩者都宣告 `empty_ok`，取不到不影響 `failed_essential`，
下游沒有任何東西在讀它們，拿掉之後與改動前完全一樣。
②**SKILL 兩處**：`git revert`，但步驟 1 那段刪節線要一起回退，否則會留下一個
「已定案」卻沒有處置的懸空段落。
③**preamble 七處**：`git revert`，**回退後一定要重跑 `slice_preamble.py`**，
否則切片停在新版而正本回到舊版 —— `--check` 會抓到，但只有在有人跑它的時候。

**當時已知的風險**：

①**`_NOW` 的 curl 可行性未驗，而它失敗的方式是「什麼都沒發生」。**
`empty_ok` 讓它取不到時安靜略過，這是刻意的（不擋整份保底檔），
**代價是沒有人會被通知**。防線只有一個：SKILL 步驟 1 那張兩列表要求下一輪的執行者
**主動去看保底檔裡有沒有這個鍵**。**那是一個每天都要靠執行者自己想起來的步驟，
也就是一個每天都在賭的步驟**（`metrics/MEASURE.md` 對 chart 那四列講過同一句話）。
若下一輪回報「`_NOW` 仍未通」，該做的是加 Referer 標頭到 `kbcore/fetch.py`，
**而不是再等幾輪看看**。

②**Referer 若真的是必要的，改的是共用底盤。** `kbcore/fetch.py` 的 `UA` 與標頭
是三套系統共用的，為了一個 ident 加一組 per-route 標頭會讓那個檔多一個維度。
**這一輪刻意沒有先加**（沒有證據說它需要），但下一輪若要加，
要加成 route 層級的 `headers` 欄位而不是全域預設 —— 全域帶 Referer 會讓
其他端點的請求看起來像從別人的網站點過來的。

③**發稿日曆的 CME 能源那一列只有一次觀測，而我把「落後兩個交易日」寫成了問句而不是常數。**
09-06 那次是落後兩個交易日（週日讀到週三），**但單一觀測分不出「固定落後兩天」與
「這一週剛好卡到」**。那一列因此明寫「下一輪碰到能源結算時把 TRADE DATE 抄回報告，
累積到三次再判」—— **這正是 SPDR 那一條花了三輪才判掉的同一個教訓，
差別在這一次我一開始就把它寫成待累積，而不是先寫成診斷再回頭更正。**

④**STAT 的新判準把觸發條件從「機械可數」換成「要人判斷」。**
「出現在免費稿」與「句型變了」都比「第 N 次」難以機械化，
**而難以機械化的判準會安靜地不被執行**。接受這個代價，
是因為舊判準的問題更嚴重：它每輪要花一次請求，**而且答不出那個要判的問題**。
若下一輪起連續三輪的回報裡都沒有提到這一節，那代表新判準也沒被讀進當下的判斷，
那時要考慮的是把它變成任務卡上的一行明文指示。

⑤**`preamble.md` 又長了約 6.5%**，而它每天進七個採集員的 context。
第六節那張表現在有六列帶著 09-06 的新段落，**都是「失敗的樣子跟沒新聞一模一樣」這一類**。
切片機制吸收掉大部分（單一採集員只拿到自己那幾家），但**第六之二節是 ALL**，
這一輪加的 STAT 那一段七份都吃到。**下一次若要再加通則段落，
先問它是不是真的不綁來源** —— 綁得住來源的東西放進按來源分派的子節裡才不會七倍計費。

## 2026-09-06 續｜裸 curl 回 `200 2378`：那個「空回應」是工具層，而它差點讓我去改共用底盤

**動到哪些檔**：`kbcore/fetch_tw.py`（`SPDR:GLD_NOW` 的註解）、
`skills/advisory/SKILL.md`（步驟 1 的待驗段改寫）、
`scripts/advisory/preamble.md`（**第七之二節那一行被推翻**）、七份切片重生成。

### 一、驗掉了，而結果推翻的是我自己上一節寫的風險

上一節的風險①寫著「`_NOW` 的 curl 可行性未驗」，並準備了一條後路：
「若下一輪回報仍未通，該做的是加 Referer 標頭到 `kbcore/fetch.py`」。

**使用者在 Mac 終端機跑了那一行，回 `200 2378`。**
**與瀏覽器內帶 Referer 拿到的位元組數逐位元相同** —— 所以：

- **「header 把關」不成立**，那條 Referer 後路不必走。**這很重要**，
  因為 `kbcore/fetch.py` 是三套系統共用的，為了一個 ident 去動它的請求標頭
  是一個代價遠大於收益的改動，而**我當時已經把它寫成了「下一步」**。
- **兩個 client 的位元組數相同**，順便排除了「這個端點吃 session 或做個人化」。
- **先前那個空回應是工具層，不是站方。**

> **這一次的形狀值得單獨記：一個真的觀測，指向一個錯的結論。**
> 「無 Referer 取回是空的」是實際量到的，沒有任何一步是猜的；
> 而它能支持的解釋有兩個（工具層 vs 站方把關），**兩者在畫面上完全一樣**，
> 其中一個會讓人去改共用底盤。**分辨它們的成本只是換一個 client 再試一次。**
> 這與本檔反覆記過的「拿一個近似的觀測當成結論」是同一族，
> 但它多了一層：**這一次連觀測本身都是對的，錯的只有推論。**
> 所以 SKILL 那一段留了刪節線而不是刪掉。

**仍未驗的只剩最後一段鏈**：`kbcore/fetch.py` 送的是自訂 `UA`，不是 curl 預設。
端點既然連 Referer 都不看，UA 被擋的機率很低 —— **但「很低」不是「驗過」**，
所以 SKILL 步驟 1 那張表留著，只是兩列的意思都改了：
現在它問的不是「端點通不通」（已知通），而是「`fetch.py` 那一段通不通」。
**端到端的證據是明天 07:20 之後保底檔裡有沒有這兩個鍵帶值。**

### 二、順手抓到一行會勸阻別人的文件，而它就在同一個主題上

改註解時回頭看 `preamble` 第七之二節，發現這一行：

> **SPDR 的 API 不要浪費時間試**：`api.spdrgoldshares.com/api/v1/{data,table,historical-archive}`
> 存在但缺參數一律回 **422**，而正確參數沒有公開文件。

**前半是對的（缺參數確實回 422），後半今天不成立** ——
正確參數就是 `?product=gld&exchange=NYSE&lang=en`，
**與本站早就在用的 `historical-archive` 是同一組**。
也就是說，**答案一直寫在我們自己的 `ROUTES` 裡，只是沒有人把它套到另一個路徑上試一次**。

**這一行錯的方向是最糟的一種：它勸阻別人去找。**
「不要浪費時間試」把一個**可以被推翻的觀測**變成了一個**沒有人會回頭看的結論**，
而推翻它的成本只是把已知的參數套到隔壁的路徑上。
若不是這一輪為了別的理由去追那個端點，這一行會一直留著。

**因此加了一條寫作規矩進那一節**：
**以後寫這一類句子要寫成「試過什麼、失敗成什麼樣」，不要寫成「不要試」。**
（同節另補一句：產品頁那條退路不能拿掉 —— 它是前端渲染的，
原始 HTML 24 個欄位全是 `AWAITING`，**curl 抓不到、只有瀏覽器讀得到**，
所以採集端現場複驗仍然只能開產品頁。）

---

**量測**：`preamble.md` 由 71,962 增為 **72,841 字元**（＋1.2%、＋879 字元），仍是 18 節；
七份切片 A 36,654／B 29,316／C 25,512／D 42,341／E 26,021／F 31,411／G 31,085 字元
（**只有 A 與 D 變動，各 ＋879**，因為第七之二節只分派給這兩位 —— 逐份核對過，
新段落在 A 與 D 各 1 次、其餘五份 0 次）。`advisory/CHANGELOG.md` 2,443 行。

**怎麼驗的**：`curl -sS -o /dev/null -w '%{http_code} %{size_download}'` 在 Mac 終端機
回 **`200 2378`**（使用者實跑，非本工作階段推論）；
`py_compile tools/*.py checks/*.py systems/*.py kbcore/*.py` 全綠；`selftest()` 失敗數 **0**；
`advisory_verify` 對 **2026-09-06 與 2026-09-05 兩期**都是 **19 PASS · 0 WARN · 0 FAIL**；
`anchors.json` 與 37 個日期檔全部 `json.load` 可讀；資料 repo 符號連結 **0**；
`slice_preamble.py --check` 七份「一致」、exit 0；`bash -n launchd/kbprefetch-advisory.sh` 通過。
**本節沒有動任何程式邏輯**（只動註解與文件），所以沒有新的回歸面。

**怎麼倒回去**：三處都是純文件，`git revert` 即可；
**`preamble.md` 回退後一定要重跑 `slice_preamble.py`**。
**注意不要連同上一節的 `_NOW` 一起回退** —— 那兩個 ident 現在已經驗到端點層，
回退它等於把一個已知有效的修法拿掉。

**當時已知的風險**：

①**上一節的風險①（curl 未驗）已解除，但它留下的那句「下一步是加 Referer 標頭」
如果被人只讀上一節就照做，會白改共用底盤。** 這是本檔第一次出現
「上一節的待辦被下一節解除」——**讀 CHANGELOG 要讀到最下面，而它是舊在上、新在下**。

②**第七之二節現在同時說「這個 API 能用」與「產品頁抓不到」，兩句話容易被讀成矛盾。**
它們講的是不同東西：`api.spdrgoldshares.com` 是 JSON API（curl 可用）、
`www.spdrgoldshares.com/usa/gld/` 是前端渲染的網頁（只有瀏覽器可用）。
**若下一輪有採集員因此把兩者搞混、用 fetch 去打產品頁，那一句要再寫清楚一次。**

③**`preamble.md` 這一節只長了 1.2%（879 字元），比上一節輕得多**，
而它每天進七個採集員的 context。
這一節只給 A 與 D，成本比上一節的 ALL 段落低 —— **但「只給兩個人」是分派機制幫的忙，
不是我寫得短**。下一次仍要先問那段話綁不綁得住來源。

## 2026-09-08｜十條來源實測與五條流程修正，其中兩條是「正本改了而讀者沒跟上」

**動到哪些檔**：`scripts/advisory/preamble.md`（十處：CNBC、ECB、華爾街見聞 ×2、
**新增 Investing.com Fed Rate Monitor 一列**、MoneyDJ、WSJ、Korea Herald、Kitco、
〈電子報彙整頁〉那張表 ＋ 該節一段新說明）、七份 `preamble/<代號>.md`（重生成）、
`skills/advisory/SKILL.md`（六處：步驟 1 `top_ups` 表新增一列、步驟 1 刪 SPDR 待驗表、
步驟 3 門檻計數措辭、步驟 3 刪 STAT+ 指定、步驟 7 絕對路徑、步驟 7 `exempt_card_freshness` 補一段）。
**沒有動任何程式或 `anchors.json`。**

**量測**：`preamble.md` 72,841 → 79,281 字元（**+6,440，+8.8%**），
七份切片重生成後 `--check` 全部「一致」、exit 0。
切片字元數：A 39,607／B 30,868／C 28,400／D 44,385／E 27,573／F 32,887／G 32,407。
**這一節是近期最重的一次**（對照 09-06 那節 +1.2%），因為它一次收了七位採集員的「下游應知」。

**怎麼驗的**：①十條逐條 `assert` 錨點在正本裡恰好出現 1 次，改完印出各自的增量；
②重生成後對十個新增片段做**正反雙向**檢查 —— 該拿到的組拿到（各 1 次）、
不該拿到的組是 0 次（實測 E 沒拿到 CNBC 那條、C 沒拿到 Kitco 那條、A 沒拿到 Korea Herald 那條）；
③`slice_preamble.py --check` exit 0；④`py_compile` 全綠、檢查自檢 0 失敗、
`advisory_verify` 對當日已發布檔 19 PASS · 0 WARN · 0 FAIL。

**怎麼倒回去**：全部是純文件，`git revert` 即可；
**`preamble.md` 一旦回退就一定要重跑 `slice_preamble.py`**，否則切片會領先正本、
而 `--check` 會抓到（那是它存在的理由）。

**當時已知的風險**：

①**這一節有兩條修的是同一種病，而那種病沒有任何機器看得到：正本改了、引用它的地方沒跟上。**
一條是步驟 3 的 STAT+ 指定 —— `preamble` 第六之二節在 **09-06** 就判掉了那個計數、
並逐字寫著「派工端也不必再指定」，而 `SKILL.md` **在那之後兩輪都還在要求它**，
09-08 的 G 任務卡照樣帶著。另一條是本檔上一節留下的 SPDR 待驗表，
它的放行條件訂成「`_NOW` 比 archive 末列新」，而 09-06 修好 archive 補抓之後**兩者相等才是正常**
—— 條件訂窄了，**一件已經成立的事因此差點永遠等不到結案**。
**兩條都是靠人讀出來的**（前者是採集員 G、後者是我在步驟 1 讀保底檔時），
**不是靠任何檢查**。目前沒有任何機制在問「`SKILL.md` 引用的 `preamble` 結論是不是還有效」。

②**步驟 7 那條 `~` 的修正，改掉的是一個「所有檢查都通過而檔案在錯的地方」的失效。**
09-08 那一輪照舊寫法把草稿寫進 `~/outbox/`，而沙箱裡 `~` 是 `/sessions/<id>`、
掛載點是 `/sessions/<id>/mnt/outbox` —— **`wc -c` 回 284,230、JSON 讀得開、
`advisory_verify` 19 PASS，只有 publish 掃不到**。
**現在文件改成要求先量實際絕對路徑、並在完成條件加了「`ls` 看見檔名」**，
但**那仍然是一個靠執行者記得的步驟**，與 `MEASURE.md` 說的「每天都要靠人想起來的步驟，就是每天都在賭的步驟」同型。
真正的解法是讓步驟 7 從環境讀出 outbox 路徑而不是寫在文件裡，**本輪沒做**。

③**新增的 Fed Rate Monitor 那一列，是這張表第一次收一個「非新聞來源」。**
表上其餘各列都是新聞站，而它是一個每天要引用的數據頁。
**好處是它終於有家了**（先前整張表沒有這一家，而它是央行組每天用的那一格）；
**代價是這張表的定位從「選擇器與路徑」被撐寬了一點**。
若之後 CME、LBMA、EIA 那幾個官方端點也各要一列，該考慮的是另開一節，而不是繼續往這張表加。

④**`preamble.md` 一次長 8.8%，而它每天進七個採集員的 context。**
分派機制擋掉了大部分（最省的 E 只拿 65% 之外的部分、C 拿 64%），
**但 D 只省 44%、A 只省 50%** —— 這兩份已經接近 4 萬字元。
**下一次再往 CNBC 或 WSJ 那兩列加東西之前，先問舊的段落有沒有可以收掉的**：
CNBC 那一列現在有六段 ⚠️、WSJ 那一列有五段，其中最早的幾段（08-24、08-28）
描述的行為已經被後面的段落取代過一次以上。

## 2026-09-08 續｜`~` 在兩個檔案系統上各對一半，以及把護欄搬到「改動的那一端」

**動到哪些檔**：`skills/advisory/SKILL.md`（新增 **步驟 1-0**、另四處 `~/outbox` 改成沿用它）、
`scripts/advisory/preamble.md`（五節各加一行反向指標）、
**`scripts/advisory/slice_preamble.py`（新增 `strip_maintainer_only()`）**、
`skills/maintain/advisory/MAIN.md`（第 2 步漂移清單加一項）、
**排程 prompt `advisory-daily-0730`（`update_scheduled_task`，只送 `prompt` 本體）**、
七份切片重生成。

**量測**：`preamble.md` 79,281 → 79,919 字元；`SKILL.md` 34,152 → 33,985 字元（**淨減**，
因為步驟 7 那段當天上午寫的說明被收攏進步驟 1-0）；排程 prompt 47 → 50 行。
**七份切片的字元數與加反向指標之前逐字相同**（A 39,607／B 30,868／C 28,400／D 44,385／
E 27,573／F 32,887／G 32,407）—— **這就是 `strip_maintainer_only()` 有效的證據**。

**怎麼驗的**：①`py_compile` 通過、`slice_preamble.py --check` exit 0；
②正本 5 條反向指標、七份切片各 0 條；③切片字元數與剝除前逐字相同（見上）；
④排程 prompt 用 `Read` 覆驗：**單層 frontmatter（第 1–4 行）、本體從第 6 行開始、
指標路徑仍是 `/Users/macmini/kb-core/skills/advisory/SKILL.md`、末行「以 SKILL.md 為準」完整** ——
**這一項是刻意逐項看的，因為 `update_scheduled_task` 是整份取代，而 2026-08-28 那次事故
正是連 frontmatter 一起送、變成兩層、整份偏移五行**；
⑤`SKILL.md` 裡剩下的 `~/` 只有三種：禁令本身、描述那次事故的敘述、
以及第 285 行 Mac 端的 `~/.config/fred/api_key`（那一處 `~` 是對的，因為它描述的是 Mac 上的腳本）。

**怎麼倒回去**：文件三處 `git revert`；`slice_preamble.py` 回退後要重跑一次切片；
**排程 prompt 沒有版控**，要倒回去只能再送一次 `update_scheduled_task`，
本節上面那段引言就是它現在的內容。

**當時已知的風險**：

①**我對「反向指標成本」的第一次估計是錯的，而錯的方向是低估。**
提案時說「成本是 5 行、在一個我本來就自由編輯的檔案裡」，
第一版寫成「每節一行指標 ＋ 一段理由」之後實測**每份切片多 4–5 個區塊、約 1,300–1,700 字元**，
乘以七個採集員、每天一次。**那是提案裡沒有算到的**——因為我當時沒想到切片器是整節複製、沒有行級粒度。
處置是把理由集中到 `MAIN.md`、指標壓成單行、並在切片器加剝除。
**記這一筆是因為那個錯誤的形狀會再出現：對「加在正本裡的一段話」估成本時，
要先問它會被複製到幾個地方去。**

②**`strip_maintainer_only()` 是這支腳本第一個「行級」處理，先前全部是節級。**
它剝的是「整行以 `**↩ 被引用：**` 開頭」，判準夠窄、不會誤傷正文；
**但它建立了一個先例** —— 下次有人想加第二種「只給某某看」的行級標記時，
要先問這是不是又一個新的分派維度。**節級分派是這支腳本可解釋性的來源，行級標記多了會侵蝕它。**

③**步驟 1-0 仍然是一個「靠執行者在對的時候做對的事」的步驟。**
它比先前好在：路徑是**量到的**而不是記住的、而且只量一次。
**但沒有任何東西會擋住一個跳過它、直接寫 `~/outbox` 的輪次** ——
`wc -c` 照樣會回一個合理的數字。真正的護欄是步驟 7 新增的那條完成條件
（`ls` 那個目錄、看見檔名），**而那條也是靠人做的**。
要機械化，得讓 publish 在掃不到當日草稿時主動出聲，那是 `publish.py` 的事、**本輪沒做**。

④**排程 prompt 這次被改了，而 `MODIFY.md` 目前列的「真的需要 `update_scheduled_task` 的情況」只有三種
（改 cron、改 enabled、換掉指標本身），這次是第四種。**
它不是副本漂移（那三行不是任何檔案的複製，是這份 prompt 自己的緊急備援內容），
**但下一個維護者照 `MODIFY.md` 讀，會以為這種改動不該發生。**
`MODIFY.md` 那一節本輪沒有更新，**這是一個已知的、留著的不一致**。

## 2026-09-09｜十四條來源觀測落地，而擋住我的是切片器：新增一列來源時它拒絕猜

**動到哪些檔**

- `scripts/advisory/preamble.md`（正本，165,942 → 見下方量測）：第二節分頁例外補兩句；
  第六節選擇器表**十一列改寫 ＋ 一列新增**（Bloomberg、CNBC、Fed、Nikkei、MoneyDJ、鉅亨、
  Fierce Biotech、Korea Herald、STAT News、TrendForce、MarketWatch，新增 **BOJ 官方英文站**）；
  第六節 `[BLOCKED]` 註腳新增一種觸發物；第七之二 CME 結算列補連假形態、並新增一條 EIA 通則。
- `scripts/advisory/slice_preamble.py`：`ROW_OVERRIDE` 新增 `"BOJ 官方英文站": "央行官方"`。
- `scripts/advisory/preamble/{A..G}.md`：七份切片重生成（**生成物，不是手改**）。
- `skills/advisory/SKILL.md`：步驟 3 新增 FRED 歸屬的處置；步驟 5 新增能源組退化態；
  步驟 6 新增兩條素材清單護欄。
- 本檔。

**量測**（全部當場量到，估算標 `~`）

| 項目 | 改動前 | 改動後 |
|---|---|---|
| `preamble.md` | 149,203 位元組 | **165,942 位元組**（88,214 字元、18 節） |
| 切片 A／B／C | 74,819／60,463／56,361 | **84,041／63,271／60,233** |
| 切片 D／E／F／G | 85,102／54,012／65,430／63,186 | **92,207／56,820／70,066／68,746** |
| 切片省下的比例 | — | A 50%・B 63%・C 66%・D 46%・E 67%・F 60%・G 60% |

**⚠️ 這張表本身有一個容易讀錯的地方，值得記**：切片器的報表用**字元**、`wc -c` 用**位元組**，
中文字在 UTF-8 是三個位元組。第一次跑完看到報表寫「A 44,191」而先前記憶裡是 74,819，
**看起來像切片被砍掉四成**。實際上七份**全部變大**。**兩個單位混用時，先換算再下結論。**

**怎麼驗的**

- `slice_preamble.py` 重生成 exit 0、`--check` 七份全部「一致」exit 0。
- **逐項抽驗路由**：`BOJ 官方英文站` 只進 A；`段數分佈很寬`（Fierce）只進 G；
  `startAt 沒有被嚴格遵守`（鉅亨）只進 F；分頁與 `[BLOCKED]` 兩個通用節**七份都有**。
- `python3 -m py_compile tools/*.py checks/*.py systems/*.py kbcore/*.py scripts/advisory/slice_preamble.py` exit 0。
- 檢查自檢（fixture ＋ near_miss）**失敗數 0**。
- `advisory_verify.py` 對當期 `data/2026-09-09.json`：**18 PASS · 1 WARN · 0 FAIL**
  （與當日發布前逐字相同 —— 本輪沒有動 `checks/` 或 `anchors.json`，**不變才是對的**）。
- `advisory/anchors.json` 可 `json.load`；`data/*.json` **41 檔全部可讀**；
  資料 repo 內 symlink **0 個**。
- `SKILL.md` 對 `preamble` 各節「現行結論」的引用只有一處（第六節、Barron's 計量制），
  **本輪沒有動那個結論**，五處反向指標全部仍在 —— 無「正本改了而讀者沒跟上」的漂移。

**怎麼倒回去**

七份切片是生成物，**倒回去只需要還原 `preamble.md` 與 `slice_preamble.py` 再重跑一次**。
`SKILL.md` 三處都是新增段落、沒有刪改既有規則，逐段刪除即可。
本輪**沒有動** `anchors.json`、`checks/`、`systems/`、`tools/`、`index.html` 與任何 `data/*.json`，
所以倒回去不會牽動檢查、發布或歷史封存。

**當時已知的風險**

①**切片器擋下了我，而它擋的正是這次唯一會讓人瞎掉的錯。**
新增 BOJ 那一列之後第一次跑，`slice_preamble.py` 直接 exit 1：
`選擇器表這一列對不到任何採集員 … **丟掉它等於讓某一組瞎掉，所以這裡不容許猜。**`
**關鍵是它同時做對了兩件事**：拒絕猜、而且**一份切片都沒有寫出去**（七份大小當場確認未變）。
**如果它選擇「猜一個」或「靜默丟掉這一列」，症狀會是 A 的切片裡沒有 BOJ，
而 A 明天照樣交得出東西 —— 沒有任何東西會報錯。**
`ROW_OVERRIDE` 這個機制的存在理由就是它：roster 把四家央行寫成一個字串
（`央行官方（Fed／BOJ／ECB／美國財政部）`），所以每加一家官方站都要在這裡對映一次。
**下一個加央行來源的人會再撞到同一個 exit 1，那是設計成這樣的。**

②**我自己的待修清單第 15 條寫得不準，而它差點變成一次假的「第二次觀測」。**
報告寫「CME 結算落後是第二次觀測，該列點名要三次才寫成常數」，
**而 `preamble` 第七之二那一列其實已經有這條規則了**，還比報告更完整：
08-29 與 09-04 兩次實測已經定出機制（當日結算美東 23:55 CT 才貼、換台北是次日 12:55）。
今天真正新增的是**連假會把落後從一個交易日拉成兩個日曆日**，那是細化不是計數。
**真正只有一次觀測、仍在等第二次的是另一件事**：09-06 量到「能源三頁比金屬頁更落後」，
而今天沒有同時量金屬頁 —— **已在該列明寫「不要拿今天這一筆當成第二次」**。
**記這一筆是因為那個錯誤的形狀很常見**：寫待修事項時憑印象認定「文件裡還沒有這條」，
而它已經有了、只是自己沒讀到那一列的末段。**成本是差點在計數器上多加一格，而那會讓門檻提早滿足。**

③**第 19 條指的「素材檔模板」不存在，所以那兩條護欄被搬到了 `SKILL.md` 步驟 6。**
素材清單是每一輪現寫的暫時產物，沒有模板檔可以改。
**這代表那兩條護欄仍然是「靠執行者在寫清單的當下想起來」的** ——
與 ①那種「跑不過就停」的機械護欄不是同一個等級。
兩條的根因（配額與汰除建議互相矛盾、汰除了前一版 `watch` 指名的素材）
**當天都是撰寫分身不照做才擋下來的**，形狀與 2026-08-31 那三次相同。
**要機械化，得讓素材清單變成一個有結構的檔（例如 JSON）並加一條檢查**，本輪沒做。

④**BOJ 那一列刻意寫成「待查」而不是結論，而這會讓它在表上看起來很奇怪。**
它的末欄是 `09-09（待查）`，內文明寫三種未分辨的可能與「下一輪請對照日文站」。
**這違反了選擇器表其餘每一列的慣例**（其他列記的都是已驗證的取法）。
留成這樣是刻意的：**寫「BOJ 停更」會被下一輪當成事實接手，而分辨的成本只是換一個 client 再問一次**。
**但代價要講清楚**：一列「待查」如果沒有人在下一輪真的去查，它會就這樣留在表上變成雜訊，
而 9/18 BOJ 議息之後就沒有人會記得它。**這一列有沒有被判掉，是下一輪維護的可查證指標。**

⑤**本輪一條檢查都沒有新增，所以「這些觀測有沒有被遵守」仍然沒有任何機器在看。**
十四條來源觀測全部是寫給採集員讀的散文，`advisory_verify` 的 19 條檢查一條都碰不到它們。
**偵測方式仍然只有一種：下一輪的採集員撞到同一個坑，然後在「下游應知」裡再寫一次。**
這不是本輪的疏忽（選擇器眉角本來就難機械驗證），**但它說明了為什麼那七份回報的最後一段最貴** ——
它是這一整類問題唯一的感測器。
