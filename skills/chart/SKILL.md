---
name: chart-daily
description: 產出每日五圖。每天台北 11:30 在 Mac mini 上執行；也可在互動對話說「跑今天的五圖」手動觸發。
---

# 每日五圖｜今天怎麼做出來

**什麼算對的產出**在 `chart/BRIEF.md`；**每一個數字**在 `chart/anchors.json`；
**取數眉角**在 `chart/SOURCES.md` —— 第 3 步才需要，所以它是一份靠指標到達的參考檔，不隨每次執行載入。

排程開的是**全新對話、沒有任何記憶** —— 這份文件與它指到的三個檔就是全部的輸入。

**這份文件的正本在 `kb-core/skills/chart/SKILL.md`。** 排程裡那一份是它的副本，
改動一律先改版控那份再整份貼過去。

## 這一輪的地形

| 段 | 在哪 | 有什麼 | 沒有什麼 |
|---|---|---|---|
| 預抓 | Mac launchd（11:00） | 網路、FRED／Tiingo／證交所 | LLM |
| 選題與撰寫 | 這一輪（11:30，Mac 桌面版） | LLM、上游投顧的當日 JSON | 網路寫入、git |
| 發布 | Mac launchd（`publish.py`） | 網路、SSH 金鑰 | LLM |

**這一輪不推 GitHub，也不跑任何 git 指令。** 產出寫進 `~/outbox/chart`，
`publish` 每分鐘掃一次、跑檢查當閘門、原子寫入、rebase、push、寫回執。

---

## 步驟

### 1. 清點手上有什麼

讀 `chart/anchors.json`。讀上游投顧的當日 JSON（題材來源）。
讀預抓狀態檔，確認它在 `anchors.prefetch.status_valid_hours` 之內。

**上游還沒好就等**（`anchors.schedule.upstream_wait_minutes`），仍無則用前一日並在 `about.run` 註明。

**完成條件**：手上有一張表 —— 今天是星期幾、該出哪一條軌道、預抓涵蓋到哪些序列、
以及上游今天給了什麼題材。

### 2. 選五個題目

五個 slot 順序固定，值在 `anchors.structure`。三條硬規則：

- **五張不得有兩張同 `theme`**（值域是投顧的十五組，不是另一份清單）
- **slot 1、2、4 必須是當天的事**；素材不足改「前瞻框架」並記錄，不是硬湊
- **slot 3 的原題七天內，但我們的圖必須用最新資料**

軌道圖那一張**由星期決定**（`anchors.tracks`），週末改週線複查、
**週六與週日不得挑同一條**。

**完成條件**：五個題目各自能說出「它要回答什麼問題」，且 theme 互不重複。

### 3. 取數

每張圖在 `series_spec` 寫明代號與轉換，由取數工具實體化資料點。

**開工前把 `chart/SOURCES.md` 讀一遍。** 那裡面每一條都是實測撞出來的，而其中一半的失效方式是安靜的 —— 例如櫃買被節流時回的是空 body 而不是錯誤碼，**空不是「沒有資料」，是被擋**。

**代理必須在 `series_spec` 明寫 ETF 代號**（對照表在 `anchors.proxies`），
**不由取數層自動替換** —— 曾經寫成「`^SOX` 取不到就改抓 `SOXQ`」，
那會把 ETF 的資料寫進指數的快取，**圖上標的是指數、數字卻是 ETF**。

**429 是對方在說「慢一點」。等，不要繞。** 不可以換 host、改用瀏覽器規避。
先分辨是哪一種：**第一個請求就 429 等再久也不會好**，打了一陣子才 429 才是該等的那種。

**完成條件**：每條序列都有資料點與末日，且末日通過 `anchors.freshness` 的門檻。

### 4. 過一次 QA

跑 `anchors.quality` 的 5σ 檢查。每個旗標要分類並在 `about` 記處置：
真實事件／轉倉／錯價／衍生序列的窗口效應。

**先看幅度再看故事**：轉倉價差正常在 1% 以內，所以「跌 11% 是轉倉」在數量級上就不成立。

**完成條件**：每個旗標都有具名的處置，沒有「不確定」。

### 5. 寫判讀

長度區間在 `anchors.lengths`。`BRIEF.md` 第五節那幾條是這一步的判準，其中最常違反的三條：

- **數字一律來自我們自己算出來的序列**，不是抄新聞
- **不要引用本圖序列涵蓋不到的日期** —— 若那個數字其實是抄來的，刪掉它
- **類比必須成對出現：相同點＋關鍵差異**，寫不出關鍵差異就把它刪掉

**完成條件**：`BRIEF.md` 第六節的五條當期失敗判準逐條檢查過，每一條都能說出結果。

### 6. 出圖

兩軌**從同一份 spec 渲染**，日檔不存 option（值在 `anchors.rendering`）。

**首次使用任何一種新圖型的當天，一定要實載網頁看一眼** ——
檢查程式只檢查欄位齊不齊，不會把資料餵進 ECharts。
2026-08-12 gauge 的 markLine 在無直角座標系上丟例外，
而前端只有一層 catch，**一張圖丟例外就整頁死掉**。

**完成條件**：五張圖的 PNG 與 SVG 都產出了，且新圖型（如果有）在瀏覽器裡看過。

### 7. 交出草稿

寫成 `~/outbox/chart/<今天>.draft.json`。

**暫存腳本一律寫在專屬輸出目錄、檔名帶日期，並且確認寫檔成功再執行。**
2026-08-12 事故：腳本命名 `/tmp/mk.py` 撞上前一輪殘留的同名檔，
`cat >` 因權限失敗但 `python3 /tmp/mk.py` 照樣跑了**舊的那一份**，
把當期 JSON 覆寫成 15KB 空殼，**180 秒內被推上線**。
**`/tmp` 的檔案會跨輪次殘留，而寫檔失敗與執行成功是兩件獨立的事。**

寫完用 `wc -c` 確認落地。

**完成條件**：`wc -c` 的位元組數與預期相符，且 JSON 可被 `json.load` 讀開。

### 8. 等回執

`publish` 跑完會寫 `~/outbox/chart/<今天>.receipt.json`。讀它的 `exit`：

| 退出碼 | 意思 | 這一輪要做的事 |
|---|---|---|
| 0 | 已發布 | 收工，把 commit 記進交付訊息 |
| 10 | 內容沒過檢查 | 看 detail 指的是哪一條，改草稿重寫，回步驟 5 |
| 11 | 今天的檔已存在且內容不同 | **不要改草稿** —— 已發布的一期就是已發布的樣子，掛 errata |
| 12 | 輸入或目的地壞掉 | 停下來回報 |
| 14 | 網路或 SSH | 等下一輪自動重試；**它會自己完成** |
| 15 | rebase 衝突 | 停下來回報 —— 重跑不會好 |

**沒有回執**與**回執說失敗**是兩件不同的事：前者代表 `publish` 根本沒跑。

**完成條件**：手上有一份回執，且它的 `exit` 有被讀過並依上表處置。

### 9. 執行報告

分段耗時（每一步的起訖台北時刻）、`about.data_path` 走了哪一條、
QA 旗標與處置、降級與理由、以及下一輪要修的事。

**完成條件**：報告裡每一個時間數字都能指出是量到的還是估的，估的標 `~`。

---

## 進度落後時砍什麼

依序砍，一次砍一項：

1. **slot 4（主題深掘）改用已在預抓涵蓋內的序列**，不另外取數
2. **slot 3 改用更輕的圖型**（序列點數少的）
3. **判讀取區間下緣**

**五張圖、theme 不重複、軌道對星期不在這張清單上。** 它們是這套系統的定義。

## 這一輪不做的事

- **不推 GitHub** —— 那是 `publish` 的事
- **不跑 git 指令**（含 `git status`）—— 沙箱刪不掉 `.git/index.lock`
- **不改既有的日期檔** —— 每天只新增一個
- **不轉載媒體的圖** —— 取材可以，資料一律自己抓、自己算
