# 執行修改

第 4 步的細節。**拿到使用者對「要改什麼」的確認之後才讀這份。**

## 改哪裡

| 要改的東西 | 動這個檔 | 連帶 |
|---|---|---|
| 什麼算對的產出、失敗判準 | `kb-core/podcast/BRIEF.md` | 判準若引用門檻，門檻要在 `anchors.json` |
| 任何一個數字或門檻 | `kb-core/podcast/anchors.json` | 讀它的檢查與 `preamble.md` 一起看 |
| 每天怎麼跑（流程或分支） | `kb-core/scripts/podcast/DIGEST-PROMPT.md`（**正本**） | **改完整份貼進排程 `SKILL.md`**，見下方 |
| 撰寫 subagent 的規則 | `kb-core/scripts/podcast/preamble.md` | 與 `BRIEF`／`anchors` 的重疊處兩邊都要改（見 `FILES.md`） |
| 發布前檢查 | `kb-core/checks/podcast.py` | 見下方「新增檢查」 |
| 節目清單、官方稿入口 | `AGENT_BRIEF.md` 第 1 節＋見下方「節目清單」 | `shows.json` **有兩份**（kb-core 與 `~/.podfetch/`），沙箱通常只看得到一份 |
| podfetch 行為 | `~/kb-core/scripts/podcast/` 的 `podfetch.py` 與 `config.json`（**不是 `~/.podfetch/`**，那裡只剩執行期狀態） | 見下方「podfetch」 |
| 已知的坑與待辦 | `MAINTENANCE.md` 第 4、6 節 | |

> **2026-08-22 改寫**：上一版第一列寫「規格與判斷規則 → `AGENT_BRIEF.md`」，
> 而 brief 早在 08-21 就被降級成只剩四塊權威。照舊表改，會改到沒在跑的那一份。

## 排程 SKILL.md

它對檔案工具唯讀，只能走 `update_scheduled_task`，而 `prompt` 是**整份覆寫**：先 Read 目前檔案，以它為基礎修改後送出完整內容。拿記憶中的版本重寫＝覆寫事故（來歷見 `MAINTENANCE.md` 第 7 節）。

## brief 的預算量測史

`podcast/BRIEF.md` 的 `budget.tokens` 是 5,000，**改本檔就重量一次**：

```
python3 tools/repo_check.py .
```

| 日期 | 字元 | ~token | 備註 |
|---|---|---|---|
| 08-19 | 3,327 | 3,025 | 首測。預算 5,000 取的是它的約 1.65 倍 |
| 08-22 | 4,694 | 4,267 | |
| 08-23 早 | 5,309 | 4,826 | 同批加了 937 字元、當場量到 5,119 超標，壓縮第八節收斂回來 |
| 08-23 午 | 5,846 | 5,315 | **超標**。加完沒有重量 —— 08-18 立的「加字之後量一次」又漏了一次 |

字元／token 比取 1.1（出處：工單 11 量測舊 brief 38,570 字元 ~35k token）。
舊的 `AGENT_BRIEF` 實測 25,120 token、截斷於第 417 行 —— **那是這條預算存在的理由**。

> **這張表 2026-08-23 從 brief 的 frontmatter 搬過來。** 它原本是 `source:` 那一行，
> 473 字元 ≈ 430 token ≈ 預算的 8.6% —— 而當時超標的量是 315 token。
> **量測紀錄長在被量的東西裡面，每量一次就把它推近上限一點。**
> 儀器要住在被量的東西外面。

### 2026-08-23 為了回到預算內移出 brief 的紀錄

**規則全部留在 brief，移走的只有來歷**（照本節「只搬紀錄，規則留在 brief」）：

- **`quotes[].by` 一度在 brief 裡寫成 `speaker`**，而實檔與 `systems/podcast.py`
  都用 `by`，08-23 訂正。brief 現在只留「是 `by` 不是 `speaker`，寫錯會安靜落空」。
- **08-20 首輪 63 條金句全部通過機械比對** —— `original` 那條規則的首次驗證。
- **`trackId` 與 `minutes` 在 08-23 之前沒有寫進 brief**，只存在於實檔裡；
  兩個都是閘門的必要輸入，補寫時一起把「漏掉的後果不一樣」講清楚。

## brief 第 8 節歸檔

開工時就把 brief 第 8 節的非當日條目移到 `MAINTENANCE.md` 第 11 節（由新到舊插在最前）。**只搬紀錄，規則留在 brief。** 移完打開第 11 節確認條目真的在它底下——曾兩次留在第 7 節，而 brief 仍宣稱已歸檔。

## 第 12 節登記簿

依序做完三件事才算動過登記簿，缺一都算沒做：

1. 把上一列結案（本場驗證了它嗎？）
2. 新增本場那一列
3. 三個 bullet 逐條重讀，看有沒有列該進來或該移類

改防護機制前，先看第 12 節登記簿「後來成立嗎」那一欄（次數與清單以那張表為準）。共同點都是正常路徑有效、極端路徑放大傷害。動手前問：**它在最壞情況下會怎麼失敗？**

## 節目清單

照 `MAINTENANCE.md` 第 5 節的完整步驟表逐步走，清單以那張表為準。撞色**三處分別檢查**，與最近鄰 RGB 距離 <20 一定換色。

## podfetch

設定檔不一定管得到程式邏輯。動任何「順序」「優先權」「門檻」類設定前，先讀程式確認它讀得到這個值、以及它實際接到哪條分支。

## 新增檢查

新增任何檢查時，先問「正常情況會不會也觸發」。**要求兩個獨立訊號同時成立才報**——單獨看必然有雜訊的訊號被當成獨立判準，初版就會對 9 集報 8 集、對 72 集報 15 集。能寫成條件的判斷就寫成條件，叮嚀會被忽略。

## 驗證

**全數通過才算完成**；任何一項失敗就回上一步。

1. 重跑 `python3 ~/kb-core/scripts/podcast/healthcheck.py`，確認沒有新的 FAIL。**在沙箱裡跑時，`shows.json 兩份`／`節目在文件裡`／`podfetch` 三條會固定 WARN 跳過，等於沒驗到**——動過那三條涵蓋的東西就要在 Mac 上再跑一次。
2. **再叫一次子代理**照 [`SYNC-CHECKLIST.md`](SYNC-CHECKLIST.md) 獨立比對，確認這次改動沒有製造新的不同步、沒有弄丟關鍵規則、也沒有覆寫掉別場維護的成果。若這次改了任何指標、門檻或因果宣稱，明確指定子代理去讀程式確認那個值實際接到哪條分支——文件對讀查不出「理由寫錯」這種矛盾。
3. 這次新增的任何量測或自動化，當場驗證它回傳非空結果（空值與 0 都算失敗）。
4. ~~收工快照~~（2026-08-22 退役）。改成確認 kb-core 推得出去：`tail -5 ~/outbox/kbcorepush.log`。**若看到「檢查自檢有失敗的條目」就是新加的檢查沒過 `fixture`／`near_miss` 契約，它會擋住之後所有 kb-core 推送**——`fixture` 要能觸發檢查（非 PASS），`near_miss` 要剛好通過（PASS），兩者方向相反，很容易寫反。
5. 驗線上狀態時，網址帶 cache-buster 並確認 `updatedLabel`。
